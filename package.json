{
  "name": "miami-dj-beat-platform",
  "private": true,
  "description": "Monorepo root: Vercel may build from here if Root Directory is not set to web/.",
  "scripts": {
    "build": "cd web && npm run build"
  }
}
