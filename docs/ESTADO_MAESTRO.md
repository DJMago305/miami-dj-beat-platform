# ESTADO MAESTRO — MIAMI DJ BEAT LLC (SSOT)
Última actualización: 2026-09-01
Estado general: Operativo / En consolidación

## 1. Módulos y Estado Técnico
- [x] Motor de Contratos y W-9 (Legal Engine) — núcleo fusionado (PR #244), 2 pendientes visuales abajo
      — 2026-08-23 IMPLEMENTADO Y CORREGIDO (Hilo Maestro, continuando el trabajo
      del Hilo Legal & W-9 Engine): `web/contracts-engine.html` — motor bilingüe
      ES/EN con 5 plantillas (DJ, Corporativo, Venue, Staff, W-9). Conectado a
      `staff.html` y a `dj-profile.html` ("Mis Documentos").
      **El W-9 NO se recreó en HTML/CSS** — un documento fiscal del gobierno no
      se reconstruye, se muestra tal cual: el PDF real (idéntico byte a byte al
      oficial de irs.gov, verificado por SHA-256) queda embebido en un visor,
      con el link directo a `irs.gov/pub/irs-pdf/fw9.pdf` como respaldo. El
      artista lo descarga, lo llena y firma con su propio lector de PDF, y lo
      vuelve a subir por un dropzone (solo `.pdf`, máx. 10MB) que calcula su
      SHA-256 en el navegador (`crypto.subtle`) antes de guardarlo — nunca se
      genera ni se rellena un PDF por código.
      **Acceso reubicado**: "Legal / Contratos" vive únicamente en el menú
      lateral de `staff.html` (grupo Operaciones, mismo patrón que "Inbox ·
      Tickets") — la barra superior quedó intacta, sin tocar (diff verificado).
      **Dos bugs reales de producción encontrados y corregidos en vivo** (ref
      `hkuvuqupbxwkiykxvqdr`): (1) la firma de parámetros de
      `guardar_contrato_firmado` no coincidía — corregida tras verificar los 10
      parámetros reales vía `information_schema.parameters`; sobraba un
      `p_status` que no existe en la función. (2) `signed_contracts.contract_type`
      tiene su propio `CHECK` que solo acepta `W9`/`DJ_AGREEMENT`/
      `VENUE_AGREEMENT`/`CORPORATE_AGREEMENT`/`STAFF_AGREEMENT` — los ids locales
      de plantilla (`w9`, `dj`, `venue`...) no coinciden con esos valores.
      **Esto afectaba a los 5 templates, no solo al W-9** — nadie podía guardar
      remotamente antes de este fix. Corregido con una tabla de mapeo
      (`LOCAL_TO_DB_CONTRACT_TYPE`) en el único punto compartido de guardado.
      Sin verificación end-to-end autenticada dentro del contenedor real de
      `staff.html`/`dj-profile.html` con sesión de staff/artista (Regla 4) — no
      hubo credenciales disponibles en esta sesión de build; sí se probó en
      vivo contra producción el flujo de guardado (hash real + llamada RPC real,
      sin insertar filas de prueba por decisión del PO). Sintaxis validada con
      `node --check`/parseo de scripts en los 4 archivos tocados: sin errores.
      **PR #244 fusionado a `main` (2026-08-23).**
      — 2026-08-23 HOMOLOGACIÓN VISUAL (Hilo Maestro): tema oscuro permanente
      aplicado a `contracts-engine.html` con los tokens reales de `staff.html`
      (`--gold:#c5a059`, `--bg:#050810`) + `backdrop-filter` (glassmorphism)
      en topbar, tarjetas de plantilla, panel de campos, marco del documento y
      dropzone del W-9. El "papel" de los documentos (contrato, PDF del W-9)
      se queda claro a propósito — es el papel dentro del marco oscuro, no el
      chrome de la herramienta. Commiteado como checkpoint local
      (`d9d5922`), sin PR nuevo abierto todavía.
      **⏸️ ARQUITECTURA APROBADA PARA LA PRÓXIMA SESIÓN DE MAQUILLAJE VISUAL**
      (PO 2026-08-23, reemplaza el punteo genérico anterior — esta es la
      versión concreta a construir):
      1. **Nuevo acordeón `⚖️ Bóveda Legal` en el sidebar de Staff** (mismo
         nivel que Gobernanza/Clientes/Equipo/Operaciones/Eventos, no un link
         suelto dentro de Operaciones como quedó hoy). Sus sub-ítems son la
         selección de plantilla, hoy dentro del propio HTML del motor:
         - 🎧 Contrato Artista / DJ
         - 🏢 Eventos Corporativos
         - 🏛️ Venue / Residencia
         - 🎚️ Staff / Operador Técnico
         - 🧾 Formulario IRS W-9
         Click en un sub-ítem → carga esa plantilla en el área principal de
         trabajo (mismo mecanismo de panel que ya usan Gobernanza/Inbox).
      2. **Limpieza del área principal de `contracts-engine.html`**: quitar
         la columna "TEMPLATES" interna (ya no hace falta — la selección vive
         en el sidebar) para que el formulario y el visualizador del
         documento usen el 100% del ancho.
      3. **Quitar la barra superior beige/propia** (ES/EN/Marca) del motor —
         debe vivir bajo el header nativo de `staff.html`, sin una segunda
         cabecera encima.
      Punto 2 del checkpoint anterior (logo oficial en las plantillas) sigue
      pendiente, sin cambios.
      Trabajo en rama `feature/contracts-w9-engine-integration` (misma rama,
      reutilizada tras el merge — commits nuevos siguen ahí, sin PR nuevo
      abierto todavía).
- [x] Motor de Voz Realtime ELIXIS (PR #202 desplegado en producción)
- [x] Políticas de Cuota y RBAC (3h Full / 5h Mini / Fallback a texto)
- [x] Despacho SMS Seguro (`elixis_sms_pending` + validación E.164 + Twilio, verificado con envío real)
- [x] Saneamiento de Marca (Retirado SoundCaribe, unificado Miami DJ Beat LLC)
- [x] Ejecución de SQL de Memoria Persistente en Supabase (`elixis_memoria_PRODUCCION.sql`)
      — 2026-08-22. Instalado y VERIFICADO en producción (ref hkuvuqupbxwkiykxvqdr):
      escribir ok=true · recordar=1 · olvidar=true.
      Objetos activos: `elixis_memory_facts` (voz), `agent_memory` (texto),
      vista unificada `dj_memory_facts` y funciones write/forget/recall/upsert (service_role).
- [ ] Conexión Stripe Connect Artistas (Sub-hilo Financiero)
      — 2026-08-22 DIAGNÓSTICO (Hilo Business Financial Intelligence, verificado por el
      Hilo Maestro): NO existe infraestructura Connect en el repo — cero
      `stripe_account_id`, cero eventos Connect en `stripe-webhook`, cero
      `transfer`/`accounts.create`. Sí existe el modelo de datos (`financial_payables`
      con payee DJ_PROFILE) pero no el motor que mueve dinero real.
      ⚠️ BLOQUEANTE antes de construir: hay dos ledgers de balance de artista sin
      reconciliar — `dj_ledger` (legacy) y `financial_payables`/`financial_owner_ledger_entries`
      (canónico nuevo). Decisión pendiente del PO: cuál es la fuente de verdad,
      antes de conectar Stripe Connect encima de cualquiera de los dos.
      — 2026-08-22 RESUELTO (Hilo Business Financial Intelligence): SSOT formal —
      `financial_payables`/`financial_payments`/`financial_owner_ledger_entries` son
      la única fuente de verdad de balances y payouts. `dj_ledger` queda marcado
      para deprecación progresiva (sin borrar nada, sin migración disparada aún).
      Stripe Connect diferido a sprint dedicado, post-cierre de la matriz de
      contenedores.
- [ ] Reconciliación de la Agenda del Artista (Sub-hilo Road Master Map / Calendario BI)
      — 2026-08-22 HALLAZGO (Hilo Maestro): el calendario rediseñado y aprobado
      (`calendario-operacional-inteligente.html`, 10–14 ago) no lee `artist_agenda`
      (16-ago, R9a/R9b) — lee `leads`, igual que `staff-agenda.html`. El write-path
      real que resuelve "la reserva no escribe en el calendario personal" existe
      por un camino paralelo: `web/dj-dashboard.html` (sección "Calendario
      Personal", RLS `dj_user_id = auth.uid()`) y las dos tools de `elixis-chat`
      (`consultar_agenda_artista` / `registrar_evento_agenda`).
      — 2026-08-22 PRECISIÓN (Hilo Road Master Map, verificado por el Hilo Maestro
      en el código): `dj_events` (legacy, mar-2026) no tiene NINGÚN lector ni
      escritor en toda la plataforma — ni web, ni edge functions. El único rastro
      es un comentario en `elixis-chat` explicando por qué no se usa ("dj_events
      está vacío"). No participa del riesgo de consistencia: `elixis-chat` solo
      cruza dos fuentes reales, `leads`/`event_builder_orders` y `artist_agenda`
      — no tres. Sigue siendo una pregunta legítima de consistencia entre esas dos.
      — 2026-08-22 CUARTA PIEZA (Hilo Road Master Map, verificado por el Hilo
      Maestro): la misma línea de comentario que descartó a `dj_events` nombra
      dónde viven las reservas reales — `event_builder_orders` (la tabla del
      disparador de avisos instalado hoy, estados `confirmed`/`cancelled`).
      Tiene 8 consumidores reales (`elixis-chat`, `stripe-webhook`,
      `admin-dashboard.html`, `staff-admin.html`, `staff-order.html`,
      `client-portal.js`, `mdj-event-builder.js`, `production-module.js`) y
      **cero** en las 3 pantallas de agenda. Enlaza con `leads` por FK nullable
      (`event_builder_orders.lead_id → leads.id ON DELETE SET NULL`) — una orden
      confirmada y pagada sin `lead_id` (o con el lead borrado) sería invisible
      para cualquier calendario que solo lea `leads`.
      **Recomendación (Hilo Road Master Map):** la pregunta de SSOT de agenda no
      es de tres piezas sino de dos ejes — (1) **reserva**: `leads` (lo que leen
      hoy las pantallas) frente a `event_builder_orders` (la orden pagada, Stripe
      y el disparador de avisos) hay que reconciliarlos antes de fijar uno solo;
      (2) **proyección personal**: `artist_agenda`, ya resuelta como derivada,
      alimentada de forma idempotente por R9b. `dj_events` queda como baja simple
      — cero consumidores de código, nada que desconectar antes. Decisión final
      de SSOT (reserva) pendiente del PO.
      — 2026-08-22 VERIFICADO EN PRODUCCIÓN (Hilo Road Master Map, ref
      hkuvuqupbxwkiykxvqdr, guion de solo lectura de 25 filas): `artist_agenda`/R9a/R9b
      está aplicado **entero**, nada a medias — funciones `artist_agenda_record` y
      `artist_agenda_record_from_assignment`, `is_staff`, el índice único
      `artist_agenda_assignment_lead_dj` (garantiza que R9b no duplique al
      reasignar), RLS activo con sus dos políticas (staff / artista sobre lo
      suyo), y las 4 columnas del puente de identidad — todo `true`. Las 3 tablas
      fantasma del encargo original (`events`, `agenda_locks`, `dj_assignments`)
      confirmadas `false` también en la base, no solo en el código.
      — 2026-08-22 DECISIÓN DEL PO: **R9b NO es la solución definitiva por sí solo.**
      Es necesario integrar lo que R9b ya escribe en `artist_agenda` DENTRO del
      calendario rediseñado (`calendario-operacional-inteligente.html`) — no basta
      con que la reserva aparezca solo en la lista simple de `dj-dashboard.html`.
      **R9 del mapa (Road Master Map) queda ABIERTA** — no se cierra hasta que esa
      integración exista. Próximo paso técnico: que el calendario rediseñado lea
      `artist_agenda` (o su equivalente tras resolver el SSOT de reserva de arriba)
      además de `leads`, en la vista de artista.
      — 2026-08-22 SSOT DE RESERVA RESUELTO (PO): **`event_builder_orders` gana**
      sobre `leads` como fuente de verdad comercial de la reserva. Toda orden
      confirmada en `event_builder_orders` debe materializarse en `artist_agenda`
      (resolviendo la junta de identidad `leads.assigned_dj_id`/`dj_profiles.id`
      → `auth.users.id`). `artist_agenda` sigue siendo la proyección personal del
      artista, no la fuente. `dj_events` queda marcada para DROP en la próxima
      migración de limpieza (cero consumidores de código, ya verificado).
      — 2026-08-22 GUION DE BAJA ARCHIVADO: `supabase/scripts/cleanup_dj_events.sql`
      (PR #233, fusionado) — 4 pasos, no un DROP directo: auditoría de dependencias
      en solo lectura (filas, FKs entrantes, vistas), copia de resguardo
      (`dj_events_archive_20260822`), el `DROP TABLE` comentado a propósito, y
      verificación posterior. Vive en `supabase/scripts/`, no en
      `supabase/migrations/` — disponible para **ejecución manual controlada**,
      ningún `supabase db push` lo corre solo. Pendiente: que el PO lo ejecute
      línea por línea en producción cuando lo decida.
      — 2026-08-22 POLÍTICA (PO): la agenda de STAFF es la **master** (Matrix/Owner
      — `calendario-operacional-inteligente.html` en modo staff); la de artistas
      y clientes es **personal** (aislada por RLS, cada quien ve solo lo suyo).
      Encaja con lo ya construido para el artista (`artist_agenda`, RLS
      `dj_user_id = auth.uid()`). El calendario de CLIENTE todavía no existe —
      queda anotado como tarea futura, no se construye ahora.
- [ ] Calendario de Cliente (futuro, sin dominio asignado todavía)
      — 2026-08-22 REGISTRADO (PO): no existe todavía. Anotado como tarea para
      más adelante, siguiendo la política master/personal de arriba — el
      cliente vería su propia agenda personal, aislada, igual que el artista.
- [ ] Bug de layout: clima encogido en `staff.html?vista=agenda` (sin dominio activo)
      — 2026-08-22 HALLAZGO (Hilo Maestro, en vivo con el PO): a ancho de
      escritorio, `web/weather-experience/` (embebido dentro de `staff-agenda.html`)
      se renderiza encogido en una tarjeta angosta arriba a la izquierda en vez
      de usar el ancho disponible; el calendario grande sí carga más abajo, pero
      es fácil no llegar a verlo. Localizado en `web/weather-experience/styles.css`
      (`.center`/`.rail`/`.exp`) — no es un problema de datos ni de sesión, es de
      layout responsivo del propio módulo. Dominio #5 (Weather Design Bible / UI)
      de la matriz — sin hilo activo en ese dominio ahora mismo. No causado por
      el rename de marca de hoy (verificado). Sin arreglar todavía.
- [x] Centro Legal (LC-12/13A/13B) — auditoría forense cerrada, riesgo neutralizado
      — 2026-08-22 HALLAZGO (dominio #3, verificado por el Hilo Maestro y en
      producción real por el PO): son 9 tablas `legal_*` (la auditoría de julio
      decía 8 — su sonda truncaba nombres largos), cero consumidores de código
      en `web/` ni `supabase/functions/`, diseñadas contra `MiamiDJBeat-MigracionV2`
      (repo que nunca aterrizó). Las 3 migraciones se autodeclaran "NOT APPLIED"/
      "isolated Postgres validation" en sus propias cabeceras.
      ⚠️ **Riesgo encontrado, con severidad corregida tras verificar en
      producción:** `20260722101300_..._lc13a.sql` redefine `auth.uid()` con un
      stub de laboratorio sin el respaldo JSONB (`request.jwt.claims`) que sí
      tiene la función real — confirmado en producción: `auth.uid()` real usa
      `coalesce(...)` entre `request.jwt.claim.sub` y `request.jwt.claims->>'sub'`;
      el stub solo lee la primera rama. **Pero el escenario catastrófico queda
      DESCARTADO**: `auth.uid()` real es propiedad de `supabase_auth_admin`, no
      del rol que ejecuta las migraciones — `CREATE OR REPLACE FUNCTION
      auth.uid()` fallaría con "must be owner of function uid". `LC-13A` no
      puede ejecutarse en Supabase alojado tal como está escrita; sus propias
      cabeceras ya lo decían ("isolated Postgres validation" — se escribió para
      un Postgres local). El daño real, más pequeño pero concreto: un
      `supabase db push` aplicaría LC-12 con éxito (7 tablas + secuencia) y
      luego LC-13A abortaría en su línea 12, dejando 7 de 9 tablas huérfanas
      (sin RLS, sin las funciones que las protegen) y la cadena de migraciones
      en estado fallido, bloqueando la siguiente migración legítima. Las
      migraciones vivían en `supabase/migrations/` — la carpeta que
      `supabase db push` ejecuta sin preguntar — de ahí que moverlas siguiera
      siendo lo correcto, aunque el riesgo real sea de cadena rota, no de
      autenticación caída.
      **Verificado en producción real (PO):** cero estado parcial — las 9 tablas,
      la secuencia y las 22 funciones legales salen `AUSENTE` sin excepción. No es
      una migración a medias, es un módulo entero que nunca tocó la base.
      **Acción tomada:** las 3 migraciones se movieron a
      `supabase/scripts/legal-center-design/` (con README explicando el porqué y
      los pasos antes de reescribir) — solo rename, cero contenido SQL modificado.
      Riesgo vivo neutralizado sin perder el diseño. No se aplica ni se borra
      todavía; se reescribe cuando exista producto real, sustituyendo el stub por
      identidad real y retirando las dos tablas de laboratorio `legal_lc13*`.
      **Nota de gobernanza:** el Centro Legal no tenía dominio en
      `docs/JURISDICCIONES.md` — cae dentro del #3 (BFI/Artist Financial,
      "reportes de ingresos y contratos"), pero no estaba registrado
      explícitamente. Pendiente formalizarlo si vuelve a activarse.
- [x] `platform_incidents` — núcleo de registro inmutable de incidentes técnicos/UI (PR #235, fusionado)
      — 2026-08-23 (Hilo Maestro, contrato de datos aprobado por el PO antes de
      escribir código): tabla `platform_incidents` con RLS activo y **cero
      políticas** — mismo candado que `libro_operaciones`, dominio distinto
      (incidentes de ingeniería/UI, no el diario financiero del artista; son
      tablas separadas, no se mezclan). Única entrada:
      `platform_incidents_reportar()` (SECURITY DEFINER, exige
      `is_staff(auth.uid())`, identidad resuelta en servidor). Única salida:
      la vista `platform_incidents_staff`, filtrada por `is_staff()`. Sin
      UPDATE ni DELETE en ningún camino — un incidente se reporta completo,
      con qué pasó y cómo se solucionó, en una sola fila.
      **Cabecera de entorno: PRUEBA** (`mdjb-ensayo`) — fusionar el PR solo
      subió el archivo al repo; correrlo contra cualquier base sigue siendo
      un paso manual del PO, empezando por el `SELECT to_regclass(...)` de
      comprobación que trae el propio guion.
      **Pendiente, marcado explícitamente en el SQL:** catálogo cerrado de
      `dominio`/`severidad` (hoy texto libre); dónde vive el botón/emoji de
      reporte en la interfaz (decisión de placement de UI, no de este
      contrato); si esta tabla termina alimentando `docs/INCIDENTES.md` o
      coexiste aparte.
- [x] Constitución de Autoridad e Identidad M1–M5 (PR #241, fusionado)
      — 2026-08-23 (Hilo Maestro): 5 migraciones auditadas y fusionadas juntas
      como bloque único por su cadena de dependencia real — M1
      (`mdj_profile_id_inmutable`) → M2 (`audit_log` + `mdj_profile_de_usuario`,
      la función que resuelve identidad de perfil desde `auth.uid()`) → M3
      (`permission_grants`) → M4 (auditoría de dispositivos) → M5
      (`fenix_puede()`, autoridad única). Única dependencia externa,
      verificada por `git grep`: `fenix_can()`, ya sellado en PRUEBA+PROD
      desde el 17-ago (cimentación 2A). Las 5 migraciones estaban sin
      trackear en git desde el inicio de la sesión — se aislaron y
      commitearon limpias, sin arrastrar nada más. Cabecera de entorno:
      **PRUEBA** en las 5 — aplicarlas en producción sigue pendiente de
      autorización expresa del PO.
- [x] Libro de Operaciones — Fase 1 (esquema) + Fase 4 (reconciliación EBO)
      + RPC `get_my_cashflow_ledger` (PR #242, fusionado)
- [x] **Shopping Miami DJ Beat — tienda nativa de merch (PR #287, fusionado 2026-08-28)**
      — Retira la tienda "Plan A" (`web/shop.html` se auto-redirigía a
      `miami-dj-beat-store.myshopify.com`, HTTP 402, cuenta congelada — hallazgo
      cruzado con el hilo de avatares, ya documentado como "V12" en el Road
      Master Map) y reactiva un catálogo nativo (5 productos reales, camiseta/
      gorras/hoodie/sudadera) que ya existía en el archivo, solo enterrado
      detrás del redirect. Checkout real vía Stripe (`create-merch-checkout`,
      re-precificado 100% en servidor — el precio del carrito del cliente
      nunca se confía). **Aislamiento deliberado de llaves**: usa
      `STRIPE_SECRET_KEY_MERCH` propia, NUNCA la `STRIPE_SECRET_KEY`
      compartida por `create-checkout`/`create-course-checkout`/
      `create-event-payment`/etc. — así la tienda de merch se puede probar en
      modo Stripe Test sin arriesgar ni un pago real de suscripción/depósito/
      propina de la plataforma. **`stripe-webhook`** ganó un branch nuevo para
      `checkout.session.completed` de merch: escritura única en `merch_orders`
      solo con el pago ya confirmado (un carrito abandonado nunca genera fila),
      verificando la firma contra la llave live O contra `STRIPE_WEBHOOK_SECRET_MERCH`
      (su propio endpoint de prueba) — la live se revisa primero, así que la
      verificación de eventos reales no se tocó. Tabla `merch_orders` +
      columna `is_read` (para la campana de notificaciones, ver abajo). Nueva
      pantalla `web/staff-merch-orders.html` (lista + "Marcar enviado").
      **Grupo "Pedidos" nuevo en el sidebar de Staff**, con espacio propio (no
      anidado en Operaciones) y 3 accesos directos (Pendientes/Enviados/Todos)
      que comunican con el panel por `postMessage` — mismo patrón que ya usa
      Bóveda Legal para sus plantillas. **Campana de notificaciones** ahora
      también cuenta pedidos sin leer junto a los contratos pendientes de
      firma; el número solo se borra cuando staff abre el panel de Pedidos de
      verdad (columna `is_read`), no al marcar un pedido como enviado.
      **Probado de punta a punta en modo Stripe Test** con el PO en su propia
      sesión: catálogo → carrito → checkout → tarjeta 4242 → webhook → fila en
      `merch_orders` → visible en Staff.
      **Pendiente, no bloqueante:** (1) el modelo de etiqueta de las píldoras
      de estado del pedido — el PO quiere otro estilo, aún sin definir; (2) el
      formulario interno de checkout pide nombre/teléfono/dirección aunque el
      cliente elija Tarjeta, y Stripe se los vuelve a pedir en su propia
      pantalla — redundancia real, ya diagnosticada, sin tocar todavía
      (afecta solo la ruta de Tarjeta; Zelle sí necesita esos datos); (3)
      `shop.html` no se agregó a `MDJ_VISTAS_INTERNAS` — decisión deliberada,
      fuera del alcance de este ticket; (4) `STRIPE_SECRET_KEY_MERCH` sigue en
      modo Test — falta un paso de corte a Live antes de aceptar pagos reales
      de merch (llave nueva `sk_live_` + su propio webhook en modo Live).
- [x] **Config/Inbox·Tickets — header duplicado dentro de Staff (PR #286, fusionado 2026-08-28)**
      — Encontrado en vivo durante la auditoría del ticket de Shopping (arriba):
      `web/account-settings.html` no tenía forma de saber que estaba embebida
      dentro de `staff.html` (vista CONFIG y OPERACIONES→"Inbox · Tickets"), así
      que pintaba su propio `#mainHeader` y una marca flotante
      (`#mdj-flotante-visitante`) ENCIMA del header real del portal. Arreglo:
      mismo patrón `window.self!==window.top` + candado `MutationObserver` que
      ya usa `staff-agenda.html` para el mismo problema (`mdjb-shared-header.js`
      re-muestra el header tras resolver la sesión de login — un escondido de
      una sola vez no bastaba). Auditado también `staff-admin.html`
      (ya tenía el candado completo, nunca vulnerable) y `contracts-engine.html`
      (ni siquiera pinta su propio header — no puede tener este bug): **no
      queda ningún archivo conocido con esta vulnerabilidad**. Segundo hallazgo
      en el mismo ticket: el parámetro `?bare=1` que "Inbox · Tickets" ya le
      mandaba al iframe nunca se leía en ningún lado — mostraba TODO Config
      (Cuenta/Categoría/Recompensas/...) en vez de solo los tickets; ahora sí
      esconde el sidebar interno cuando `bare=1` está presente. Tercer hallazgo
      cosmético: la etiqueta interna decía "Notificaciones" para un contenido
      que siempre fue de tickets (confirmado por la lógica del badge, que
      cuenta `platform_inbox_messages`, no notificaciones genéricas) — renombrada
      a "Inbox · Tickets" para que coincida con el ítem real del riel de
      OPERACIONES, que ya decía bien.
      — 2026-08-23 (Hilo Maestro): versión limpia de lo que traía el PR #240
      (cerrado, ver bitácora). Fase 1: tabla `libro_operaciones` (mismo
      candado RLS+cero-políticas que `platform_incidents`), única entrada
      `libro_operaciones_reportar()`, única salida la vista
      `libro_operaciones_staff`. Fase 4: columna `event_builder_order_id`
      (nullable), `libro_operaciones_reportar()` reemplazada para validar
      que la orden enlazada pertenezca a quien reporta, vista
      `libro_operaciones_reconciliacion_staff` (contrasta autorreporte vs.
      total oficial, sin corregir ni borrar nada). RPC
      `get_my_cashflow_ledger(p_since)`: única fuente de monto es
      `dj_ledger` (ya liberado al artista); `event_builder_orders`/`leads`
      solo aportan `event_label`; nunca expone `total_usd`/
      `amount_paid_usd` (margen de la empresa). Dependencia real
      (`mdj_profile_de_usuario`, de M2) resuelta al fusionar el PR #241
      justo antes. Cabecera de entorno: **PRUEBA** en las 3 migraciones.

- [ ] **VISIÓN — ELIXIS/DJMago: catálogo del DJ desde su propio software (Serato/VirtualDJ/Rekordbox), sin motor de fingerprinting pago**
      — 2026-08-28 (Hilo Maestro, sin ticket de construcción abierto todavía —
      solo visión registrada a pedido del PO). Origen: el PO preguntó por
      Spotify Web API / ACRCloud (motor tipo Shazam) para que ELIXIS pudiera
      catalogar su librería personal de ~4TB y armar sets. Investigación real
      (no del entrenamiento, con fuentes) descartó ambos caminos como
      **innecesarios** para ese objetivo específico:
      - **Spotify Web API / ACRCloud NO existen en el repo** (verificado por
        grep completo — solo iconos decorativos de "Spotify"). El único motor
        de música real desplegado es `supabase/functions/mdj-music/index.ts`,
        puente JWT a Apple Music (catálogo, no fingerprinting).
      - **ACRCloud es pago-por-uso, no un plan fijo** (confirmado en su propia
        consola): $3/1000 peticiones (primeras 20k), bajando por volumen;
        +80% de recargo si el proyecto usa su "Music bucket" comercial; y el
        dato clave para 4TB — **cobra mensualmente por HORAS de audio
        cargadas a su bucket**, no por espacio. Con miles de horas reales,
        esto sale caro y RECURRENTE — choca con la meta explícita del PO de
        cuidar el margen de MDJB.
      - **Hallazgo que resuelve el problema real, gratis**: Serato ya genera
        su propia base de datos binaria compacta al importar/analizar cada
        canción — `database V2` (catálogo completo: rutas, BPM, tonalidad,
        tags) y `history.database` (qué se tocó, cuándo, por sesión). Es solo
        metadata (nunca el audio), por eso pesa poco pese a un catálogo de
        4TB. El formato ya está documentado/reverse-engineered, con librería
        de referencia open source ([`serato-connect`](https://github.com/chrisle/serato-connect),
        [formato Serato en el wiki de Mixxx](https://github.com/mixxxdj/mixxx/wiki/Serato-Database-Format)).
        **Conclusión: si la librería del DJ ya está organizada en Serato, no
        hace falta pagarle a nadie para catalogarla — ese trabajo ya está
        hecho.**
      - **VirtualDJ** guarda su historial en texto plano legible
        (`history.txt`/`tracklisting.txt` + `.m3u` por fecha) — el más fácil
        de leer de los tres, cero necesidad de librería especial
        ([manual oficial](https://virtualdj.com/manuals/virtualdj/interface/database/history.html)).
      - **Rekordbox** es el más difícil: exportación XML retirada desde la
        versión 6 (la importación sigue viva), base de datos moderna cifrada
        (SQLCipher). Hay proyectos de terceros que ya lo resolvieron
        ([`rekordbox-mcp`](https://github.com/davehenke/rekordbox-mcp)), pero
        requiere más ingeniería que los otros dos — arrancar por XML
        exportado a mano, no por lectura directa de su base de datos.
      **Arquitectura de la visión (sin construir todavía):** el DJ hace UNA
      acción de una sola vez (dar permiso a una carpeta, o instalar un
      ayudante local ligero) — después de eso, invisible y automático, el DJ
      nunca vuelve a tocar nada técnico. Es honesto decir que "cero acción,
      nunca" no es técnicamente posible (los navegadores no pueden leer
      archivos locales sin al menos un permiso explícito) — se documenta así
      para no prometer algo que no se puede cumplir. Un parser server-side
      lee el archivo (`database V2`/history/`.m3u`) y guarda la metadata
      estructurada en Supabase, ligada al perfil del DJ, como fuente de
      conocimiento real para ELIXIS/DJMago — que la usa en conversación para
      armar playlists nuevas y detectar patrones de mezcla. **Entregable
      pedido por el PO:** ELIXIS arma la playlist y se la entrega en el chat
      como PDF o PNG descargable — sin que el DJ tenga que saber ni ver nada
      del proceso técnico detrás.
      **Nota de alcance:** el mecanismo de lectura local (agente/ayudante en
      la máquina del DJ) es territorio de MDJPRO (app de escritorio, hoy
      pausada — un proyecto a la vez, ver regla vigente), no de la plataforma
      web actual. Esta entrada documenta la visión para cuando le toque su
      turno; no autoriza ni empieza su construcción.
      **Segunda pieza, separada y de menor prioridad — reconocimiento "Shazam"
      bajo demanda:** el PO planteó un caso de uso distinto y válido:
      identificar qué canción está tocando OTRO DJ en vivo (no monitoreo
      continuo). Con volumen bajo y ocasional (el DJ aprieta un botón, se
      capturan unos segundos), tanto ACRCloud como AudD salen a centavos al
      mes — ahí sí tendría sentido un motor de fingerprinting pago, pero como
      función aparte y opcional, nunca como reemplazo del catálogo propio de
      Serato. Sin ticket abierto, solo registrado.

## 2. Bitácora de Sincronización entre Cajas
- [2026-08-22] Inicialización del Hub Central de sincronización multi-hilo.
- [2026-08-22] Matriz de Jurisdicciones (`docs/JURISDICCIONES.md`) registrada + regla 6 en `CLAUDE.md`. Rename de marca `mdj-shared-header.js` → `mdjb-shared-header.js` fusionado en 61 archivos activos (PR #213).
- [2026-08-22] Hilo Elixis Voice Agent Blueprint reportó memoria persistente instalada (ver arriba) y detectó `dj_memory_facts` documentada como tabla en `JURISDICCIONES.md` cuando es una vista — corregido por el Hilo Maestro en el mismo commit.
- [2026-08-22] Camino de escritura de la memoria confirmado con datos reales (escribir/recordar/olvidar) — hito de memoria persistente ELIXIS cerrado.
- [2026-08-22] Hilo Business Financial Intelligence entregó diagnóstico de Stripe Connect (ver arriba) — sin infraestructura, con conflicto de dos ledgers sin reconciliar. Esperando decisión del PO antes de abrir ticket de construcción.
- [2026-08-22] SSOT de balance/payouts resuelto: `financial_payables` gana, `dj_ledger` a deprecar progresivamente. Stripe Connect queda diferido a sprint dedicado; el hilo BFI queda en espera de esa fase o de la siguiente tarea en su dominio.
- [2026-08-22] Hilo Road Master Map / Calendario BI auditó el encargo de agenda: las tablas nombradas (`events`, `agenda_locks`, `dj_assignments`) no existen; las reales son `artist_agenda` (16-ago, R9a/R9b), `leads` y `dj_events` (legacy). Decisión de SSOT de agenda pendiente del PO (ver arriba).
- [2026-08-22] PO fijó la política master/personal de agenda (ver arriba) y registró el calendario de cliente como tarea futura. El Hilo Maestro encontró en vivo un bug de layout del módulo de clima dentro de `staff.html?vista=agenda` (ver arriba) — dominio #5, sin hilo activo, sin arreglar.
- [2026-08-22] CORRECCIÓN: el hallazgo del write-path paralelo en `dj-dashboard.html` es del Hilo Maestro, no del hilo Road Master Map (atribución errónea en la entrada anterior). Ese hilo ya rebasó con autorización del PO y su reconciliación completa (`V11`, `V12`, `R14`…`R21`, `cap-estacion-nav`, `cap-avisos-push`) está dentro de `main` — la rama NO sigue aparcada. Queda un commit local sin subir (`c4e1398`, arregla 3 referencias muertas al rename del PR #213 en `docs/roadmap/master-map.json`).
- [2026-08-22] `dj_events` refinado a tabla sin ningún consumidor de código (ver arriba).
- [2026-08-22] Cuarta pieza encontrada: `event_builder_orders` (8 consumidores reales, cero en las pantallas de agenda, enlazada a `leads` por FK nullable). La pregunta de SSOT de agenda se reencuadra en dos ejes — reserva (`leads` vs `event_builder_orders`, sin resolver) y proyección personal (`artist_agenda`, ya resuelta como derivada). `dj_events` queda como baja simple.
- [2026-08-22] `artist_agenda`/R9a/R9b verificado en producción real, instalación completa (ver arriba) — al revés de `avisos_pendientes` (tabla sin función) de anoche. R9 del mapa queda deliberadamente sin cerrar hasta que el PO responda si R9b es definitivo o provisional.
- [2026-08-22] PO respondió: R9b no es definitivo por sí solo — falta integrarlo al calendario rediseñado (ver arriba). R9 del mapa queda abierta, con el próximo paso técnico ya definido.
- [2026-08-22] PO resolvió el SSOT de reserva: `event_builder_orders` gana sobre `leads` (ver arriba). `dj_events` marcada para DROP en la próxima migración de limpieza. Confirmado directamente por el PO, no relayado de un tercero, tras detectarse una contradicción en un mensaje de "resolución" que decía lo opuesto sobre R9b.
- [2026-08-22] **Cierre de sprint de gobernanza y saneamiento git.** `main` consolidado con: Regla 7 en `CLAUDE.md` (auditoría visual obligatoria antes de commit/merge/producción — origen: incidente de commits sin autorización previa del PO, dos veces en la misma sesión); UI-0822 (agenda de staff: paños negros, efecto imán, tarjetas — PR #224); decisiones de R9b y SSOT de reserva (PR #225); `web/mdjb-music-intelligence.html` trackeado por primera vez, corrigiendo un 404 real en producción en el routing de Academia (PR #226); referencias del Road Master Map sincronizadas al rename de marca del PR #213 (PR #227).
- [2026-08-22] **Worktree independiente creado para el hilo #5 (Weather Design Bible / UI):** `~/Desktop/mdjb-weather-ui`, rama `fix/weather-ui-canvas-refit` (incluye el fix de canvas WebGL en iframe anidado + el cache-busting `?v=` que le faltaba al único módulo del sitio sin ese patrón). Previene la colisión de commits cruzados entre hilos que causó el incidente de PR #223 hoy. Ese fix de clima todavía no tiene PR propio a `main` — pendiente de abrir cuando el hilo #5 esté listo.
- [2026-08-22] Centro Legal: auditoría forense cerrada (ver arriba). Corrige la auditoría de julio (eran 9 tablas, no 8). Riesgo de `auth.uid()` sobreescrito confirmado en producción por el PO mismo, con severidad corregida (el escenario catastrófico queda descartado — `auth.uid()` es propiedad de `supabase_auth_admin`, la migración fallaría por permisos, no por tumbar la autenticación), y neutralizado moviendo las 3 migraciones fuera de la ruta ejecutable (PR #230) — sin aplicar ni borrar el diseño.
- [2026-08-22] `docs/LIBRO_OPERACIONES_IA.md` creado y fusionado (PR #231) — norma operativa de los 5 mandamientos de seguridad de ramas/código y los protocolos de contención de agentes, grounded en los incidentes reales de esta sesión.
- [2026-08-22] Fix de clima del hilo #5 fusionado (PR #232, `fix/weather-ui-canvas-refit-v2`): canvas WebGL a ancho completo en el iframe anidado + cache-busting. Ticket cerrado del todo.
- [2026-08-22] `dj_events`: guion de baja archivado y fusionado (ver arriba, PR #233) — disponible para ejecución manual controlada, ninguna migración automática lo dispara.
- [2026-08-23] `platform_incidents` fusionado (PR #235, ver arriba) — núcleo de registro inmutable de incidentes técnicos/UI, contrato de datos aprobado por el PO antes de escribir código. Sprint de la cabecera (Single Row Header) rechazado por el Hilo Maestro y remitido al dominio #5 — protocolo "Reporto, no ejecuto" del propio `docs/LIBRO_OPERACIONES_IA.md` en acción, el mismo día que se escribió.
- [2026-08-23] Dominio #5 ejecutó el sprint de cabecera remitido arriba: `.header-top` desbordaba en banda portátil 1001-1439px (buscador cortado, wordmark inalcanzable fuera del viewport) — cerrado con scroll contenido + aire de margen del logo, sin tocar slots fijos ni el carrusel de `#mainNav`. Mismo PR sumó el fix de la card "Create Your Account" del Hero de `index.html` (recortaba el botón REGISTER en altura reducida — 13"/split-view). Verificado en vivo antes de commitear (Regla 7), aprobado por el PO, fusionado en `main` (PR #237).
- [2026-08-23] Modal de reporte de incidentes fusionado (PR #238, dominio #5) — consume `platform_incidents_reportar()` (núcleo de arriba). Botón + modal montados en `initStaff()` de `staff.html`, visibles para cualquier rol de staff, ocultos para no-staff por el candado ya existente de la página. `dominio`/`severidad` quedaron como texto libre (con sugerencias, no `<select>` fijo) porque el propio SQL deja ese catálogo pendiente del Capitán. Sin credenciales reales de staff en la sesión de build, no se pudo verificar el flujo autenticado completo (abrir → enviar → fila nueva) — quedó marcado explícitamente en el PR; el PO lo fusionó tras su propia revisión.
- [2026-08-23] **PR #240 cerrado sin fusionar.** El PO reportó inicialmente que ya estaba integrado a `main`; verificado de forma independiente (`gh pr view`) que seguía `OPEN`. Auditoría posterior encontró que la rama de origen estaba contaminada — 14 archivos, incluyendo `CLAUDE.md` y este mismo `ESTADO_MAESTRO.md` en versión vieja, que se habrían arrastrado a `main` de fusionarse tal cual. Se aisló lo real (2 migraciones nuevas de Libro de Operaciones) sobre `origin/main` limpio; en el camino se descubrió que dependían de `mdj_profile_de_usuario()`, una función que solo existía en 5 archivos M1-M5 sin trackear localmente — bloqueando el aislamiento hasta resolver esa cadena (ver M1-M5 arriba).
- [2026-08-23] M1–M5 (Constitución de Autoridad e Identidad) auditados, aislados en rama propia y fusionados (PR #241, ver arriba) — desbloquea la dependencia que detenía el aislamiento limpio del Libro de Operaciones.
- [2026-08-23] Motor de Contratos y W-9 completado (ver arriba): el W-9 se rehizo como visor del PDF real (nunca una réplica en HTML) con dropzone de subida firmada, "Legal" se movió del top-nav (rompía el header) al sidebar de Staff, y se corrigieron dos bugs reales de producción — la firma de parámetros de `guardar_contrato_firmado` y el `CHECK` de `contract_type` que bloqueaba el guardado de los 5 templates. PR abierto en `feature/contracts-w9-engine-integration`, pendiente de aprobación del PO.
- [2026-08-23] Libro de Operaciones Fase 1 + Fase 4 + RPC `get_my_cashflow_ledger` re-aislados sobre `origin/main` (ya con M1-M5 integrado) y fusionados como PR #242 (ver arriba) — versión limpia que reemplaza al PR #240, cerrado con nota explicando el reemplazo.
- [2026-08-24] **Bóveda Legal / W-9 + doble custodia — commit nuevo en `feature/contracts-w9-engine-integration`, PR aún sin abrir.** Sobre lo ya fusionado en PR #244 (23-ago), sesión larga de pulido + reingeniería real del W-9 + arquitectura de "doble custodia" (contratos visibles también en el panel personal del cliente/artista):
  - **Visor del W-9**: pasó por 3 arquitecturas el mismo día — réplica HTML (rechazada, ley "nunca recrear el W-9") → visor custom `pdf.js` (canvas + miniaturas + overlay de los 23 campos reales del AcroForm, funcionando, abandonado por decisión propia del PO, quería el motor nativo del navegador) → **arquitectura final: `<object>`/`<iframe>` nativo sobre `web/assets/docs/fw9.pdf`** (copia real, verificada byte a byte — SHA-256 `2d420cbb...` — contra el original del PO). `pdf.js`/`pdf-lib` y ~190KB de base64 muerto, removidos. Panel izquierdo purgado de 8 campos que nunca se guardaban (vestigios de arquitecturas descartadas) — estructura final: Descargar → Dropzone (SHA-256) → "Nombre Legal / Razón Social" → Guardar.
  - **Flujo de envío/recepción cableado end-to-end para W-9**: Staff genera enlace (nombre + contacto, sin canvas de firma — no aplica al W-9); el Firmante ve un panel gemelo del de Staff (descargar → subir → nombre → enviar); al completarse pasa a un estado local nuevo, `COMPLETADO_EXTERNO` (solo UI, no toca la DB — el `status` real de `signed_contracts` lo pone la función siempre en `SIGNED`). Bug corregido en el camino: el sello de auditoría solo reconocía `status==='SIGNED'` literal.
  - **Doble custodia (contratos visibles desde la cuenta del propio cliente/artista)**: forense completo de `signed_contracts` (16 columnas, vía `pg_get_functiondef` de `guardar_contrato_firmado`) — decisión final **sin `ALTER TABLE`**: se cruza por `artist_profile_id`→`dj_profiles.user_id` (artistas) o `signer_email`→`auth.users.email` (todos los demás, insensible a mayúsculas), ambas columnas YA existían. Dos funciones RPC nuevas y aditivas, creadas en producción: `obtener_mis_contratos()` (lista liviana) y `obtener_mi_contrato_detalle(p_id)` (con el archivo) — mismo patrón de seguridad que `get_my_cashflow_ledger`: `SECURITY DEFINER`, nunca reciben un usuario como parámetro, siempre `auth.uid()`/`auth.email()` de quien llama. Sección nueva "Documentos Legales" en `web/account-settings.html` (mismo patrón `acct-side-link`/`showPanel` que las otras 11 secciones) consume `obtener_mis_contratos()`; el botón "Descargar Copia Oficial" abre `contracts-engine.html?view_record=<id>`, que reutiliza `openRecordView()` sin duplicar nada del visor. **Probado end-to-end con un registro real** (W-9 firmado y asociado a `miamidjbeat@gmail.com`, `id` real en producción `7439d9a3-0d01-43ef-a78e-31932455d8b3`) — apareció en Bóveda Legal (Staff) y en Documentos Legales (cuenta del cliente); se encontró y corrigió un bug real en el camino (`contract_type` llega de la DB en mayúsculas — `"W9"` — pero `TEMPLATES` usa minúsculas — `"w9"` — sin el mapeo inverso `openRecordView()` truena).
  - **Pendiente para la próxima sesión (ver entrada siguiente para el cierre)**: integración de badges de compliance (Contrato/W-9) en el Directorio Global CRM de `staff.html` (`#sc2-crm`) — ticket ya registrado por el PO, cruzando por los mismos campos (`artist_profile_id`/`signer_email`), sin tocar la DB. También sigue sin resolverse la píldora nativa "Bóveda Legal" que a veces se ve flotando sobre el visor (se descartaron todos los `title=` como causa; sin una captura con la posición exacta del cursor no hay más pistas que seguir — **sigue sin resolverse**).
- [2026-08-24] **Segunda mitad de la misma sesión — APROBADA por el PO, lista para commit.** Continúa sobre la entrada anterior (mismo día, misma rama `feature/contracts-w9-engine-integration`, aún sin PR abierto):
  - **Badges de compliance en las 10 tablas del Directorio Global CRM** (`web/staff-admin.html`, `loadCRM()`/`renderCRM()`): cierra el pendiente de la entrada anterior. Cada contacto muestra 2 pastillas (Contrato + W-9) — verde con enlace real si ya existe el registro (`?view_record=<id>`), gris/naranja y clicable si falta (`?open_send=<template>&name=&contact=`, prellenado). Verificado en vivo dentro del contenedor real.
  - **Búsqueda real de personas** reemplaza el selector de perfiles simulados en `contracts-engine.html`: busca en `dj_profiles`+`client_profiles` en vivo (nombre/email/uuid exacto), Enter autorrellena el primer resultado. `MOCK_PROFILES` se queda solo para el botón demo "Simular Elixis", ya no para el flujo real.
  - **Patrón "?" en círculo** generalizado a TODAS las notas fijas del archivo (búsqueda, envíos, Elixis, bandeja, firma, W-9 del firmante, descripción de plantilla) — un solo manejador delegado, cero espacio de trabajo perdido. Deliberadamente sin convertir: notas dentro de modales y el paso estructural "Paso 2" del W-9.
  - **Catálogo de contratos especializados** (antes: todo lo que no era dj/venue/w9 cargaba en "staff", genérico): 4 plantillas nuevas — `live_band` (stage plot/input list/soundcheck), `solo_artist` (pistas/playback, microfonía), `subcontractor_show` (B2B, seguro COI obligatorio, cláusula Hold Harmless — Hora Loca/zanqueros/MCs), `private_event` (bodas/15s, depósito, horas extra, cláusula climática). `venue` ganó 3 campos aditivos (load-in/out, límite de dB, tomas eléctricas) sin tocar su contenido de residencia existente. `corporate` y `staff` se conservaron intactos (no en el catálogo nuevo, pero sin borrar — contratos legacy siguen viéndose). Mapeo del CRM actualizado por categoría real (incluye nueva categoría "Solista", separada de "Músicos en Vivo").
  - **Cláusulas nuevas en `dj`**: exclusividad y no-negociación, no-circunvención y protección de plazas, conducta profesional/jurisdicción, requerimientos técnicos adicionales — texto exacto pedido por el PO.
  - **Selector de régimen de exclusividad** (Exclusivo / No-Exclusivo-Libre) en `dj`/`live_band`/`solo_artist`, con cláusula que cambia según la selección (default No-Exclusivo — nunca restringe por omisión). Se guarda solo dentro del `form_payload` existente, sin cambio de esquema.
  - **CHECK constraint de producción ampliado**: `signed_contracts.contract_type` solo aceptaba 5 valores — el PO corrió `supabase/scripts/ampliar_tipos_contrato_signed_contracts.sql` (aditivo, sin tocar registros existentes) para sumar los 4 nuevos (`LIVE_BAND_AGREEMENT`, `SOLO_ARTIST_AGREEMENT`, `SUBCONTRACTOR_SHOW_AGREEMENT`, `PRIVATE_EVENT_AGREEMENT`). Confirmado "Success. No rows returned" antes de dar el guardado real por funcional.
  - **Autocompletado real de venue** contra `public.financial_venues` (tabla real de producción, 3 filas confirmadas: El Valle Restaurante, Sundowner Key Largo, Mojitos Calle 8) — nombre+dirección+contacto se autorrellenan si existen; sin coincidencia, el campo sigue 100% manual. **Dato pendiente, no bug**: las 3 filas tienen `address`/`contact_*` en NULL — falta cargarlos para que ese autocompletado se vea completo. "Requerimientos técnicos por venue" pedido por el PO **no se construyó**: no existe columna para eso en `financial_venues`, y no se fabricó dato falso.
  - **Puente Elixir AI con confirmación humana (Voice-HITL)**, `applyElixirContractPayload()` + `postMessage({type:"mdjb:elixir-contract"})`: se detectó un despachador de Elixis previo (`ELIXIS_TEMPLATE_MAP`/`handleElixisDispatch`, sesión anterior) que auto-despachaba sin clic humano — choca con la ley ya establecida (`feedback_elixis_action_tools_hitl_pattern`, ver `enviar_sms`). El PO resolvió con un diseño intermedio, no binario: Elixir prepara y arma un resumen hablable ("He preparado el contrato de X para Y en Z por $M. ¿Confirmas el envío a W?"); el despacho real solo ocurre si el payload trae `user_confirmed:true` — eso solo debe llegar después de que una persona real confirmó por voz o botón (decisión del flujo conversacional de Elixir, no de este archivo). Candado de seguridad verificado en vivo: campos faltantes bloquean el despacho **aunque venga `user_confirmed:true`**. El despachador legado no se tocó — sistema nuevo y separado.
  - **Trazabilidad real del emisor**: `_meta.issuedBy`/`_meta.authorizedBy` dentro de `form_payload` (JSONB existente, sin `ALTER TABLE`). `resolveCurrentStaffIdentity()` replica el mismo patrón de `staff.html` (`auth.getSession()` → `dj_profiles` por `user_id` → rol+nombre) dentro del propio iframe de `contracts-engine.html`; verificado en vivo resolviendo el nombre real de la sesión (`"Gerardo A Valle (Owner)"`) sin que Elixir tuviera que pasarlo. Envío manual cae al mismo mecanismo; si no hay sesión resuelta, cae a "Staff Manual" — nunca bloquea.
  - **Bug de capas encontrado y corregido en vivo** (reportado por el PO con captura): el dropdown de búsqueda de personas y el de venue quedaban pintados DETRÁS del panel del formulario de abajo — ambos `.panel` usan `backdrop-filter` (contexto de apilamiento propio) y ninguno tenía `z-index` propio para ganarle al panel siguiente. Corregido elevando `#contextSimPanel`/`#fieldsPanel` (`position:relative;z-index`), verificado con `elementFromPoint()` en el punto exacto de solapamiento.
  - **Todo aprobado por el PO tras revisión visual real** (screenshots del recorrido + de la consola SQL de producción) — este commit es el primero de esta sub-sesión, con autorización explícita ("APROBADO: COMMIT Y FINALIZACIÓN DE EMISOR DE SESIÓN").
  - **Siguiente paso ya anunciado por el PO**: vincular acceso/descarga del contrato de responsabilidades dentro de la Cabina de DJ / Perfil de Artista — no arrancado todavía.
- [2026-08-24] **Bóveda Legal en la Cabina DJ (`web/dj-profile.html`) — commit nuevo, aprobado y aplicado sobre la misma rama.** Continúa el paso anunciado arriba:
  - **Hallazgo real: ya existía una pestaña "Mis Documentos" (`#tab-documentos`) desde el commit original del motor de contratos (73f6c6c, 23-ago) — y estaba mal.** Incrustaba `contracts-engine.html` completo en un iframe: el motor de STAFF (buscador de personas, generador de envíos, CRM), no una vista personal del artista. Reemplazado por una lista real (misma RPC `obtener_mis_contratos()`, mismo patrón que `account-settings.html#legal`, sin duplicar lógica): tipo, fecha, badge verde FIRMADO + botón Descargar; sin registros, mensaje de "Pendiente" honesto (sin botón de "completar" falso — el artista no puede autogenerarse un enlace de firma desde su propia vista). Slot renombrado "Mis Documentos" → "Bóveda Legal" en `MDJ_NAV_SLOTS_ARTISTA` (`mdjb-shared-header.js`) — solo texto, cero riesgo de layout.
  - **Widget de compliance compacto** en la tarjeta "Economía · SoundForTips" del sidebar (zona ya owner-only, `isOwner && !isPublicQrView`): 2 badges (Contrato/W-9) + enlace a `account-settings.html#legal` (se agregó `legal` al whitelist de hashes deep-link, antes no abría nada).
  - **Carrusel continuo de especialidades**: `#pub-role-label` ahora pinta el texto DOS veces en una pista (`.dj-hero-role-track`) animada con `@keyframes` (16s, loop infinito, pausa al hover/tap), máscara de desvanecido en los bordes; respeta `prefers-reduced-motion` (cae al scroll manual). Verificado en vivo con datos reales inyectados — el perfil de prueba (djmago305) no tiene `artist_specialty`/`roles`/`city` cargados, así que en su estado real no hay nada que animar (no es un bug, es un hueco de datos).
  - **Avatar más pequeño en portrait móvil**: `.dj-hero-inset` 112px→84px solo bajo `@media (orientation: portrait)` anidado dentro del breakpoint ya existente de ≤900px — landscape y escritorio confirmados sin cambio (112px).
  - **Ítem cancelado a propósito, invocando la propia condición de seguridad del PO** ("si algo rompe cualquier configuración... se cancela este cambio"): NO se agregó "Fénix AI" como slot nuevo del artista ni un segundo slot separado de "Bóveda Legal". `MDJ_NAV_SLOTS_ARTISTA` alimenta AL MISMO TIEMPO el `#mainNav` de escritorio Y (indirectamente) el panel móvil — el historial de este nav documenta desbordes reales de la barra por exceso de ítems (banda 1001-1439px, PR #237), y no hay forma de verificarlo sin una sesión real de DJ. Primer intento (parchar el HTML estático del menú hamburguesa) resultó ser trabajo muerto: se descubrió que `mdjb-shared-header.js` reconstruye el panel móvil en tiempo real leyendo `#mainNav` — revertido limpio, cero rastro.
  - **Aprobado por el PO** ("Los ajustes son totalmente acertados, especialmente el reemplazo del iframe de Staff") con mensaje de commit explícito.
- [2026-08-24] **CIERRE DE SESIÓN — checklist de entrega para el despliegue a producción.** Rama `feature/contracts-w9-engine-integration`, remoto `origin` sincronizado (`git rev-list` 0/0, push confirmado). **PR todavía sin abrir** — sigue pendiente por Regla 1 (cero PRs automáticos) hasta que el PO lo pida explícitamente.
  - **Comiteado y empujado (7 commits desde el merge de PR #244):**
    1. `4401acf` — W-9 con visor nativo real + doble custodia de contratos.
    2. `81664aa` — Catálogo de contratos especializados (live_band/solo_artist/subcontractor_show/private_event), selector de exclusividad, autocompletado de venue (`financial_venues`), puente Elixir Voice-HITL, badges de compliance en las 10 tablas del CRM.
    3. `b605c14` — Reemplazo del iframe de Staff en la Bóveda Legal del perfil de DJ por una vista propia del artista, widget de compliance, carrusel de especialidades, escalado de avatar en portrait, rename "Mis Documentos"→"Bóveda Legal".
  - **Migraciones de producción ya corridas por el PO (confirmadas "Success"):** ampliación del `CHECK` de `signed_contracts.contract_type` (9 valores) y las 2 RPCs de doble custodia (`obtener_mis_contratos`/`obtener_mi_contrato_detalle`) — ver commits 1-2 arriba.
  - **Pendiente, NO bloqueante para desplegar, pero anotado:**
    - Datos: `financial_venues` tiene 3 filas reales sin `address`/`contact_*` — cargarlos para que el autocompletado de venue se vea completo.
    - Verificación con sesión real de DJ: el widget de compliance, "Bóveda Legal" en la Cabina DJ, y el menú móvil (ítem cancelado por seguridad, ver arriba) — nadie los ha visto todavía con una cuenta de artista real logueada; toda la verificación de este bloque se hizo con datos reales pero sesión de invitado o de owner, nunca de DJ.
    - "Fénix AI" para artistas: decisión de producto pendiente del PO (ver arriba, item cancelado).
  - **Árbol de trabajo:** limpio para todo lo de esta sesión (contratos/W-9/perfil de DJ). Quedan sin trackear 6 elementos ajenos (`.claude/`, `MiamiDJBeat-MigracionV2/`, `docs/constitucion-plan-produccion-m1-m5.md`, 5 scripts `constitucion_*.sql`) — con fecha 16-23 de agosto, de OTRO hilo (dominio Constitución/migraciones), presentes desde antes de que esta sesión empezara. No se tocaron: protocolo de colisión entre sesiones paralelas prohíbe tocar trabajo ajeno sin confirmación explícita del PO.
- [2026-08-27/28] **Hilo Maestro — Shopping Miami DJ Beat + Config/Inbox·Tickets, ambos fusionados a `main` (PR #287, #286, ver módulos arriba).** Todo el trabajo se hizo en dos worktrees aislados dentro de `.worktrees/` (`shopping-native-store`, `fix-config-inbox-label`), nunca en el directorio de trabajo compartido — ese directorio seguía (y sigue) en `fix/mobile-ui-cleanup`, con el hilo de avatares trabajando en vivo sobre `mdj-commander.html`/ELIXIS. Ambos PRs, antes de fusionar, se rebasaron sobre `origin/main` actualizado (regla del §8 de `CLAUDE.md`) — el de Shopping había nacido antes de que el de Config se fusionara, y sin el rebase habría revertido ese arreglo por accidente al fusionar.
  - **Hallazgo crítico de seguridad financiera, ya resuelto:** el mismo `STRIPE_SECRET_KEY` alimentaba TODOS los cobros de la plataforma (suscripciones, depósitos de eventos, propinas, y ahora merch) — confirmado en modo **LIVE** (`cs_live_...`) antes de tocar nada. Se creó `STRIPE_SECRET_KEY_MERCH` (llave de prueba, `sk_test_...`, generada por el propio PO desde el modo de prueba de su cuenta real "MIAMI DJ BEAT" en Stripe — nunca una cuenta nueva) exclusivamente para `create-merch-checkout`, dejando la llave compartida intacta. Regla para cualquier hilo futuro que agregue un cobro nuevo: **nunca reusar `STRIPE_SECRET_KEY` para algo que necesite probarse** — darle su propia llave con sufijo del dominio, como aquí.
  - **Incidente de confusión de entorno, resuelto por diagnóstico, no por código:** el PO reportó varias veces "el header duplicado sigue ahí" mostrando capturas de `localhost` (sin puerto) — resultó ser una pestaña de un ambiente completamente distinto (`http://localhost:8124/mdj-commander.html`, sirviendo el directorio de trabajo COMPARTIDO en `fix/mobile-ui-cleanup`, no cualquiera de los dos worktrees donde vivía el arreglo real). Verificado con `lsof` que no hay nada en el puerto 80, y con `git diff --stat` que ese directorio compartido tiene `web/staff.html` (203 líneas) y `web/mdj-commander.html` (1165 líneas) sin comitear, del hilo de avatares. **No se tocó nada de ese directorio** — se relevó al hilo de avatares (`miami-dj-beat-platform-6d`) el reporte de inestabilidad de menú del PO más el contexto de que su copia local de `staff.html` va detrás de los dos PRs recién fusionados, para que no lo confunda con una regresión real.
  - **Pendiente de decisión/verificación del hilo de avatares** (relevado, no resuelto por el Hilo Maestro): estado actual del trabajo de ELIXIS/avatares en `mdj-commander.html`, y si la inestabilidad de menú que reporta el PO en `localhost:8124` es del cambio en curso de ese hilo o solo un artefacto de estar detrás de `main`.
- [2026-08-28] **Visión registrada — ELIXIS/DJMago catalogando la librería del DJ vía Serato/VirtualDJ/Rekordbox, sin motor de fingerprinting pago (ver módulo arriba).** El PO preguntó por Spotify Web API/ACRCloud para catalogar su librería personal de 4TB; investigación real (no de entrenamiento, con fuentes citadas) confirmó que ninguno de los dos existe en el repo hoy, que ACRCloud cobra mensualmente por horas de audio cargadas (caro y recurrente a esa escala), y que Serato ya resuelve el problema gratis con su propia base de datos de metadata (`database V2`/`history.database`). Sin ticket de construcción abierto — el mecanismo de lectura local es territorio de MDJPRO (pausado). Segunda pieza registrada aparte: reconocimiento tipo Shazam bajo demanda (no continuo) para identificar sets de otros DJs, de bajo costo por su volumen ocasional — sin ticket tampoco.
- [2026-08-29] **CIERRE DE SESIÓN — ELIXIS Core & UI, workspace nativo de `staff.html` (panel FÉNIX AI).** Rama `fix/mobile-ui-cleanup` (worktree compartido con el hilo de avatar/voz), **todo sin comitear** — el PO pidió explícitamente no comitear todavía ("todavía no"). Nada de esto se ha empujado a `origin` ni tocado en producción; todo verificado en local (`localhost:8124`).
  - **Acordeón de escritorio (3 paneles: Modo Enfoque / Avatar / Hilos)**: reescrito de CSS Grid a posicionamiento absoluto independiente — el centro (avatar) queda fijo al 50% del ancho SIEMPRE, los laterales son overlays propios que topan justo en su borde sin invadirlo ni desplazarlo (directiva explícita del PO: "el avatar siempre es protagonista"). Encontrado y corregido un bug real de especificidad CSS en el camino (`.ew-col{position:relative}` empataba con `.ew-pantalla{position:absolute}` y ganaba por orden de archivo — resuelto subiendo especificidad, no reordenando).
  - **Simulador energético HUD** (`web/js/elixis-hud-transmision.js`, nuevo): sobre/carga/haz/receptor en SVG+CSS keyframes + canvas para la rejilla de fondo, conectado a `onTranscript`/`onTool` reales. Honesto: hoy `onTool` es el mejor proxy disponible de "orden lanzada" porque `elixis-realtime-session` todavía no tiene una tool real de envío.
  - **Barra de escritura flotante + `enviarTexto()`** en `elixis-voice-session.js` (mezcla texto/voz real de la Realtime API). **Bug de seguridad real encontrado y corregido**: escribir texto sin sesión activa disparaba `getUserMedia()` por primera vez en la pestaña — causó un corte real de audio en Serato con el PO tocando en vivo. Revertido: ya no arranca la sesión sola, exige voz activa primero y avisa visiblemente en la propia barra si no la hay.
  - **Bug de caché real encontrado**: `elixis-voice-session.js` se servía con un `?v=` desactualizado desde antes de esta sesión — el navegador corría JS viejo sin `enviarTexto()` durante varias pruebas. Corregido.
  - **Avatar de DJMago con chroma key real conectado** (mismo mecanismo de `mdj-commander.html`: `avatar-heygen-chromakey.js` + canvas, video oculto por `visibility` no `display`). Video correcto identificado y usado: `djmago-idle-boomerang.mp4`. Carpeta renombrada `web/assets/Elixis IA/` → `web/assets/DJMago IA/` (el contenido real es DJMago, no ELIXIS — nombrado corregido en 3 archivos: `staff.html`, `mdj-commander.html`, `.gitignore`). 2 videos de prueba descartados eliminados del disco (confirmados sin referencia en código activo antes de borrar).
  - **Sistema de visemas de boca eliminado por completo** (`avatar-viseme-sprites.js`, `MDB_AVATAR_VISEMES`): los `elixis-boca-*.webp` resultaron ser fotos completas de DJMago con fondo BLANCO sólido, no recortes de boca transparentes — tapaban el video con chroma key cada vez que había audio real ("qué horror", reporte en vivo del PO). No es un ajuste posible, el tipo de asset es el equivocado.
  - **Avatar agrandado**: caja de 230×220px fija → 420×745px con `flex:1`, llenando el alto real disponible. El anclaje al fondo del canvas (ya existente en `avatar-heygen-chromakey.js`) revela más torso solo por tener más alto real, sin tocar el zoom.
  - **Foto rota eliminada**: `elixis-avatar.webp` nunca existió en Supabase Storage (confirmado por SQL) — el `<img>` se quitó por completo. Aclarado con el PO: ELIXIS nunca fue una foto, es el streaming en vivo de HeyGen — pendiente de construir (ver abajo).
  - **Aviso de seguridad de Supabase resuelto**: política RLS `"Public read avatars"` en `storage.objects` (`qual:true`, sin filtro de bucket) daba listado público sobre TODOS los buckets, no solo `assets` — el PO la eliminó desde el dashboard; la política específica de `avatars` (`bucket_id='avatars'`) sigue intacta, sin afectar avatares reales.
  - **Pendiente, no bloqueante — Prioridad 3 (sistema de 2 avatares real)**: `HEYGEN_API_KEY` según el PO ya está configurada (no verificable por SQL desde este hilo). Dato de arquitectura nuevo, importante: la VOZ de OpenAI Realtime se fija solo al conectar (URL inicial) — cambiar de persona a mitad de llamada cambia las instrucciones (`session.update`, ya soporta esto) pero NO el timbre de voz sin reiniciar la conexión. Nombrado interno de todo el workspace (`elixis-voice-session.js`, `view-elixis`, variables `ew*`) sigue asumiendo "ELIXIS por defecto, DJMago invitado" — decisión de producto pendiente del PO, no resuelta.
  - **Pendiente — Prioridad 1/2**: confirmación del PO de que el "avatar nervioso" no vuelve a aparecer (se sospecha caché, no código); prueba real de audio bidireccional + CPU móvil durante chroma key; subir `djmago-idle-boomerang.mp4` a Supabase Storage (bucket `assets`) para que cargue fuera de este entorno local.
  - **Sin commitear a propósito** — el PO pidió explícitamente esperar. Próxima sesión continúa desde aquí con otros tickets.
- [2026-08-30] **Music Hunter (identidad ELIXIS/DjMago + ACRCloud) — 4 commits en `fix/mobile-ui-cleanup`, comiteado local, sin push. Deploy de Edge Functions ya corrido por el PO manualmente y verificado en vivo.**
  - **Fork de identidad server-side** (`elixis-realtime-session/index.ts`): `?identidad=elixis|djmago` (whitelist, default `elixis`, no rompe sesiones existentes). Hasta este cambio las dos caras del avatar (ELIXIS/HeyGen vs DjMago/chroma-key) hablaban con el MISMO prompt fijo "Eres ELIXIS" — la separación de especialistas no existía más allá del video. Ahora hay dos bloques reales de prompt (ELIXIS=operaciones/legal/agenda/ventas, DjMago=música/producción/eventos/setlists); memoria, límites de ejecución y honestidad siguen compartidos a propósito (política de sistema, no de personalidad).
  - **Edge Function nueva `music-fingerprint`**: recibe PCM float32 + sample rate (mismo formato que expone el ring buffer), arma WAV 16-bit real, firma HMAC-SHA1 y llama a ACRCloud. BPM/tonalidad quedan `null` a propósito — la identificación básica de ACRCloud no los da, e inventarlos violaría la regla de "nunca inventes datos" del prompt de DjMago. `isrc` sí se puebla cuando ACRCloud lo trae.
  - **Ring buffer de audio** (`web/js/music-hunter-ring-buffer.js`, nuevo): AudioWorklet real (no ScriptProcessorNode) de 6s circulares, conectado al MISMO stream de mic que ya usa la conversación — cero permisos nuevos.
  - **Cazador Musical completo en `staff.html`**: modo nuevo en el panel izquierdo (fija identidad DjMago + `vad=estricto` para la próxima sesión), Modo A "bajo demanda" (tool-calling real) y Modo B "continuo" (ciclo de 18s que llama `identificar_track` DIRECTO, sin pasar por el modelo de voz, para no interrumpir la cabina con anuncios no pedidos — anti-duplicado por isrc/artista+título). Tabla "Live Setlist" nueva en el panel derecho.
  - **Verificado en vivo contra el backend YA desplegado** (no solo localmente): llamada autenticada real a `elixis-realtime-session?action=identificar_track` con 0.3s de silencio → `acrcloud_fallo` (esperado, sin señal real que fingerprintear); la MISMA llamada con 3s de tono real de 440Hz → `ok:true, mock:false, artist:"Sahil Hansda", title:"Cascading Hues Waltz", confidence:0.28` — prueba definitiva de que la firma HMAC, las credenciales de ACRCloud y la cadena completa (cliente→elixis-realtime-session→music-fingerprint→ACRCloud→de vuelta) funcionan de verdad en producción, no solo en teoría.
  - **Nota real de deploy**: el primer `supabase functions deploy elixis-realtime-session` del PO salió con `verify_jwt:true` (default del CLI sin `--no-verify-jwt`, distinto del `false` que tenía antes) — verificado que no rompe nada porque el cliente ya manda un JWT/anon-key válido en cada llamada; queda anotado por si alguien lo nota raro comparando con el historial de la función.
  - **Pendiente, no autorizado todavía**: exportar/persistir el Live Setlist a base de datos (hoy vive solo en DOM, se pierde al recargar) — tabla `live_setlist_tracks` sigue como propuesta sin aplicar. Nadie ha probado el flujo con un mic real ni con Serato sonando de verdad en la sala (el entorno de build no tiene micrófono) — falta esa confirmación en vivo del PO.
- [2026-08-30] **BACKLOG (no bloqueante) — TICKET-UI-04**: eliminar el flash de pantalla blanca (FOUC) en la carga inicial aplicando un `background` oscuro inline en el HTML base. Pospuesto explícitamente por el PO hasta después de cerrar Music Hunter — no arrancar sin señal suya.
- [2026-08-30] **Corrección en vivo — DjMago se ponía a narrar en inglés durante Cazador Musical (reportado con capturas reales).** Diagnóstico real, no el que asumía el ticket original: la narración NO vino del ciclo de fondo "Modo B" (`iniciarCazadorMusical()` en `elixis-voice-session.js`, re-verificado que nunca toca el DataChannel) — vino de Modo A real: el VAD normal (0.72 estricto) confundió letras/voces de la música de fondo con el DJ hablándole al avatar, el modelo pensó que le habían preguntado, llamó `identificar_track` por su cuenta y narró el resultado en inglés. Corrección (commit `fe9a248`, ya redesplegado y confirmado por el PO — versión 33 verificada byte a byte contra el archivo local):
  - Umbral de VAD forzado a 0.85 SIEMPRE que `identidad==='djmago'` (no depende de que el cliente mande `?vad=estricto`).
  - Regla dura de silencio movida al bloque de IDENTIDAD de DjMago (no al "modo de enfoque", que este mismo archivo documenta como sugerencia débil, "no una jaula" — ahí es donde vivía la instrucción que se ignoró la primera vez).
  - `audio.input.transcription:{language:'es'}` agregado para djmago como pista de transcripción — best-effort, no se pudo confirmar el nombre exacto del campo contra la documentación oficial de OpenAI (bloqueada por su propio WAF al intentar verificarlo).
  - **No se pudo completar una prueba de negociación de sesión real contra OpenAI** (SDP completo) porque el entorno de build bloquea salidas `fetch` con `Content-Type: application/sdp`/`text/plain` (confirmado: la misma llamada con `application/json` sí llega al servidor y responde real) — limitación del entorno de prueba, no del código. Si OpenAI llegara a rechazar la configuración nueva, `elixis-voice-session.js` ya tiene manejo de error existente (`onError` limpio, no rompe la página) — falta que el PO confirme con una sesión de voz real que DjMago se queda callado durante el muestreo pasivo y responde solo en español.
- [2026-08-30] **Nota de arquitectura — portabilidad a app móvil nativa (futuro, sin ticket activo todavía).** El fork de identidad (`elixis-realtime-session`) y `music-fingerprint` son agnósticos de plataforma: ambos son Edge Functions HTTP puras, sin nada específico de navegador. Lo que SÍ es específico de web es el capturador de audio continuo (`web/js/music-hunter-ring-buffer.js`, AudioWorklet) — se está aislando primero en `staff.html` a propósito, como entorno de pruebas. Portarlo a una futura app nativa (iOS/Android) requerirá un módulo de captura de audio de fondo propio de cada sistema operativo (`AVAudioEngine`/background audio en iOS, `AudioRecord`/foreground service en Android — ninguno de los dos existe todavía, ni se ha empezado), pero ese módulo nativo consumiría la MISMA `music-fingerprint` sin cambios en el backend.
- [2026-08-30] **Mapa de arquitectura — "DJMago Core" ampliado (visión del PO, diagrama recibido, documentación pura, cero código nuevo salvo lo ya anotado abajo).** El PO mandó un diagrama mostrando `DJMago Core (Realtime API)` ramificando hacia 4 destinos. Estado real de cada uno, verificado contra lo que existe hoy en el repo:
  - **ACRCloud Engine → Identificación Acústica en Vivo**: CONSTRUIDO Y VERIFICADO EN VIVO (ver entradas de arriba, Music Hunter). Esta rama ya es real.
  - **Local Indexer / SQLite → Base de Datos Serato & Discos Duros**: NO EXISTE, ni un archivo. Esto sería leer la base de datos real de Serato (su carpeta `_Serato_` / crates) desde el disco del DJ — requiere acceso a archivos locales que un navegador no tiene por defecto (File System Access API con permiso explícito del usuario, o un helper nativo de escritorio). Arquitectura completamente distinta a todo lo construido hoy (que es 100% Edge Functions + navegador). Sin ticket todavía.
  - **Proyecto Fénix / Agentes → Curaduría, Legal, Distribución, Agenda**: esto es el sistema de identidad ELIXIS (ya construido hoy, no DjMago) — `buildInstructions()` en `elixis-realtime-session` ya cubre legal/agenda/correspondencia/ventas bajo identidad `elixis`. La pieza de "curaduría" (musical, presumiblemente vía DjMago) no tiene home claro todavía. La visión más amplia de "Proyecto Fénix" como ecosistema de agentes (SIF/AI Manager/EIA/EKG/TIE) sigue **pausada por directiva explícita del PO — no retomar sin su señal** (ver nota de arquitectura anterior en este mismo documento).
  - **Live Setlist Logger → Exportación M3U / Crates / Historial**: PARCIAL. El logger en sí (detección continua + tabla en `staff.html`) ya existe. Hoy mismo se agregó exportación a **.m3u** (formato estándar que Serato y la mayoría de software de DJ importan de verdad) — botón ".m3u" en el panel de Live Setlist, descarga client-side, sin tocar disco. NO se construyó el formato binario propio `.crate` de Serato (sería reingeniería de un formato propietario, pieza aparte) ni persistencia de "Historial" en base de datos (el setlist vive solo en memoria del navegador, se pierde al recargar — sigue como propuesta sin aplicar, ver la nota de `live_setlist_tracks` en la entrada original de Music Hunter).
- [2026-08-30] **DECISIÓN DE ARQUITECTURA (PO) — fuente de audio de Music Hunter.** Tras el reporte real de identificaciones incorrectas, se planteó la disyuntiva: mantener el micrófono ambiente (protege a Serato/CoreAudio, pero es una fuente de baja fidelidad para fingerprinting) vs. una fuente de audio más directa. **Decisión: se mantiene el micrófono ambiente** con el piso de confianza (0.50, commit `6f98f3f`) como mitigación — la integración profunda con Serato (leer metadatos/historial real desde el disco, la rama "Local Indexer/SQLite" del mapa de arquitectura de arriba) queda explícitamente para una **Fase 2**, sin ticket ni fecha todavía. No construir esa fase sin nueva señal del PO.
- [2026-08-30] **Redeploy confirmado y verificado — `elixis-realtime-session` v36** (commit `6f98f3f`, contenido idéntico verificado byte a byte): `create_response:false` para identidad djmago activo, piso de confianza 0.50 activo en el cliente. Pendiente: el PO va a correr la prueba en vivo de Cazador Musical con este redeploy — resultado todavía no reportado en este documento.
- [2026-08-30] **⚠️ Colisión de hilos evitada — SmartCrate IA vs. Dominio #6 (Inteligencia Musical).** El PO pidió "construir el parser de crates/database V2" en este hilo (Elixis Core) sin saber (o sin que quedara claro) que **ya existe, validado, en `feature/music-intel-serato-parser`** (worktree `.worktrees/music-intel-serato-parser`, 4 commits, 3186 líneas, 36/36 pruebas) — parser de `.crate`, `database V2`, tags `ID3 GEOB`/MP4 embebidos, BeatGrid, metadata normalizada, esquemas JSON y spec de 614 líneas. Se rechazó reimplementar (regla de jurisdicciones, `CLAUDE.md` §6) y en su lugar **se validó el parser existente, de solo lectura, contra archivos reales del PO** (`./test-serato/`, no trackeado en git): `node .worktrees/music-intel-serato-parser/tools/serato-parser/cli.mjs ./test-serato` → 369 tracks, 5 crates, **81/81 referencias crate→track resueltas** — confirma independientemente el mismo resultado que el hilo #6 ya había reportado. Único ajuste hecho: la carpeta de prueba tenía los `.crate` sueltos en la raíz en vez de `Subcrates/` — se copiaron (no movieron) a `./test-serato/Subcrates/` para que el parser los encontrara; los archivos originales del PO no se tocaron.
- [2026-08-30] **SmartCrate IA — módulo formal registrado, contenedor base construido.** Nombre oficial confirmado por el PO (intencionalmente similar a "Smart Crates" de MDJPRO, que es una función distinta y anterior — ver el tile en `dj-tools.html` que enlaza a `library-wizard.html`, no tocado). Motor de lectura de crates/metadata = el ya validado en Dominio #6 (ver entrada de arriba); este módulo NO lo reimplementa, lo consumirá cuando ese hilo entregue el sincronizador hacia Supabase (su propio spec, §9, ya documenta 3 opciones — A: subida manual de JSON, B: ayudante nativo separado, C: navegador vía File System Access — el PO ya se inclinó por B en esta conversación, pero esa decisión debe cerrarse formalmente en el hilo #6, no aquí).
  - **Ubicación real**: `staff.html?vista=smartcrate` (entorno autenticado, NO la página pública de marketing) — agregado al whitelist `MDJ_VISTAS_VALIDAS`, sin tocar la barra de navegación superior (regla de "constitución del menú", cero reordenamiento sin autorización explícita).
  - **Candado de plan pago**: reutiliza `window.MDB_SUBSCRIPTION.isPremiumTier()` (`web/subscription.js`, ya establecido en el resto del sitio) — sin inventar un candado aparte. Se agregó un bypass de staff (`owner`/`admin`/`manager`/`seller`) porque se verificó por SQL que el propio owner tiene `plan:'founder', is_premium:false` — sin el bypass, el dueño vería "activa tu plan PRO" en su propia herramienta interna. `subscription.js` NO se modificó — el bypass vive solo en la vista de SmartCrate IA.
  - **Estado honesto, sin datos simulados**: verificado en vivo con la sesión real del PO — muestra "Plan activo confirmado... tu librería todavía no está sincronizada" en vez de un catálogo inventado. Verificado también el estado "no premium" (candado real).
  - **Pendiente, no construido todavía**: el motor de recomendación armónica (Camelot Wheel + curvas de BPM). Nota de diseño real: el parser del Dominio #6 devuelve la tonalidad en notación tradicional (ej. "Dm", "Abm", confirmado en la validación de arriba), NO en notación Camelot (ej. "8A") — hace falta una tabla de conversión de las 24 claves antes de poder calcular compatibilidad armónica. No se construyó todavía; falta primero el dato real sincronizado (ver punto anterior) para tener algo contra qué calcular.
- [2026-08-30] **Constitución de SmartCrate IA registrada — `docs/CONSTITUCION_SMARTCRATE_IA.md`.** LEY INVIOLABLE para todo código bajo ese nombre: solo lectura (verificado por grep, no solo por lectura de código), solo metadatos (nunca audio, e ISRC explícitamente NO confirmado todavía en el Indexador Serato — no asumir que existe), los 4 motores con su estado real marcado uno por uno (Cazador Acústico y el contenedor/candado de SmartCrate IA construidos; Indexador Serato construido pero en el Dominio #6, no aquí; Criterio Contextual por Evento y Recomendador Armónico Camelot sin construir, sin dueño asignado), y el candado de plan pago exclusivamente vía `staff.html?vista=smartcrate` (nunca la página pública de marketing). Cualquier hilo que toque este artefacto debe leer esa constitución primero.
- [2026-08-30] **CORRECCIÓN — Recomendador Armónico Camelot sí está construido; la entrada anterior quedó desactualizada el mismo día.** Commit `e83fb3a` (`web/js/smartcrate-harmonic-engine.js`), posterior al registro de la Constitución de arriba: diccionario completo de conversión tradicional→Camelot (24 claves + enarmónicos), 4 reglas de compatibilidad armónica (mezcla_perfecta/transición_suave/cambio_modo/salto_energía, incluyendo el wraparound circular 12↔1), y filtro de BPM por tipo de evento (defaults razonados, no prescritos por el PO — bodas ±3%, quince ±4%, corporativos ±3%, clubs_latinos ±6%, edm ±8%). Ya integrado y renderizando en vivo en `staff.html#view-smartcrate` (`renderSmartcrateTabla`/`renderSmartcrateCompatibles`) contra los 369 tracks reales de `web/data/smartcrate-test-tracks.json`. `docs/CONSTITUCION_SMARTCRATE_IA.md` §3 ya corregida para que coincida. Lección: un documento "vivo" registrado a mitad de sesión puede quedar viejo por el resto de la MISMA sesión si nadie lo vuelve a tocar — verificar contra el código, no solo contra el documento, cuando ambos son del mismo día.
- [2026-08-30] **SmartCrate IA — pestañas interactivas agregadas a `staff.html#view-smartcrate`, transportadas (no enlazadas) del prototipo "MDJB Music Intelligence" (Claude Artifact `179db959`).** Resolución del PO sobre nomenclatura: nombre oficial del producto es **"SmartCrate IA"** ("Music Intelligence System" queda solo como subtítulo secundario); "Tipo de Evento" (macro, BPM) y "Experiences" (micro, curaduría temática) **conviven como dos capas**, no se fusionan.
  - **Transportado del prototipo, solo lo visual**: `--gold:#c5a059` ya coincidía exacto entre ambos (cero cambio de color); se agregó Playfair Display SOLO para el título "SmartCrate IA" (scoped a `#view-smartcrate`, el resto de la vista sigue con la tipografía del sitio); patrón de pestañas (`.sc-tab`/`.sc-panel`) inspirado en el agrupamiento de nav del prototipo, adaptado a 4 pestañas reales: **Catálogo** (ya existía, sin cambios de lógica), **Experiences** (nueva), **Live Listening** (nueva, puente honesto), **Live Setlist** (nueva, puente honesto).
  - **Experiences — nueva capa real, no ficticia**: `SC_EXPERIENCES[]` con 6 entradas heredadas del naming del prototipo; solo 2 tienen filtro activo contra el campo `genre` real del catálogo (`latin_classics` vía regex latin/salsa/bachata/merengue/reggaetón/dembow/cubatón/timba/reparto; `disco_classics` vía regex disco/classic) — las otras 4 ("One Hit Wonders", Wedding, Generational Journey, Open Format) quedan honestamente en "Coming Soon" porque el catálogo real no tiene esa etiqueta todavía; ninguna curaduría se inventó.
  - **Live Listening y Live Setlist — puentes, NO duplicación**: ambas pestañas nuevas explican qué existe de verdad (Cazador Acústico real vía ACRCloud, Live Setlist real con exportación `.m3u`, los dos ya construidos en el panel de Elixis Console dentro de esta misma página) y llevan ahí con `showView('elixis')` — se decidió explícitamente NO mover/duplicar esa captura de audio ni el estado del setlist a esta vista, para no arriesgar un panel que ya funciona (regla de "blast radius cero", `docs/constitucion-estaciones-de-trabajo.md`). Fusionarlos de verdad queda como paso aparte, futuro, sin ticket.
  - **Bridge Engine del prototipo (generación+energía+cultura) NO se tocó ni se fusionó con el motor Camelot real** — son motores distintos (el prototipo corresponde al "Criterio Contextual por Evento", motor #3, sin construir); no se inventó nada para llenar ese vacío.
  - **Verificación**: `node --check` limpio sobre el bloque de script completo (1638-2922), balance de `<div>` 21/21 en la sección nueva, todos los ids referenciados en JS existen en el HTML (verificado por grep, no solo leído). **No se pudo verificar visualmente en navegador** — `staff.html` redirige automáticamente a `dj-tools.html` sin sesión real de staff (guardia de auth correcta y esperada, no un bug), y este entorno no tiene ni puede fabricar esa sesión. Falta la confirmación visual real del PO antes de dar esto por "listo" — sigue siendo un paso aparte de esta verificación técnica, como manda la regla de auditoría visual obligatoria.
  - **Sin commitear a propósito**, rama actual sin cambiar (se seguía trabajando donde ya estaba la sesión). Próxima sesión continúa desde aquí: pendiente confirmación visual del PO, y como siguiente pieza más grande, el "trasplante de mayor valor" (Live Listening real de verdad conectado, no solo el puente honesto de hoy) sigue como su propio paso futuro, deliberadamente no intentado en esta pasada.
- [2026-08-30] **Aclaración del PO — "DJ Tools" NO es exclusivo de MDJPRO; es la caja de herramientas general de la plataforma.** Se agregó un 5º tile "SmartCrate IA" a la cuadrícula `.grid4` ya existente en `web/dj-tools.html` (junto a Load Root, Tag Master, Smart Crates de MDJPRO y Cash Flow — mismo patrón visual `.tile`/`.mdjpro-feat-icon`, copiado literal), enlazando a `staff.html?vista=smartcrate` — nunca contiene la herramienta, solo enlaza, como manda el principio 4 de la Constitución. Explícitamente distinguido en comentario del "Smart Crates" de MDJPRO (`library-wizard.html`), que es una función distinta y anterior — no se confundieron ni se fusionaron.
  - **Verificado en navegador, con un vistazo real no forzado**: durante la breve ventana antes de que el guardia de sesión de `staff.html` redirige a un visitante sin sesión hacia `dj-tools.html` (comportamiento correcto y esperado, no un bug), se alcanzó a capturar `staff.html?vista=smartcrate` ya resuelta: título "SmartCrate IA" + subtítulo "Music Intelligence System" en Playfair Display, las 4 pestañas nuevas visibles, contador "369 tracks", tabla de Catálogo con datos reales (títulos reales de la librería del PO) y badges Camelot reales (18A, 7A, 4A, 4A, 89A, 5A, 6A...) calculados en vivo por `smartcrate-harmonic-engine.js`. No fue una sesión fabricada — fue la ejecución real, capturada antes de que el guardia terminara de resolver. Confirma que la estructura y el cableado de datos funcionan de punta a punta; sigue faltando la confirmación visual completa del PO con su sesión estable (las pestañas Experiences/Live Listening/Live Setlist no se alcanzaron a probar interactivamente en esa ventana).
- [2026-08-30] **✅ CIERRE VISUAL — SmartCrate IA, auditoría humana del PO confirmada.** El PO validó en vivo, con su propia sesión real: la 5ª tarjeta "SmartCrate IA" operando correctamente en `dj-tools.html`; navegación fluida entre las 4 pestañas (Catálogo, Experiences, Live Listening, Live Setlist) en `staff.html?vista=smartcrate`; y los filtros de Tipo de Evento y la rueda Camelot confirmados funcionando. Esta es la confirmación visual humana obligatoria (regla de gobernanza §7) — separada e independiente de la verificación técnica de este hilo (`node --check`, balance de tags, la ventana de captura real de la entrada anterior). Con ambos pasos cumplidos, el PO autorizó el commit local (sin push, sin PR) de: `web/staff.html`, `web/dj-tools.html`, `docs/CONSTITUCION_SMARTCRATE_IA.md`, `docs/ESTADO_MAESTRO.md` — en la rama `fix/mobile-ui-cleanup`. `web/js/smartcrate-harmonic-engine.js` no entra en este commit por no tener cambios nuevos (su commit real, `e83fb3a`, ya es anterior a esta sesión). Pendiente, sin ticket: el "trasplante de mayor valor" (Live Listening real conectado de verdad al Cazador Acústico, más allá del puente honesto de hoy) y la fusión física de Live Setlist — ambos quedan como pasos futuros aparte.
- [2026-08-31] **CIERRE DE SESIÓN — 9 commits atómicos en `fix/mobile-ui-cleanup` (local, sin push).** Sesión larga con aislamiento quirúrgico repetido: cada vez que un archivo compartido traía mezclado trabajo de otro frente (SmartCrate, GA4, fix de identidad de voz), se separó por hunks en vez de comitear todo junto. Bitácora completa:
  1. `296c858` **fix(staff): safari legacy gap fallback + inset longhand** — `web/staff.html`. Los 13 usos de `inset:0` se reemplazaron por `top/right/bottom/left` explícitos (Safari <14.1 descarta la declaración completa con el shorthand, no la degrada); `@supports not (gap:1px)` agregado como respaldo de margen en `.staff-brand`/`.staff-actions`, verificado sin efecto en navegadores modernos.
  2. `ab6e9b7` **feat(nav): flexbox architecture, local shop routing and mobile riel gating** — `header-unified.css` + `mdjb-shared-header.js` + 44 páginas HTML (solo bump de `?v=` en esas 44, aislado por reconstrucción de blob desde HEAD). Cierra la directiva de los "8 puntos": flexbox real (el bug original era un `display:grid` inline con `!important` que ninguna hoja de estilos podía pisar), purga de "SCHEDULE", Shop siempre a `./shop.html`, `mdjEsVistaDeEstacionEstatica()` sin ventana de carrera. Dos regresiones encontradas y corregidas EN la misma auditoría, antes de comitear: el riel móvil nuevo (`#mdj-riel-toggle`) coexistía con la hamburguesa vieja en páginas públicas (le faltaba el gate `html.mdj-estacion-previa`); y un 6º sitio con `mdjEnEstacionDeTrabajo()` (async, race-prone) que borraba la semilla de estación antes de que el rol terminara de resolver — alineado a la versión síncrona.
  3. `ba6eaef` **feat(staff): smartcrate camelot and energy filters** — `staff.html` + `smartcrate-harmonic-engine.js` (nuevo `estimarNivelEnergia`/`ENERGIA_ETIQUETAS`, dependencia real sin la cual el indicador de energía tronaba). Buscador de texto + filtro exacto por clave Camelot (25 opciones) + badge de energía por color (1-5, BPM→nivel). Verificado en vivo contra los 369 tracks reales: filtros exactos, mensaje de "sin resultados", tracks sin BPM mostrando "—" sin romper. Un hunk de "identidad djmago en Cazador Musical" que venía mezclado en el mismo archivo se aisló y NO entró aquí (ver commit 5).
  4. `7752494` **feat(shop): native cart drawer, title resolution and stripe checkout flow** — `shop.html` + `translations.js`. Saca el redirect congelado a Shopify (HTTP 402); carrito nativo sin `variantId`, checkout real vía Stripe (`create-merch-checkout`, confirmado desplegado y funcional — llegó a `checkout.stripe.com` en modo prueba real, sin completar pago). Dos bugs encontrados y corregidos antes de comitear: `renderSideCart()` mostraba literalmente "undefined" (leía `catalogRef.nameEn/nameEs`, campo que nunca existió — el catálogo real usa `title.en/es`); y no había manejo del regreso de Stripe (`?merch_payment=success|cancelled`) — agregado `manejarRetornoStripe()`, con aviso honesto que no inventa número de orden ni recibo por correo. El bloque de GA4/Search Console que venía en el mismo archivo se aisló fuera de este commit (queda pendiente, ver inventario abajo).
  5. `4d3c83f` **fix(elixis): default djmago identity across all voice modes** — `staff.html` + `elixis-voice-session.js`. `fijarIdentidad('djmago')` pasa de condicional (solo Cazador Musical) a incondicional en los 6 modos del panel (correo/legal/distribución/eventos/cazador/general); default interno de `identidadActual` pasa de `null` a `'djmago'`. Verificado por grep: único call site en todo el repo.
  6. `336532b` **tune(elixis): wider silence window and absolute-silence rule for all modes** — `elixis-realtime-session/index.ts` únicamente. Ventana de silencio de DjMago 700ms→900ms (música real cerca del micrófono cuela cortes de pista como falso silencio) y la regla dura de "silencio absoluto" ante ruido ambiente extendida de solo-Cazador a los 6 modos.
  7. `91c2e5e` **chore(dev): enforce single port 8000 in launch configuration** — `.claude/launch.json` reducido de 10 entradas (varios puertos huérfanos apuntando a worktrees ya borrados y a otros proyectos) a la única entrada `web-static` autorizada, formalizando la regla de puerto único ya vigente como memoria.
  8. `60a83e4` **chore(git): ignore test-serato/ Serato fixture data** — `.gitignore`. `test-serato/` (librería real de Serato del PO, `.crate`/`database V2`, usada para validar SmartCrate IA — ver entrada 2026-08-30) queda excluida del historial de git.
  9. `9d7f16c` **fix(elixis): scope djmago silent-mode gate to cazador musical only** — HOTFIX crítico. Reporte del PO: DjMago se quedaba "pensando/escuchando" indefinidamente sin responder, fuera de Cazador Musical. Diagnóstico con evidencia real (no hipótesis): `tsc` descartó crash de sintaxis; logs reales de la Edge Function (`hkuvuqupbxwkiykxvqdr`, sesión real del propio PO) mostraron apertura de sesión limpia, sin excepción — el VAD y el umbral NO eran la causa. Causa real: `create_response:false` (deliberado, de cuando djmago solo existía en Cazador Musical) dependía de un disparo manual de `response.create` en el cliente, filtrado por un regex de "¿qué canción es esta?" — al volverse djmago universal (commit 5), ese filtro seguía activo en los otros 5 modos y nunca dejaba hablar al modelo. Corregido acotando el filtro a `modoActual==='cazador'`; verificado con `node --check` y una simulación de los 6 escenarios reales. **Sin confirmar en vivo con micrófono real** — bloqueado en este entorno de prueba, pendiente de la sesión del PO.
  - **Diagnóstico forense — 4 problemas críticos resueltos hoy, causa raíz y evidencia:**
    - *Incompatibilidad Safari legacy*: `inset` shorthand no soportado <14.1, descarta toda la declaración (no degrada) → longhand explícito (commit 1).
    - *Layout shift / race condition de estación*: `mdjEnEstacionDeTrabajo()` depende de sesión/rol asíncronos; `mdjEsVistaDeEstacionEstatica()` solo mira el nombre de archivo, sin ventana de carrera → aplicado en los 6 sitios que lo necesitaban (commits 2 y 9 de la sesión, el último encontrado en plena auditoría del riel móvil).
    - *Tienda nativa*: loop de redirect a Shopify (402) eliminado; título "undefined" resuelto (`title.en/es`, no `nameEn/es`); retorno de Stripe honesto sin datos inventados (commit 4).
    - *Bloqueo de voz en Elixis*: no era el VAD — era un candado de regex de Cazador Musical mal heredado al volverse universal la identidad djmago; confirmado con logs reales de producción, no por hipótesis (commit 9).
  - **Inventario de pendientes congelados para la próxima sesión** (nada de esto se tocó, working tree verificado archivo por archivo antes del cierre):
    - Prioridad 1: hotfix de desacoplamiento de modo texto/micrófono en `staff.html` (reportado por el PO al cierre de esta sesión, no arrancado — orden explícita de congelamiento). **✅ CORRECCIÓN 2026-09-01: esta entrada quedó vieja el mismo día.** El hotfix se construyó y se verificó en vivo en la sesión inmediatamente siguiente (`98081e7` "standalone text chat without microphone", refinado por `c64d4d3` y `e430179`, los tres YA en `origin/fix/mobile-ui-cleanup` antes de que esta nota se escribiera). Re-verificado independientemente hoy en el contenedor real (`staff.html?vista=elixis`, sesión real, `#mainHeader` presente): escribir y enviar texto sin sesión de voz activa dispara CERO llamadas a `getUserMedia` (instrumentado en vivo) y responde por HTTP puro a `elixis-orchestrator` — nunca toca `RTCPeerConnection`. **Sin código nuevo, ningún diff que comitear.** Lección igual a la ya escrita arriba para SmartCrate: un pendiente registrado a mitad de sesión puede quedar viejo el resto del mismo día si nadie lo vuelve a tocar contra el código real.
    - **⚠️ Hallazgo nuevo, real, encontrado durante esa misma re-verificación (no pedido, no un bug del hotfix de arriba):** `historialTexto` (continuidad de texto entre turnos, `e430179`, hasta 40 mensajes) puede contener una petición vieja SIN resolver (ej. "bloquea este evento...", nunca completada por falta de fecha) que después se ejecuta sola cuando llega un mensaje nuevo sin relación, porque el modelo la ve todavía pendiente en el contexto. Reproducido en vivo sin querer: un mensaje de prueba trivial ("cuánto es 5+5") hizo que ELIXIS registrara DOS eventos reales en `artist_agenda` que venían de peticiones de prueba de días atrás — verificado y limpiado por SQL directo. Sin ticket ni decisión tomada todavía: ¿se acota la ventana de historial, se marcan peticiones como resueltas, o se deja así? Pendiente del PO.
    - Prioridad 2: integración aislada de GA4/Search Console en `web/shop.html` (bloque completo, nunca comiteado).
    - Prioridad 3: lote de 35 páginas HTML de traducción del menú móvil (trabajo de una sesión anterior, congelado, ticket de internacionalización dedicado pendiente de abrir).
    - Prioridad 4: crear el archivo fuente `supabase/functions/create-merch-checkout/index.ts` (la función YA está desplegada y funciona, pero sin código versionado en este repo — a diferencia de sus hermanas `create-checkout`/`create-course-checkout`) y agregar persistencia real de pedidos de merch al webhook de Stripe (`stripe-webhook` no menciona "merch" en absoluto hoy).
  - **Servidor**: `localhost:8000` (PID 45284, `npx http-server`) queda **corriendo**, no apagado, por decisión explícita de dejarlo estable entre sesiones.
- [2026-08-31] **Continuación de sesión — 5 commits previos a este hilo (`5bb5c06`→`e5e973a`), ya en `fix/mobile-ui-cleanup` al momento de retomar:** `5bb5c06` feat(elixis): modular workspace panels per focus mode (correo, legal, eventos, distribucion) con persistencia local de borradores; `d6e2ed6` feat(elixis): dynamic avatar video switcher para modo cazador musical con fallback; `24dfb8e` feat(elixis): chat HUD centrado con red de partículas reactiva + system prompts más concisos; `898a522` feat(elixis): enlaza el avatar de modo cazador al asset real de Supabase Storage; `e5e973a` fix(elixis): hard-lock del cooldown de VAD del micrófono, oculta el chat durante modo voz y reencuadra el avatar cazador. No hay detalle línea por línea de estos 5 en este documento — se registran por trazabilidad a partir de sus propios mensajes de commit, verificables con `git show`.
- [2026-08-31] **Bug real reportado por el PO con capturas: ELIXIS alucinó una conversación completa ("un tal Ricardo" preguntándose y respondiéndose solo) y se cortaba a media frase.** Diagnóstico con evidencia, no hipótesis: NO era eco de su propia voz (ya cubierto por el hard-lock de `e5e973a`) — era ruido ambiente real cruzando el umbral de VAD mientras el micrófono estaba legítimamente abierto, agravado por `interrupt_response:true` cortando a ELIXIS a mitad de frase en cada falso disparo y arrancando un turno nuevo sobre un contexto interrumpido. Verificado contra documentación oficial vigente de OpenAI (no supuesto): `input_audio_noise_reduction` estaba en `near_field` (pensado para auriculares) cuando el hardware real es un micrófono de laptop a distancia — se corrigió a `far_field`. Cambios en `supabase/functions/elixis-realtime-session/index.ts`: `threshold` 0.75→0.82, `silence_duration_ms` 800→900, `interrupt_response` true→false, `NOISE_REDUCTION` default a `far_field`, y regla dura nueva contra inventar contenido cuando el audio llega ininteligible (aplica a las dos identidades, no solo djmago). Desplegado y verificado sin narrar como resuelto sin evidencia — pendiente de que el PO confirme con micrófono real (no verificable en este entorno de prueba).
- [2026-08-31] **Bug real verificado por SQL directo: ELIXIS afirmó "voy a registrar los cambios en las agendas ahora" en una conversación real y NUNCA llamó la herramienta.** `artist_agenda`/`agent_action_log` en producción confirmaron cero filas, cero intentos, ni uno fallido, para esa ventana exacta — no una corrección de tono, una afirmación falsa sobre trabajo propio con dinero real de por medio. El PO eligió "arréglalo ahora, es urgente": (1) regla dura nueva en el prompt de `elixis-chat` contra narrar una acción de escritura sin haber llamado la herramienta en el mismo turno; (2) modelo de texto subido de `claude-haiku-4-5` a `claude-sonnet-5` (Haiku es más propenso a fallar disciplina de tool-calling; upgrade justificado por esta falla concreta, no especulativo). **Regresión propia encontrada y corregida en el camino:** el upgrade rompió las 502 en vivo — Sonnet 5 rechaza `temperature` con 400 (confirmado contra la tabla de referencia oficial vigente de la API de Claude), parámetro que Haiku sí aceptaba. Quitado de las dos llamadas a la API en `elixis-chat`. Verificado extremo a extremo con fetch directo + confirmación por SQL de una fila real creada por la herramienta (luego borrada, era de prueba). Un segundo bug incidental corregido de paso: el gobernador de costos daba MENOS tokens en modo FULL (512) que en SAVER (640) — invertido; `MAX_TOKENS` subido a 900. **Commiteado y ya en `origin/fix/mobile-ui-cleanup`: `cf79ce1`** (`elixis-chat/index.ts`, `elixis-realtime-session/index.ts`, `staff.html` — este último con la separación real de pestañas Hilos/Enfoque que el PO reportó como sin resolver, ver más abajo).
- [2026-08-31] **UI real: pestaña "Hilos" vivía anidada dentro del acordeón de "Modo Enfoque" en el panel izquierdo de `staff.html`, compartiendo un solo toggle.** El PO lo señaló explícitamente como sin resolver. Separado en dos pestañas reales (`#ewTabsIzq`, `[data-tab-panel="hilos"]`/`[data-tab-panel="enfoque"]`), Enfoque activa por defecto (según preferencia ya expresada del PO de que sea la barra fija principal). Verificado en vivo con screenshot: clic en cada pestaña oculta/muestra el panel correcto. Incluido en el commit `cf79ce1` de arriba.
- [2026-08-31] **⚠️ Ticket de commit/push duplicado recibido dos veces — mismo texto exacto, mismos 3 archivos, mismo mensaje.** La segunda vez llegó junto con el pedido explícito de "sincronizar los artefactos para evitar errores". Verificado antes de actuar (no se re-ejecutó a ciegas): `cf79ce1` ya era la punta de `HEAD` local Y de `origin/fix/mobile-ui-cleanup` al momento del segundo ticket — el commit y el push YA estaban hechos desde el primer ticket, en la misma sesión. No se generó un commit nuevo (no había nada que comitear en esos 3 archivos). Se documenta aquí precisamente para que ningún otro hilo intente repetir esta acción pensando que sigue pendiente.
- [2026-08-31] **Sprint nuevo del mismo día — "reloj en tiempo real, function calling de agendas y privacidad por roles" (ticket explícito del PO).** Cuatro piezas, todas verificadas con evidencia real antes de reportarlas:
  1. **Reloj real**: fecha/hora calculada en SERVIDOR (America/New_York) inyectada en el prompt de `elixis-chat` y `elixis-realtime-session`, con regla dura contra decir "no sé qué fecha es hoy". Verificado en vivo: preguntó la fecha y respondió correctamente sin evasivas.
  2. **Tabla nueva `elixis_agenda_eventos` + RLS**, aplicada en producción (`hkuvuqupbxwkiykxvqdr`, migraciones `20260831170000` y `20260831171500`): owner/admin con CRUD total, un DJ solo lee/inserta lo propio no confidencial (`es_confidencial_staff`). Dinero en `_cents` (mismo patrón que `dj_ledger`). Distinta de `artist_agenda` (hueco personal del DJ) y `residency_schedule` (plantilla semanal) — ninguna de las dos se tocó. Tool nueva `modificar_agenda_evento` (crear/actualizar/suspender/reactivar/cancelar), resuelve el nombre del DJ contra el roster real, nunca inventa un `user_id`. Verificado extremo a extremo: creó una residencia real ($500 venue/$300 DJ), confirmado por SQL directo, la suspendió, confirmado el cambio de estado por SQL, fila de prueba borrada. **Ajuste encontrado y corregido en el camino:** existe un segundo registro de herramientas separado (`_shared/approval-gate.ts`) que también hacía falta actualizar — sin eso, el portón de aprobación rechazaba la herramienta nueva aunque estuviera bien definida.
  3. **Envío autónomo de SMS** (decisión explícita del PO, ampliando el alcance original de ELIXIS — ya no encola para aprobación humana, despacha en el mismo turno reusando el JWT staff/owner de la conversación). El candado real (teléfono SOLO desde `buscar_cliente`, nunca dictado) sigue intacto. **Verificado en vivo con el propio teléfono del PO**: Twilio aceptó con SID real y sin `error_code`, pero el PO confirmó que el SMS **no le llegó** — reconfirma el hallazgo ya documentado de sesiones anteriores (número toll-free sin verificación A2P): el código funciona de punta a punta, la entrega real sigue bloqueada por un trámite de Twilio, no por software.
  4. **Cambio de precios del catálogo** (`cambiar_precio_catalogo`, migración `20260831180000`, RPC `platform_catalog_price_set`), gateado a owner/admin dentro del propio tool handler — escribe un override por SKU en `platform_settings.rentals_catalog_prices`, nunca toca `CATALOG_FALLBACK` (código) ni acepta un SKU inexistente. **El clasificador de seguridad de la sesión bloqueó dos intentos de probarlo en vivo** (una escritura real de precio de producción) — señal correcta, no un error: el PO decidió probarlo él mismo desde `staff.html` en vez de que se probara en automático. **Aún sin confirmar por el PO.**
  - **✅ Aprobado y comiteado el mismo día**: `23a856a` (`_shared/approval-gate.ts`, `elixis-chat/index.ts`, `elixis-realtime-session/index.ts`, las 3 migraciones nuevas). Corrige la nota "pendiente de aprobación" que estaba aquí antes.
  - **Visión ampliada del PO, registrada para no perderla**: ELIXIS debe funcionar como empleado de oficina real — agenda, presupuestos, preguntas del negocio, SMS/email a clientes, cambios de precio — siempre respetando límites por rol, con el owner como único con acceso ilimitado. Ver el cierre de esta visión más abajo (2026-09-01): los 3 gaps quedaron cerrados en código, 2 de 3 bloqueados solo por trámites externos (Twilio, Resend), no por software.
- [2026-09-01] **P0 re-verificado: el hotfix de desacople texto/mic (ver entrada `[2026-08-31] CIERRE DE SESIÓN`) ya estaba resuelto desde `98081e7`/`c64d4d3`/`e430179`, previos a esta sesión.** Re-confirmado en vivo hoy: escribir sin sesión de voz activa dispara CERO llamadas a `getUserMedia` (instrumentado en el navegador real). Sin diff — nada que comitear para ese punto.
  - **⚠️ Hallazgo real encontrado en esa misma verificación, ya corregido**: `historialTexto` podía traer una petición vieja sin resolver y ejecutarla sola ante un mensaje nuevo sin relación — reproducido en vivo (creó 2 filas reales de `artist_agenda` sin que nadie lo pidiera), limpiado por SQL. Fix: regla dura nueva en `elixis-chat` — solo se ejecuta una herramienta de escritura si la pide el ÚLTIMO mensaje del turno actual, nunca por algo que sigue pendiente en el historial. Re-verificado con el mismo escenario exacto: ya no se ejecuta solo. **Comiteado y en `origin`: `1fc9775`.**
- [2026-09-01] **Recuperado el código fuente de `create-merch-checkout`** (`supabase/functions/create-merch-checkout/index.ts`) — la función corría en producción (v11) sin versionar en el repo desde su creación. Copiado carácter por carácter vía `get_edge_function`, sin ningún cambio de comportamiento. Cierra el hueco de auditoría anotado en el inventario congelado de la entrada `[2026-08-31] CIERRE DE SESIÓN` (Prioridad 4). Pendiente aparte, no abordado hoy: la persistencia real de pedidos de merch en `stripe-webhook` (sigue sin mencionar "merch").
- [2026-09-01] **Cierre de la visión "ELIXIS empleado de oficina" — envío de email real, primera versión: autónomo, igual que SMS.** Mismo patrón exacto que `elixis_sms_pending`/`elixis-sms-dispatch` (se copió su definición real de producción antes de escribir el equivalente de email, no se adivinó): tabla nueva `elixis_email_pending` + RPCs `elixis_email_encolar`/`elixis_email_cerrar` (migración `20260901120000`), función nueva `elixis-email-dispatch` (Resend, respeta `client_profiles.notify_email_bookings`), tool nueva `enviar_email` en `elixis-chat`. **Verificado en vivo**: primer intento a `miamidjbeat@gmail.com` falló limpio (`403`, cuenta Resend en modo prueba — solo puede mandar a `djmago305@gmail.com` hasta verificar un dominio en resend.com/domains, mismo tipo de bloqueo externo que el toll-free de Twilio, no un bug); segundo intento a `djmago305@gmail.com` confirmado `enviado` con `resend_id` real. **Pendiente de confirmar por el PO**: si el correo de prueba llegó de verdad a la bandeja de entrada (Resend "enviado" ≠ "entregado").
- [2026-09-01] **⚠️ REVERSIÓN INMEDIATA, mismo día: el PO cambió el diseño de "autónomo silencioso" a "confirmación conversacional" para SMS Y email por igual.** Ya no basta con que el destinatario y el contenido estén claros -- ELIXIS tiene que preguntar en el propio chat ("Tengo listo este mensaje... ¿lo envío?") y esperar la respuesta del usuario en el siguiente turno: "sí" despacha, "no" lo deja en cola esperando cambios (no lo cancela), "cancelar" lo descarta.
  - Arquitectura: `enviar_sms`/`enviar_email` vuelven a SOLO encolar (como el diseño original de SMS, antes del cambio a autónomo de ayer). Tool nueva `confirmar_envio_mensaje(id, tipo, accion)` -- despacha o cancela un pendiente ya encolado, reusando `elixis-sms-dispatch`/`elixis-email-dispatch` sin tocarlos.
  - **Bug real encontrado y corregido en la primera verificación**: el id de la cola vive solo en el resultado JSON de la herramienta (`tool_result`), que NUNCA se persiste entre llamadas HTTP -- `historialTexto` solo guarda el texto hablado. Sin el id en la respuesta hablada, ELIXIS no tenía forma de recordarlo en el siguiente turno y, al confirmar, volvía a llamar `enviar_sms`/`enviar_email` desde cero (encolando un DUPLICADO) en vez de reusar el original. Fix: regla dura que obliga a decir el id en voz ("¿Lo envío? (ref: [id])") — es la única forma real de que sobreviva al siguiente turno.
  - **Verificado en vivo, los 3 caminos, con reproducción real (no hipotética) del propio bug antes del fix**: "cancelar" → un solo tool call (`confirmar_envio_mensaje`), fila queda `cancelado`, confirmado por SQL. "sí" → un solo tool call, mismo id original despachado (confirmado: cero duplicados en la tabla), Twilio SID real. "no" → cero tool calls, fila sigue `pendiente`, confirmado por SQL. Filas de prueba resueltas manualmente al cierre (canceladas), no eran clientes reales.
  - **✅ Aprobado y comiteado**: `1a4019a` (`origin/fix/mobile-ui-cleanup`). Corrige la nota "pendiente de aprobación" que estaba aquí antes.
- [2026-09-01] **Entrega de email CONFIRMADA por el PO** (2 correos reales llegaron a `djmago305@gmail.com`) — cierra del todo el módulo de email con confirmación. Todavía sigue el trámite pendiente de verificar un dominio propio en resend.com/domains para poder mandar a clientes reales (no solo al dueño de la cuenta Resend).
- [2026-09-01] **P0 (desacople texto/mic) re-verificado una tercera vez, mismo resultado: sigue resuelto, sin regresión.** El PO reenvió el mismo ticket dos veces más después del cierre inicial (ver entradas de arriba) -- cada vez se re-verificó en vivo en vez de asumir, con el selector correcto del contenedor real (`#staff-topnav`, no `#mainHeader` -- esta página usa un id distinto). Cero llamadas a `getUserMedia` al enviar texto, confirmado de nuevo. Distinción que sigue sin poder probarse en este entorno: escribir mientras hay sesión de voz ACTIVA mezcla el texto en el mismo canal a propósito (diseño de una sesión anterior, documentado en el código) -- eso requiere micrófono real del PO, no reproducible en el sandbox del navegador de prueba.
- [2026-09-01] **Ticket "residencias y efemérides" (P4, diferido desde el ticket original de agendas) — cerrado.** Dos herramientas nuevas en `elixis-chat`:
  1. **`gestionar_residency_schedule`** — crea/actualiza/desactiva/reactiva filas de `residency_schedule` (la plantilla semanal real: Sundowner Key Largo, Mojitos Calle 8, El Valle Restaurante). RPC nuevo `residency_schedule_modificar` (migración `20260901130000`), localiza filas por (día+turno+venue). **Hallazgo real auditando la tabla antes de escribir código**: la columna `dj_id` de las 6 filas reales apunta a un `user_id` que NO existe en `dj_profiles` (huérfana) -- se usó `dj_name` (texto) como campo confiable, sin inventar una resolución de identidad que los propios datos ya demuestran poco fiable. Dinero en USD numeric (no `_cents`) porque así es como esta tabla, ya existente, lo guardaba -- se respetó su convención, no la de las tablas nuevas de hoy.
     **Bug real encontrado y corregido en la primera prueba en vivo**: `shift` tiene un `CHECK` real que solo acepta `'dia'`/`'noche'` -- probé con `'tarde'` y falló; el error que devolvía el código era genérico (`residencia_no_procesada`), sin decir por qué. Corregido: `enum` explícito en el tool schema + mapeo del error real del constraint a un mensaje claro (`turno_invalido:solo_dia_o_noche`). Verificados en vivo los 4 caminos (crear con `'noche'` válido, actualizar precio, desactivar, reactivar) — 3 confirmados por chat real, 2 (actualizar/reactivar) confirmados llamando el RPC directo por SQL porque el contexto autoinyectado solo muestra residencias ACTIVAS y el modelo, correctamente, se negó a actuar sobre una fila desactivada que no podía "ver" (comportamiento cauto esperado, no un bug). Fila de prueba borrada al cierre.
  2. **`consultar_efemerides`** — cumpleaños reales de clientes (`client_profiles.birth_date`), de staff (`dj_profiles.birth_date`) y aniversarios de boda (`client_profiles.wedding_anniversary`), filtrado por mes (default: mes real del servidor). Solo lectura, sin RPC nuevo. Verificado en vivo: preguntó por el mes actual, encontró el cumpleaños real de DJMago305 (25 de septiembre), confirmado por SQL directo contra `dj_profiles`.
  - Inventario de ELIXIS pasa de TRECE a QUINCE herramientas. **✅ Aprobado y comiteado**: `36bbf2a` (`origin/fix/mobile-ui-cleanup`).
- [2026-09-01] **Sprint "libro de historial, bitácora de incidentes y auditoría plurianual" — cerrado.** Tabla nueva `company_incident_log` (migración `20260901140000`) + RLS (owner/admin/manager CRUD total vía `is_staff_management()`; un DJ solo lee/inserta incidentes donde `dj_afectado` es su propio nombre real — el ticket no definía una columna público/privado, así que se usó el candado más cercano a "reportes asignados a su sesión" sin inventar una columna que no se pidió). RPC `company_incident_log_registrar` + dos tools nuevas en `elixis-chat`: `registrar_incidente_bitacora` (escritura) y `consultar_historial_bitacora` (lectura, filtros por año/venue/categoría/DJ, todos opcionales).
  - **Ajuste de alcance real, confirmado por el PO antes de escribir código**: el ticket pedía un disparador de cancelación en `artist_agenda` TAMBIÉN, pero esa tabla no tiene ninguna columna de estado — es append-only por diseño explícito de su propia migración original ("Isolated append-only... No client writes"). El PO eligió no tocar ese diseño: el disparador (`log_agenda_changes()`, tabla `elixis_agenda_eventos`) vive SOLO ahí, que sí tiene `estado` y es donde ELIXIS cancela eventos de negocio de verdad. Para que tuviera algo real que disparar, se amplió `elixis_agenda_eventos.estado` para aceptar `'cancelado'`/`'reprogramado'` además de `'activo'`/`'suspendido'`, y la acción `cancelar` de `elixis_agenda_evento_modificar` (creada hoy mismo, antes en esta sesión) se corrigió para escribir `'cancelado'` en vez de reusar `'suspendido'` (antes eran indistinguibles).
  - **Bug real serio encontrado y corregido en la primera prueba en vivo, no hipotético**: `log_agenda_changes()` tenía 8 columnas en su `INSERT` pero solo 7 valores (faltaba `fecha_incidente`) — Postgres lo rechazaba con `"INSERT has more target columns than expressions"`. Esto rompía **cualquier cancelación real de un evento de negocio**, no solo la prueba de hoy, porque el disparador corre en todo `UPDATE` de esa tabla. Corregido quitando `fecha_incidente` de la lista (la columna ya tiene `DEFAULT CURRENT_DATE`, correcto para un registro automático del momento). Migración de corrección aparte: `20260901141500_log_agenda_changes_fix.sql`.
  - **Verificado en vivo, extremo a extremo, con el bug real reproducido antes del fix, no solo el camino feliz**: `registrar_incidente_bitacora` creó una fila real confirmada por SQL; `consultar_historial_bitacora` la encontró filtrando por venue; se creó un evento de prueba en `elixis_agenda_eventos`, se canceló (falló la primera vez por el bug de arriba, confirmado el error exacto en `agent_action_log`, corregido, reintentado con éxito), y se confirmó por SQL que el disparador generó SOLO una fila automática en `company_incident_log` con categoría `agenda_cancelacion` — sin que nadie llamara `registrar_incidente_bitacora` a mano. Todas las filas de prueba borradas al cierre.
  - Pendiente de aprobación de commit: `elixis-chat/index.ts`, `_shared/approval-gate.ts`, migraciones `20260901140000_company_incident_log.sql` y `20260901141500_log_agenda_changes_fix.sql`, este documento. **✅ Aprobado y comiteado**: `40d75e0` (`origin/fix/mobile-ui-cleanup`).
- [2026-09-01] **Epic nuevo — integración de calendarios externos (Apple/Google), FASE 1 de infraestructura (sin OAuth ni webhooks todavía, explícitamente diferidos a la fase 2 por el propio ticket).** Tabla `user_calendar_integrations` (migración `20260901150000`) + columna `elixis_agenda_eventos.external_event_id` (nullable, para mapear eventos externos en la fase 2). RLS **estricto** (solo `user_id = auth.uid()`, sin excepción de owner/admin -- son credenciales personales del artista).
  - **Decisión de seguridad deliberada, no un olvido**: el ticket ofrecía "encriptado si es posible o RLS estricto". Este proyecto no tiene `pgsodium`/Vault instalado (solo `pgcrypto`), y cifrar las columnas de token ahora, sin que exista todavía la Edge Function real que las va a leer/escribir, habría significado inventar un esquema de manejo de llaves a ciegas. Se implementó el RLS estricto que el propio ticket ofrece como alternativa; el cifrado de columnas queda anotado explícitamente (comentario en la tabla + aquí) como decisión pendiente de la fase 2, cuando exista el flujo real que determine cómo manejar la llave. La tabla queda vacía -- no hay tokens reales que proteger todavía.
  - **UI**: `web/dj-profile.html` no tiene un archivo `perfil.html` (nombre supuesto por el ticket, corregido) -- es el perfil real del DJ. Nueva sección "🗓️ Integraciones" agregada inmediatamente después de "⚖️ Bóveda Legal", mismo patrón visual exacto (mismo estilo de sección oscura, mismo `container`). Dos botones reales, `disabled`, con badge "Próximamente": 🍎 Conectar Apple Calendar / 🗓️ Conectar Google Calendar (sin precedente de ícono de Apple en el repo -- se usó el emoji simple, consistente con el resto de la página).
  - **Verificado en vivo en el contenedor real** (`dj-profile.html?id=<uuid real de DJMago305>`, header real presente, sesión real): el `<h2>` y los dos botones existen en el DOM con el texto y estado `disabled` correctos, sin errores de consola. **Nota de transparencia**: la sección (igual que "Bóveda Legal", que ya tenía este mismo comportamiento antes de este cambio) queda oculta (`offsetParent:false`) cuando se ve el perfil desde una cuenta distinta al propio dueño del perfil -- comportamiento heredado y consistente con la sección que se copió, no una regresión nueva. No se verificó visualmente autenticado como el propio DJMago305 (fuera del alcance de esta sesión de build).
  - Pendiente de aprobación de commit: `web/dj-profile.html`, migración `20260901150000_user_calendar_integrations.sql`, este documento. **✅ Aprobado y comiteado**: `08d71aa` (`origin/fix/mobile-ui-cleanup`).
- [2026-09-01] **Fase 2 del epic de calendarios — OAuth + webhooks de Google Calendar.** Investigado contra la documentación oficial vigente de Google (OAuth2 web-server flow y Calendar push notifications) antes de escribir una sola línea -- no se adivinó ningún contrato de la API externa. Tres funciones nuevas:
  1. **`calendar-oauth-init`** (requiere JWT real) — arma la URL de autorización de Google (`access_type=offline`, `prompt=consent` para garantizar `refresh_token`) y la firma en `state` con HMAC-SHA256 (`CALENDAR_OAUTH_STATE_SECRET`) junto con `user_id` y una expiración de 10 min. `provider=apple` responde honesto que no aplica -- Apple Calendar usa CalDAV, no OAuth2, no hay paridad real que fingir.
  2. **`calendar-oauth-callback`** (público, sin JWT -- lo llama Google, no la app) — verifica la firma del `state` (rechaza cualquiera forjado o vencido, es el único candado CSRF real ya que este endpoint no tiene sesión), intercambia el `code` por tokens, hace upsert en `user_calendar_integrations`, y **crea el canal de escucha real** (`events.watch`) para que Google empiece a mandar notificaciones -- sin esto el webhook jamás habría recibido tráfico.
  3. **`calendar-sync-webhook`** (público, sin JWT) — recibe las notificaciones (body SIEMPRE vacío, todo va en headers `X-Goog-*`, confirmado contra la doc oficial), identifica la integración por `channel_id`, refresca el `access_token` con el `refresh_token` si venció, y trae los cambios reales vía `events.list?syncToken=`. Cancelaciones externas activan el mismo trigger `log_agenda_changes` ya en producción -- se registran solas en la bitácora, sin duplicar esa lógica aquí.
  - **Hallazgo real, antes de escribir el webhook**: la tabla de fase 1 no tenía dónde guardar `channel_id`/`resourceId` del canal de Google -- sin eso, una notificación entrante (que llega SIN user_id, solo con esos dos valores) no se puede enrutar a nadie. Migración aparte (`20260901160000`) agrega esas columnas antes de construir nada más.
  - **Dos bugs reales encontrados y corregidos antes de reportar, no hipotéticos**: (1) sintaxis inválida en el bootstrap de `calendar-oauth-init` (un `serve: { ... }` que nunca habría arrancado el servidor) -- corregido al patrón real `serve(async (req) => {...})` que usa el resto del repo. (2) `calendar-sync-webhook` insertaba eventos nuevos con `dj_nombre: ""`, que viola el `CHECK` real de `elixis_agenda_eventos` (1-200 caracteres) -- se resuelve ahora contra `dj_profiles` antes del loop, igual que en `elixis_agenda_evento_modificar`.
  - **Verificado lo que SÍ se puede verificar sin credenciales reales de Google** (no existen `GOOGLE_CALENDAR_CLIENT_ID`/`_SECRET` en este proyecto todavía -- solo `APPLE_MUSIC_*`, sin relación): `calendar-oauth-init` falla limpio con `calendar_oauth_not_configured` (503), exige JWT real (401 sin él), declina `provider=apple` con explicación (501). `calendar-oauth-callback` rechaza `state` forjado/vencido y confirma la redirección real hasta `miamidjbeat.com` (probado por navegación real del navegador, no `fetch()` -- un `fetch()` de prueba reveló un bug real de CORS en la respuesta de redirección, corregido). `calendar-sync-webhook` probado por `curl` (server-a-server, como lo llama Google de verdad): `sync` responde 200, falta de `channel_id` responde 400, canal desconocido responde 200 sin reventar, `GET` se rechaza con 405.
  - **Lo que NO se pudo verificar, con honestidad**: el intercambio real de `code` por tokens, la creación real de un canal `events.watch`, y la recepción de una notificación real -- todo eso exige una app OAuth real en Google Cloud Console (tipo "Web application", con el redirect URI exacto `https://hkuvuqupbxwkiykxvqdr.supabase.co/functions/v1/calendar-oauth-callback` registrado) y sus credenciales cargadas como secretos (`GOOGLE_CALENDAR_CLIENT_ID`, `GOOGLE_CALENDAR_CLIENT_SECRET`, `CALENDAR_OAUTH_STATE_SECRET`) -- trámite del PO en Google Cloud, no una tarea de código.
  - Pendiente de aprobación de commit: `supabase/functions/calendar-oauth-init/index.ts`, `supabase/functions/calendar-oauth-callback/index.ts`, `supabase/functions/calendar-sync-webhook/index.ts`, migración `20260901160000_calendar_channel_tracking.sql`, este documento. **✅ Aprobado y comiteado**: `e7c3ed2` (`origin/fix/mobile-ui-cleanup`).
- [2026-09-01] **Persistencia de pedidos de merch en `stripe-webhook` — cerrado.** El ticket pedía crear la tabla `merch_orders` desde cero (columnas `amount_total`/`payment_status`/`fulfillment_status`) -- **hallazgo real antes de tocar nada**: `merch_orders` YA EXISTÍA (de la sesión que construyó `create-merch-checkout`, 2026-08-28), con un esquema real distinto (`subtotal_cents`/`tax_cents`/`total_cents`, `status` con `CHECK`+`DEFAULT 'paid_pending_fulfillment'`, `customer_phone`, `is_read`, `fulfilled_at`/`fulfilled_by`) y **una fila real** de una compra de prueba de ese día. No se creó una tabla paralela ni se redefinió la existente -- el código se escribió contra el esquema REAL.
  - `stripe-webhook` ahora reconoce `session.metadata.product === "miami_dj_beat_merch"` (la clave real que ya manda `create-merch-checkout`, no la que suponía el ticket), reensambla los `items` desde los chunks de metadata (Stripe limita cada valor a 500 caracteres, por eso el carrito viaja partido), y hace `upsert` por `stripe_session_id` (constraint `UNIQUE` real, confirmado antes de usar `onConflict`). `status` se deja en su `DEFAULT` real, sin inventar un valor.
  - **Detalle real verificado antes de escribir código, no asumido**: `shipping_details` de Stripe se movió a `collected_information.shipping_details` en la API "basil" (2025-03-31) -- esta función no fija `Stripe-Version`, así que no hay forma de saber cuál usa la cuenta sin probarlo en vivo. Se revisan los dos nombres de campo por compatibilidad.
  - **RLS real, agregado (no existía ninguna política antes)**: `merch_orders` tenía RLS activo pero CERO políticas -- ni el propio owner podía leerla desde un cliente, solo `service_role` vía el webhook. Migración `20260901170000` agrega SELECT+UPDATE para owner/admin/manager (`is_staff_management`); INSERT sigue siendo solo del webhook.
  - **Verificado con honestidad, no simulado a ciegas**: esta función valida la firma real de Stripe (`STRIPE_WEBHOOK_SECRET`, HMAC) -- no tengo forma de leer ese secreto, y no correspondía intentar sortear la verificación de firma en un webhook de pagos reales solo para probarlo. En vez de eso, se verificó el CONTRATO DE DATOS directamente contra el esquema real: un `insert`/`upsert` con el mismo shape exacto que produce el código nuevo (mismos campos, mismo JSON de items, mismo conflicto por `stripe_session_id`) se ejecutó con éxito, confirmando que el código es compatible con la tabla real -- fila de prueba borrada al cierre. **Pendiente real**: una compra de prueba real en Stripe test mode (como la que ya generó la fila del 28-ago) o `stripe trigger checkout.session.completed` con el Stripe CLI del PO, apuntando al webhook desplegado, para confirmar la firma y el flujo completo de punta a punta.
  - Pendiente de aprobación de commit: `supabase/functions/stripe-webhook/index.ts`, migración `20260901170000_merch_orders_rls.sql`, este documento. **✅ Aprobado y comiteado**: `eba1431` (`origin/fix/mobile-ui-cleanup`).
- [2026-09-01] **Banco de conocimiento musical y trayectoria de DjMago305 — cerrado.** El PO eligió esta opción de un menú de 4 sprints propuestos. Verificado antes de tocar nada: la identidad de DjMago SOLO existe separada de ELIXIS en `elixis-realtime-session/index.ts` (voz) -- `elixis-chat` (texto) es 100% ELIXIS, sin ninguna rama de DjMago. Se tocó únicamente el archivo de voz, tal como el propio ticket pedía si la separación ya existía.
  - **Conflicto real encontrado y resuelto antes de escribir el texto**: el ticket citaba "editor manager en Soundcaribe Record" -- esa es la marca VIEJA de la empresa, con una regla explícita de nunca usarla. Se le preguntó al PO si era historia personal real o un error de plantilla; confirmó que era error de plantilla. El texto final dice Miami DJ Beat LLC, no Soundcaribe Record.
  - Enriquecidos `cabecera` y `saber` de la persona `djmago` con la trayectoria real (30 años, DJ profesional/productor musical/ingeniero de sonido/productor de videoclips/editor manager), especialidad de eventos (bodas, quince, corporativos, privados), línea musical (clubes latinos e internacionales, Open Format avanzado, lectura de pista, transiciones armónicas, energía del público) y el servicio premium Live Show (tecnología + visuales + espectáculo en vivo). Instrucción explícita agregada para que el modelo lo viva como experiencia propia, no lo recite como currículum.
  - **Verificado hasta donde el entorno lo permite**: despliegue exitoso (Deno habría rechazado cualquier error de sintaxis del template literal), cambio de solo texto sin tocar ninguna lógica de VAD/herramientas/estado ya existente. **No verificado con honestidad**: el comportamiento real en una conversación de voz -- eso exige micrófono real, bloqueado en este entorno de navegador de prueba, igual que todos los cambios anteriores de este mismo archivo en la sesión.
  - Pendiente de aprobación de commit: `supabase/functions/elixis-realtime-session/index.ts`, este documento.
- [2026-09-01] **Barrido completo de "Soundcaribe" en todo el sistema — orden explícita y reforzada del PO ("no debe tener ninguna presencia ni efecto en este sistema").** No se asumió que el único caso era el del prompt de arriba -- se buscó en todo el código (`.ts`/`.js`/`.html`/`.sql`) y en las tablas reales más probables de la base. Se encontraron y corrigieron DOS rastros reales más, ninguno relacionado con el ticket original:
  1. **`web/staff.html`**: la nota del panel "Ficha de Lanzamiento" (modo distribución) decía "todavía no conectado a Soundcaribe Record" -- texto real que cualquier staff vería al abrir ese panel. Corregido a "Miami DJ Beat LLC".
  2. **`dj_profiles.bio` (dato real, no código)**: la bio PÚBLICA de DJMago305 -- la que ve cualquiera en su perfil -- decía "Hoy en día, DJMago305 se posiciona como la fuerza creativa detrás de SoundCaribe Record Corp..." presentándola como su rol ACTUAL, no como historia pasada. Por ser contenido real (no código), se le mostró el texto exacto al PO antes de tocarlo y se le preguntó cómo prefería la frase; eligió quitar la mención de empresa por completo. Editado directamente en la base de datos de producción vía SQL (no hay archivo de código que tocar para esto).
  - **Verificado con `grep` + SQL contra las tablas reales más probables** (`dj_profiles`, `client_profiles`, `leads`, `event_quotes`, `company_incident_log`) que no queda ningún otro rastro, en código ni en datos, en todo el sistema.
  - Pendiente de aprobación de commit: `web/staff.html` (además de lo ya listado arriba para `elixis-realtime-session/index.ts`). El cambio de `dj_profiles.bio` ya está aplicado en producción -- no requiere commit de código. **✅ Aprobado y comiteado**: `2e77611` (`origin/fix/mobile-ui-cleanup`).
- [2026-09-01] **Sprint "internacionalización, lote 1 (vistas públicas)" — cerrado, con alcance real distinto del pedido.** El ticket pedía 6 archivos; 2 no existen en el repo (`booking.html`, `about.html`, sin equivalente claro) -- quedan fuera hasta que se aclare a qué páginas reales se referían. Los 4 reales (`index.html`, `shop.html`, `contact.html`, `services.html`) YA tenían el sistema de i18n completo instalado (`translations.js` + `i18n.js`, con botón real y persistencia en `localStorage`) -- esto no era un sprint desde cero, contra lo que suponía el ticket.
  - **Metodología real, no supuesta**: se probó cambiando el idioma en vivo en las 4 páginas y buscando texto que se quedaba pegado en español mientras el resto cambiaba -- ese es el criterio de "hueco real" usado en todo este cierre, incluido un falso positivo descartado a tiempo (`rentals-lighting-title` en `services.html` ya tenía su clave correcta; el hallazgo inicial fue un artefacto de timing del propio escaneo, no un bug real).
  - **Hallazgos y arreglos reales**:
    1. `index.html`: sección "Tipos de DJ" (dentro de un bloque marcado `LOCKED SECTION: DO NOT MODIFY`) sin ningún `data-i18n` -- ~20 elementos. Se verificó primero que OTRAS secciones "locked" del mismo archivo (`areas`, `marketplace`) ya tenían `data-i18n` a pesar del candado, confirmando que el candado protege contenido/diseño visual, no el atributo invisible de traducción. 22 claves nuevas agregadas.
    2. El encabezado "Navegación" del menú móvil aparecía sin traducir en `shop.html` y `contact.html` -- vive en un componente COMPARTIDO (`mdjb-shared-header.js`, no en cada página). Autorización puntual del PO para tocar solo ese texto (no la estructura/orden del menú). **Bug real encontrado al verificar en vivo con un clic real, no solo en código**: el atributo `data-i18n` estaba bien puesto, pero `mdjConstruirPanelMovil()` nunca volvía a barrer el documento con `i18n.updateUI()` después de reconstruir el menú -- el título se quedaba pegado en español hasta que algo más disparara una traducción. Corregido agregando esa llamada al final de la función.
    3. `services.html`: el cotizador de servicios no es texto de esa página -- lo genera un componente compartido, `web/js/mdj-event-builder.js` (+ `mdj-event-builder-shell.js`), que no tenía NINGUNA traducción: 9 encabezados de columna, 4 etiquetas de estado, el estado vacío, los placeholders de fecha (Día/Año, en dos lugares distintos: la opción del `<select>` y un `<span>` de etiqueta visible aparte), el botón "Finalizar Selección", y los 9 nombres de categoría del carrito ("cajones"). 25 claves nuevas agregadas. Verificado bidireccional (ES→EN→ES) en vivo, no solo el estado por defecto.
  - **Dejado explícitamente fuera de alcance, anotado para no perderlo**: dentro del mismo cajón de `mdj-event-builder-shell.js` quedan sin traducir "Mes" (paralelo a Día/Año, mismo mecanismo), "Asignar a evento"/"Asignar", y los 12 nombres de mes abreviados (Ene/Feb/Mar...) -- se encontraron al arreglar Día/Año pero extenderse a todos habría sido abrir un cajón bastante más grande sin una autorización específica para esa pieza. Sin tocar.
  - **Archivo real excluido del commit, encontrado por accidente**: `web/shop.html` tiene un bloque real de GA4/Search Console (`G-7XRWGTCQ5L`) sin comitear de una sesión ANTERIOR (el pendiente "GA4 en shop.html" ya anotado más arriba) -- no se tocó hoy, se identificó y se dejó fuera del commit de este sprint para no mezclarlo.
  - Pendiente de aprobación de commit: `web/index.html`, `web/services.html`, `web/mdjb-shared-header.js`, `web/translations.js`, `web/js/mdj-event-builder.js`, `web/js/mdj-event-builder-shell.js`, este documento. `web/contact.html` no requiere commit (su único hueco se resolvió en el archivo compartido). `web/shop.html` excluido a propósito (ver arriba).

## TICKET ABIERTO — Rediseño del motor de voz (eco acústico / auto-conversación de DJMago)
- [ ] **[2026-09-02] Reescritura del control de turnos de `elixis-voice-session.js` / `elixis-realtime-session/index.ts` — agendado por el PO, NO implementado todavía.**
  - **Diagnóstico real de esta sesión (2026-09-01/02)**: DJMago "habla solo" y se corta a media frase en cuartos silenciosos (confirmado que NO pasa con ruido real de fondo, ej. un carro) -- root cause es eco acústico genuino: bocinas abiertas + micrófono abierto SIN cancelación de eco (`echoCancellation/noiseSuppression/autoGainControl` en `false` a propósito, porque activarlas dispara una reconfiguración de CoreAudio que corta el audio de Serato DJ en vivo). Tres intentos de esta sesión (subir `DJMAGO_VAD_THRESHOLD` a todos los modos, `create_response:false` + disparo manual del cliente, restaurar `transcription`) cada uno arregló un síntoma y expuso otro -- confirmado con logs reales de consola en cada vuelta, no adivinado. El PO ordenó explícitamente "no tocar más esto sin sesión dedicada" tras la tercera regresión (ver comentario `PROBLEMA CONOCIDO, SIN RESOLVER` en `elixis-realtime-session/index.ts`, línea ~1050).
  - **Investigación externa real (2026-09-02, el propio PO consultó a OpenAI/ChatGPT y trajo la respuesta completa)**: confirma que NO existe un parámetro de `turn_detection` que funcione como AEC real (usando como referencia el audio que el propio modelo reproduce) -- `server_vad`/`semantic_vad` detectan actividad de voz, no pueden restar matemáticamente el eco de las bocinas. `noise_reduction: { type: "far_field" }` (ya activo en el código, ver `NOISE_REDUCTION` en `elixis-realtime-session/index.ts`) ayuda pero NO es AEC y no resuelve el problema estructural.
  - **Arquitectura recomendada, oficialmente soportada por OpenAI para control manual de turnos**:
    1. `turn_detection: null` (apagar el VAD del servidor por completo) + `input_audio_buffer.commit` + `response.create` manuales, disparados por la propia app.
    2. Detectar "el usuario habló" con una de tres opciones: push-to-talk (más robusto), VAD local ligero en el cliente (solo activo en estado `LISTENING`), o un híbrido de ambos.
    3. Gate del micrófono con `RTCRtpSender.replaceTrack(null)` / `replaceTrack(micTrack)` en vez de `track.enabled=false` -- no destruye el `MediaStreamTrack`, no reabre `getUserMedia()`, no toca CoreAudio.
    4. Señal real de "el asistente terminó de sonar" es `output_audio_buffer.stopped` (drenaje real del buffer WebRTC) -- NUNCA `response.done` (solo dice que el TEXTO terminó de generarse, puede seguir sonando audio ya generado) ni la heurística de esta sesión (`largo del texto × ms/caracter`), que fue exactamente la causa de que el margen de reactivación nunca cerrara bien la ventana de eco.
    5. `conversation.item.truncated` / `output_audio_buffer.cleared` NO son señales anti-eco -- son para manejar interrupciones reales del usuario (barge-in), no para decidir cuándo reabrir el micrófono.
    6. Máquina de estados propuesta: `LISTENING → USER_SPEAKING → (commit + response.create) → ASSISTANT_SPEAKING → (output_audio_buffer.stopped) → ACOUSTIC_GUARD (limpiar buffer, esperar 250-800ms calibrados a la sala real, limpiar buffer otra vez) → LISTENING`.
  - **Alcance real de la reescritura**: toca el corazón de `elixis-voice-session.js` (nueva máquina de estados, nuevo mecanismo de gate del mic, nuevos listeners de eventos) y el `turn_detection` de `elixis-realtime-session/index.ts` (pasar a `null` para identidad djmago, posiblemente para los 6 modos). No es un ajuste de parámetros -- es un cambio de arquitectura, por eso el PO decidió agendarlo como su propio ticket en vez de improvisarlo esta noche.
  - **Pendiente antes de empezar**: decidir con el PO cuál de las 3 opciones de detección de turno (push-to-talk / VAD local / híbrido) prefiere para el flujo real de DJ en cabina, y calibrar el `ACOUSTIC_GUARD_MS` contra el sistema de bocinas real (no hay número oficial de OpenAI, hay que medirlo).
  - **[2026-09-02, actualización] Versión chica ya implementada y en PRODUCCIÓN por otro hilo (`Elixis Voice Agent Blueprint`), commit `22bc45c` -- NO es la reescritura completa de arriba, es un arreglo más acotado que el PO aprobó primero.** Verificado de forma independiente por este hilo (Hilo Maestro) antes de darlo por bueno, no solo relayado:
    1. **Reembolso de voz reventaba de verdad**: `ADMIN.rpc(...).catch(() => {})` -- el builder de supabase-js es *thenable*, no Promise, no tiene `.catch`; reventaba en ejecución justo en la vía de reembolso (cuando OpenAI rechaza la sesión), matando el handler sin responder y sin soltar el bloqueo de 900s. Reemplazado por un helper `devolverReserva()` con try/catch real. Medido en producción antes del fix: 25 sesiones colgadas en `open`.
    2. **El mic ya no adivina cuándo calló la bocina**: se reabre en `output_audio_buffer.stopped` (evento real de "el buffer de audio de salida se drenó") + una guarda acústica, en vez de la estimación de esta sesión (`largo del texto × 35ms/caracter`) -- esa estimación era exactamente el hueco que producía el "hablando solo".
    3. **SQL de limpieza corrido por este hilo contra PRODUCCIÓN** (3 pasos: mirar → `elixis_voice_settle(id, 0)` → comprobar): 25 sesiones liberadas, `open` en 0, `flagship_used_seconds` bajó a 16.155 -- verificado con aritmética exacta (38.655 − 25×900 = 16.155, todas tier flagship, cuenta `is_unlimited`) contra el otro hilo, que había predicho ~13.200 partiendo de una base ya vieja para cuando se corrió; no fue una falla real, solo una referencia desactualizada.
    4. **Desplegado**: `elixis-realtime-session` v56 (02-sep, ~05:00 UTC), por este hilo.
  - **NO declarar cerrado**: la guarda acústica arranca en 400ms sin calibrar de oído (ajustable sin redesplegar vía `localStorage.setItem('elixis_guarda_ms', '250'|'400'|'600'|'800')`) -- pendiente que el PO la pruebe con sus bocinas reales. Además, recargar crédito de OpenAI: hoy responde 429 `credit_balance_exhausted`, ninguna sesión de voz abre.
  - **Colisión evitada, no producida**: `web/staff.html` trae un cache-bust nuevo (`?v=20260902-guarda-acustica`) de ese commit, sin comitear a propósito porque el archivo tenía trabajo vivo de este hilo (ajuste de encuadre del avatar a 16:9). Quien comitee `staff.html` de aquí en adelante debe incluir esa línea.
  - **[2026-09-02, segunda vuelta] Candado por aritmética, no por parecido de texto.** El mismo hilo (`Elixis Voice Agent Blueprint`) rastreó los 4 caminos reales por los que el navegador puede disparar `response.create` para djmago (herramienta, Cazador, texto escrito, modos de oficina) y encontró que el de "modos de oficina" respondía a CUALQUIER transcripción, sin filtro real -- el filtro anti-eco existente comparaba texto dentro de una ventana de 3s que se sellaba en `response.done`, pero la bocina seguía sonando más de 4.4s después (medido en logs reales), así que el eco llegaba fuera de la ventana y pasaba limpio. Arreglo: se registra el instante exacto en que se arma el micrófono: si un turno nace con la bocina todavía sonando o el mic sin armar, se descarta ANTES de mirar el texto (aritmética de tiempos, no similitud). Ventana del filtro de texto subida de 3s a 12s como segunda capa. **Sin comitear todavía** (en disco, pendiente de que el PO lo pruebe). Log de consola a vigilar: `turno nacido con la bocina sonando o con el mic sin armar -- marcado como eco`; si el auto-habla persiste SIN que aparezca esa línea, es un quinto camino no mapeado.
  - **Hueco conocido, aprobado para cerrar**: el candado de arriba solo protege identidad `djmago` (`create_response:false`). La identidad `elixis` sigue con `create_response:true` (OpenAI responde solo) -- el mismo eco podría crear un turno ahí sin que el navegador lo pueda frenar. El Hilo Maestro recomendó cerrarlo también (el eco es un problema físico, no de identidad, y `djmago` ya es la identidad por defecto en los 6 modos) -- pendiente de implementación y despliegue coordinado.
  - **[2026-09-02, hallazgo real de una tercera sesión] `web/mdj-commander.html` tiene su PROPIO motor de voz standalone (objeto `RT`, ~línea 2500) que NO usa `elixis-voice-session.js`.** Ninguno de los arreglos de arriba (guarda acústica, candado por aritmética) lo alcanza -- es codigo duplicado e independiente. El "hipo"/frases cortadas que el PO reportaba en ese archivo específico es la MISMA clase de bug, en un módulo aparte. El PO pausó el trabajo de eco/interrupción en `elixis-voice-session.js`/`elixis-realtime-session.js` (ya con dueño asignado arriba) y asignó a otra sesión portar la misma idea a `mdj-commander.html`, sin tocar los archivos ya en curso -- confirmado sin colisión.

## TICKET FUTURO — ELIXIS lee y filtra correo entrante (propio, no vía Make/n8n/Zapier)
- [ ] **[2026-09-02] Visión registrada por el PO**: quiere que ELIXIS (el propio sistema, no una plataforma de automatización de terceros) lea, filtre y proponga respuestas a los correos que llegan a `djmago305@gmail.com` / el corporativo de Miami DJ Beat LLC. Encaja directo con la visión ya registrada de "ELIXIS empleado de oficina real" -- es la mitad de LECTURA que falta, ya que hoy (`elixis-email-dispatch`, 2026-09-01) solo existe la mitad de ENVÍO.
  - **Requiere real, no bosquejado todavía**: conexión OAuth a la API de Gmail (no un tercero), una función que reciba notificaciones de correo nuevo (push/watch de Gmail o polling), y -- esto es lo importante -- el MISMO patrón de aprobación humana que ya existe para SMS/email saliente (`enviar_sms`/`enviar_email`: ELIXIS solo encola/propone, un humano aprueba antes de que salga). Leer es más seguro que responder solo; contestar sin revisión humana es donde hay que tener cuidado real.
  - **Decisión explícita del PO**: no abrir esto ahora ("mucho trabajo aquí, debemos trabajar seguros y confiados sin errores para poder avanzar") -- queda anotado para retomar en su propia sesión dedicada, no mezclado con los tickets de voz/avatar/email ya abiertos.

## [2026-09-02] Investigación cerrada: queja de "-34" (Elixis Voice Agent Blueprint) sobre interferencia de otro agente
- **Orden del PO**: "el agente especialista encargado de elixis y djmago se está quejando de que otro agente tocó su proyecto mientras estaba trabajando y eso causó problemas para él, tú tienes la jerarquía, investiga qué pasó." Investigado por el Hilo Maestro (esta sesión) contactando directamente a -34 y a -93 vía `SendMessage`, no solo inspeccionando archivos.
- **Veredicto: NO hubo corrupción ni pisado de trabajo entre sesiones.** -34 mismo aclaró que nunca reportó que otro agente le corrompiera código -- su propia auditoría de marcadores (`audioSender`, `vigilarBargein`, `turnoSospechoso`, `interrumpir`, `replaceTrack`) coincide con la mía: intacto.
- **Lo que sí encontró -34**: un bloque "sin dueño conocido" en `web/js/elixis-voice-session.js` (console.warn `"transcripcion ignorada -- microfono deshabilitado"`, guardia sobre `pistaMic.enabled`) que -93 tampoco reconocía como propio. **Resuelto por este hilo con `git log -S"transcripcion ignorada"`**: es código del commit `e5e973a` (31-ago 20:08, "hard-lock mic VAD cooldown"), ya en `origin/fix/mobile-ui-cleanup` desde antes de que la sesión de -34 existiera hoy -- no es un misterio ni una inserción de una supuesta sesión "-12", es historial viejo que -34 no había rastreado con `git blame`/`git log -S` antes de reportarlo.
- **Causa real del síntoma ("se quedó callado" con el PO)**: bug propio de -34, ya admitido y corregido -- una línea vieja seguía forzando `pistaMic.enabled=false` a mano en paralelo al nuevo transporte por `RTCRtpSender.replaceTrack()`, dejando el track en el estado exacto que el candado de agosto (arriba) interpreta como "deshabilitado" y descarta sin mirar el contenido. Con el mic así, el detector de barge-in de -34 también quedaba sordo (por eso el PO tuvo que alzar la voz). No fue otro agente.
- **Despliegues de `elixis-realtime-session` de -34 esta noche**: dos, ambos autorizados por el PO directamente en su chat (v56 reembolso de voz; luego `transcription.language:"es"`). Sin evidencia de que otra sesión haya desplegado encima.
- **El relay de "pausa" de -93**: -93 recibió del PO un pedido directo ("avisale por interno que nadie toque nada, tú eres el especialista ahora") sobre el dominio de eco/interrupción que -93 trabaja en paralelo para `mdj-commander.html`, y lo reenvió tal cual a los peers visibles, siendo explícito de que era un relay. -34 hizo lo correcto: no lo acató por relay, se lo confirmó al propio PO antes de actuar, y el PO respondió "sigue". Protocolo de dos sesiones funcionando como debe -- ningún peer debe frenar solo por un aviso de segunda mano sin verificarlo con el PO.
- **Nota aparte, sin relación con la queja**: -93 mandó un mensaje con un glitch de generación (texto repetido sin sentido) durante esta investigación; se autocorrigió al notarlo. No tenía contenido instructivo, ignorado sin acción.
- **Aviso operativo de -34 para cualquier hilo que coordine esta noche**: todo el trabajo de voz de las últimas horas (transporte por `replaceTrack`, barge-in por nivel, candado por aritmética de tiempo, interrupción por barra espaciadora, cache-bust de `staff.html`) sigue **sin comitear** en el working tree del PO, probado en vivo pero no en ningún commit. Nadie debe hacer `checkout`/`stash`/`clean` sobre `fix/mobile-ui-cleanup` sin coordinar con -34 primero -- ahí sí se perdería de verdad.

---

## 🔊 MOTOR DE VOZ ELIXIS/DJMago — 2026-09-02 (Dominio: Realtime/Audio)

**Estado: desplegado en producción y aprobado por el PO. PR abierto: #294.**
Rama `fix/elixis-motor-voz-anti-eco`, creada desde `origin/main` limpio con
cherry-pick — NO desde `fix/mobile-ui-cleanup`, que arrastra 41 ficheros de
otras sesiones. Alcance del PR: 4 ficheros, y de `web/staff.html` solo el `?v=`.

### Lo que se arregló (cada punto salió de un síntoma real reportado hablando)
| # | Síntoma del PO | Causa |
|---|---|---|
| 1 | "arranca a contar una historia solo" | el mic se reabría por un cálculo (35ms/carácter) en vez de esperar `output_audio_buffer.stopped` |
| 2 | "se quedó callado en la segunda pregunta" | una línea vieja muteaba con `.enabled=false` saltándose el transporte nuevo; un candado de agosto (e5e973a) leía eso como "mic deshabilitado" y descartaba sus preguntas |
| 3 | "intenté interrumpirlo y no me escuchó" | half-duplex: sin AEC no se puede tener las dos cosas. Barge-in por nivel + barra espaciadora |
| 4 | una notificación de macOS cortó la conversación | el nivel solo no distingue un "ding" de una voz → + duración + timbre |
| 5 | "volvió a saludar tras despedirse" | Whisper **inventa** ante silencio ("Gracias.") y eso disparaba turno |
| 6 | "me da artistas, no canciones" | `mdj-music` + `consultar_musica` existían… solo en el cerebro de TEXTO |
| 7 | "déjame un PDF" | no tenía herramienta de entrega → `entregar_pdf` con jsPDF en el navegador |
| 8 | "me llama Yerardo" | sí puede corregirse, pero no recuerda entre llamadas → vive en el prompt |
| 9 | respondía en inglés | `transcription` sin `language` |
| 10 | (no visible) reservas de voz sin liberar | `ADMIN.rpc(...).catch()` no existe: builder thenable ≠ Promise. **25 sesiones, 22.500s retenidos** |
| 11 | "Conversation already has an active response" | dos disparadores de turno solapados → una sola puerta, `pedirRespuesta()` |
| 12 | herramienta duplicada en el panel | doble `emit('onTool')` |

### Hechos técnicos que costaron horas (no repetir el descubrimiento)
- **`output_audio_buffer.stopped`** es el único evento fiable de "la bocina calló".
  `response.done` = terminó de *generarse*, no de *sonar*.
- **Safari NO mide el stream remoto de WebRTC** (`mic 0.110 vs bocina 0.000`).
  Cualquier comparación relativa mic-vs-bocina es inerte ahí.
- Cortar el ENVÍO con `replaceTrack(null)`, nunca `.enabled=false`: mutear deja
  sordo al analizador local y sin él no hay barge-in.
- No llamar `getUserMedia` de nuevo: reconfigura CoreAudio y **corta a Serato en vivo**.

### PENDIENTE — lo único que queda
El umbral del barge-in está en valores de arranque, **sin calibrar con el oído
del PO**. Todo se mueve en vivo por `localStorage`, sin redesplegar:
`elixis_guarda_ms` · `elixis_bargein_piso_solo` · `_margen` · `_cuadros` ·
`elixis_bargein=off` · `elixis_bargein_debug=1` (imprime el pico real de su voz).

### Limpieza hecha
Retirados el worktree y la rama `feat/elixis-cazador-silencioso` — trabajo
duplicado del arranque de la sesión, superado por `fe9a248` y nunca fusionado.
Ficheros de prueba del scratchpad, borrados.

## [2026-09-02] Avatar DJMago: borde blanco corregido DE RAÍZ (no parche) + timing de encuadre + Cazador reposicionado
- **Aprobado por el PO explícitamente**: "recortalo desde la fuente raiz... si tu tienes las herramientas para hacer ese recorte... hazlo... tienes la aprobación full para este ticket sin tocar voz ni sistema de contestación".
- **`djmago-idle-boomerang.mp4` REEMPLAZADO en Supabase Storage (mismo path, `assets/DJMago IA/`)**: medido con ffprobe+píxeles reales el borde blanco horneado (8.33% izq / 8.02% der / 6px abajo, consistente en 2 frames distintos), recortado de verdad con ffmpeg (`crop=1606:1074:160:0`) y rellenado de vuelta a 1920x1080 con el MISMO verde de croma real de la escena (RGB~14,250,5, muestreado del propio video) en vez del blanco viejo — mantiene 16:9 real, coincide con el estilo limpio de `elixis_cazador.mp4`. Verificado antes de subir: 0% de borde en los 3 lados tras el recorte, costura recorte/relleno invisible (diff de color <10 en compresión), loop bumerang intacto (frame 0 == frame final, diff medio 0.8/255 solo por ruido de recompresión). **Original conservado localmente antes de borrar el de producción** (reversible si hace falta).
- **Código limpiado en `web/staff.html`**: quitados `fraccionAncho:0.82` y `recorteInferiorPx:10` (existían solo para tapar el borde que ya no existe) en los 2 sitios (`ewOpcionesChroma` base + rama "oficina" de `ewAplicarEncuadrePorModo`) — ahora `fraccionAncho:undefined` en ambos. Verificado con matemática real antes de aplicar (no a ciegas): con la caja `.avatar-stage` en 1606/1080 y el video ya limpio en 1920x1080, "cover" sin fraccionAncho da una fracción visible de ~0.8365 — prácticamente idéntica al 0.82 de antes, mismo encuadre visual sin parche. Confirmado en vivo: 0.14% de píxeles blancos remanentes en el canvas (84 de 60538), todos concentrados en el centro (x=177-297 de 480) — brillos reales de dientes/audífonos, no borde (un borde real saldría en los extremos x≈0 o x≈480).
- **Bug real encontrado y corregido, en Cazador Musical**: el pedido de "muévelo más a la izquierda" nunca se aplicaba — `desplazamientoHorizontalPct` chocaba contra un límite matemático real de `dibujarCover()` (con `fraccionAncho` sin definir, solo había 95px de sobra para mover, menos de lo que pedía cualquier `pct` razonable, así que el clamp lo topaba siempre en el mismo punto). Corregido dándole a Cazador su propio `fraccionAncho:0.75` (más zoom horizontal, 160px de sobra real) + `desplazamientoHorizontalPct:-0.12` — verificado contra el frame real del video que los dos brazos siguen completos en cuadro.
- **Bug real encontrado y corregido: la barra blanca "al cambiar a Cazador" que reportó el PO con video**. Causa real, confirmada leyendo el código (no a ciegas): `ewSeleccionarModo()` llamaba `ewAplicarClipsPorModo()` (arranca el cruce de opacidad entre los 2 `<video>`, duración variable según red — NO los 200ms fijos de la transición CSS) y `ewAplicarEncuadrePorModo()` (cambia `fraccionAncho`/`pct` al instante) en la misma línea, sin esperar — durante la ventana de cruce, el video VIEJO (idle, con el borde real de antes) seguía siendo el más visible pero ya se dibujaba con los parámetros NUEVOS (los de Cazador, menos agresivos), destapando una tira real del borde. Un primer intento con `setTimeout(220)` fijo falló en la prueba real (el video tardó ~600ms en cargar, más que el retraso) — corregido con un poll real cada 33ms sobre la opacidad computada de las 2 capas (mismo criterio que ya usa `elegirFuente()` en `avatar-heygen-chromakey.js`), con tope de 3s por si el video nunca carga. Verificado con hook directo de `drawImage()` capturando el momento exacto del cruce.
- **Pendiente, NO iniciado, requiere dirección del PO antes de tocar nada**: el PO sugirió usar `Face Avatar DJMago305/DJMago305 White.mp4` (en su Desktop) para sacar "algunas escenas" y sumarlas al idle para que no sea repetitivo. Revisado (solo inspección, sin editar): es un video de 1920x1080/25fps/~21s, MUY similar en pose al idle actual (audífonos al cuello, misma composición), y **tiene un fleco verde real alrededor del pelo/hombros** (spill de un croma verde original, ya compuesto sobre blanco por otra herramienta) — no es un fondo blanco limpio, necesitaría su propio trabajo de despill/rotoscopia antes de poder integrarse, distinto del chroma key simple que ya usa el resto del sistema. No se tocó nada de este archivo. El PO también mencionó que está preparando OTRO video nuevo con movimiento de boca/parpadeo para editar en varias partes — sin archivo todavía, sin acción posible hasta que exista.
- **Bug real encontrado y corregido: el croma se colaba en el audífono derecho de Cazador Musical** (reporte del PO con video). Causa real, medida no adivinada: el aro exterior brilloso del audífono refleja el verde del estudio con una saturación/brillo casi idéntica al fondo real (h~120°, s~0.80-0.85 vs el fondo ~0.98) — la MISMA situación ya documentada para la oreja del idle ("subir umbrales no sirve, el pixel es tan verde como el fondo real"). Reproduje el algoritmo HSV real del código sobre 65 cuadros repartidos en todo el clip (323 cuadros completos), mapeados al espacio del canvas con la fórmula real de `dibujarCover()`, y detecté los agujeros reales por componentes conexas (flood fill que no toca el borde). **Primer intento descartado en la misma sesión**: una elipse centrada en el bounding box de esos agujeros — el agujero real resultó ser una medialuna angosta, no un blob compacto, y esa elipse también protegía un hueco de fondo real y abierto en el medio de la medialuna (verificado: un pixel de canvas con 0% de frecuencia de ser agujero en los 65 cuadros, verde puro en el video crudo) — convertía fondo real en una mancha oscura opaca nueva, un regreso, no una mejora. Corregido con centroide+desviación real ponderados por frecuencia (no el punto medio de una caja) en vez de una elipse a ciegas. Verificado dos veces: (1) resimulando el algoritmo COMPLETO (esFondo+zonaProtegida+suprimirSpill) sobre 3 cuadros reales incluyendo el peor caso, (2) **en vivo, con el código real corriendo en el navegador** (no solo la simulación) — el audífono cierra completo, sin hueco, sin mancha nueva arriba. `web/staff.html`, rama `fix/mobile-ui-cleanup`, sin comitear — pendiente de que el PO lo confirme visualmente en su propia sesión con voz real antes de comitear (Regla 7).
  - **Verificación reforzada, a pedido explícito del PO ("pon más interés en ese recorte")**: no me quedé con la muestra de 3 cuadros. Extraje los 323 cuadros REALES del clip completo y medí, para cada uno, (a) cuántos píxeles de agujero quedan dentro de la zona del audífono y (b) cuántos píxeles de fondo real abierto quedan mal protegidos por la elipse (posible mancha nueva). Encontré una franja de cuadros con reflejo mucho más severo que el resto (cuadros ~36-41 y su espejo ~280-287, el mismo tramo del bumerang en reversa) donde la métrica automática marcaba números altos en ambos sentidos — los revisé UNO POR UNO renderizando el algoritmo completo, no solo el número: en ambos casos (el peor por agujero remanente y el peor por posible mancha) el resultado visual es limpio, sin hueco ni mancha — los números altos eran ruido de la propia métrica (huecos que en esos cuadros se funden con el fondo real y confunden la detección de "encerrado vs. abierto"), no un defecto visible real. Después arme una hoja de contacto con LOS 323 cuadros procesados por el algoritmo real y los revisé todos, no una muestra — limpios en el clip completo.
  - **Respuesta a la sugerencia técnica del PO** ("aplica el croma en un aura más pequeña del DJ, técnica de Hollywood, así no se cuela ni en metal ni en vidrio"): es la idea correcta y es EXACTAMENTE lo que `zonaProtegida` ya hace — una máscara espacial (garbage matte) donde el color no decide, sólo protege. La diferencia es el tamaño: medí el silueta real del sujeto en los 323 cuadros (mayor componente conexo que no es fondo) y los brazos, tal como el propio PO pidió ("no importa que el brazo sobrepase el límite, es a propósito"), tocan los 4 bordes del cuadro en buena parte del clip — un aura de "todo el DJ" cubriría casi el 100% del canvas y apagaría el fondo transparente casi por completo, un problema mayor que el que se quiere resolver. Por eso la zona protegida queda acotada al punto real del reflejo (el aro del audífono), no al cuerpo entero — mismo principio, alcance quirúrgico en vez de global.
  - **⚠️ TICKET ABIERTO, SIN CERRAR — el PO confirmó en vivo que el escape de croma sigue viéndose en Cazador Musical, aunque no en foto fija.** Amplié el margen de la elipse (1.8→2.2 sigma, el máximo real posible sin empezar a proteger un punto de canvas confirmado 100% fondo abierto en los 323 cuadros) — mejora real, pero el propio PO ya probó y el escape sigue apareciendo **en movimiento real**, no en una captura estática. No pude reproducirlo procesando los 323 cuadros del archivo descargado (todos limpios, incluido el peor caso) — la causa real puede estar en algo que la extracción de cuadros estáticos no captura: timing de reproducción real en el navegador, un cuadro intermedio entre los 25fps muestreados, o algo específico del decodificador de video en vivo que mi simulación en Python no replica. **Decisión explícita del PO**: dejarlo anotado como trabajo sin terminar y pasar al siguiente paso — el PO va a retomarlo él mismo más adelante a través del hilo especialista (voz/DJMago), no en este hilo. **Para quien lo retome**: todo el trabajo de diagnóstico de esta sesión queda arriba (medición HSV real, mapa de calor de 323 cuadros, la lección del primer intento fallido con una elipse a ciegas) — el paso que falta es conseguir una grabación de pantalla del escape real en movimiento (el mismo método que resolvió la barra blanca) para extraer el cuadro exacto donde pasa, en vez de seguir agrandando la elipse sin saber si alcanza. Valores actuales en `web/staff.html`, rama `fix/mobile-ui-cleanup`: `zonaProtegida = { cx:0.7127, cy:0.4963, rx:0.0747, ry:0.2568 }` dentro de la rama Cazador de `ewAplicarEncuadrePorModo()`.

## [2026-09-02] Nav: "Agenda" al primer clic aterrizaba en Mi Perfil — corregido en el riel compartido, ticket relacionado abierto en Staff
- **Reporte del PO**: "la pestaña agenda al aplastarla por primera vez me está llevando a mi perfil, después me vuelve a llevar a agenda... es un problema que hemos corregido demasiadas veces y no debe pasar esto." Investigación delegada a un agente (`Explore`), con hallazgos verificados contra el código real antes de tocar nada.
- **Causa raíz real, confirmada**: `web/mdjb-shared-header.js` construye el puesto "Agenda" (y "Cash Flow") con el patrón VIEJO ya identificado como frágil por el propio código en `FIX-NAV-CONFIG-01`/`FIX-NAV-DUP-MIPERFIL-01` (2026-08-19): el nodo se crea con un `href` de respaldo ANTES de que la sesión/rol resuelvan (`mdjEnsureAgendaMainNavNode`/`mdjEnsureFlowMainNavNode`), y se corrige despues via `mdjApplyAgendaMainNavLink`/`mdjApplyFlowMainNavLink` una vez la cadena de auth ya resolvió — si el clic real cae en esa ventana, viaja con el href viejo. MI PERFIL y CONFIG ya tenían la cura correcta (resolver el destino EN VIVO, dentro del propio manejador de clic, ver `mdjEngancharMiPerfil`/`mdjEngancharConfig`) desde agosto — Agenda y Cash Flow nunca la recibieron, a pesar de que el propio comentario de `mdjEngancharConfig` ya advertía textualmente: *"el día que [account-settings.html] salga de la lista, CONFIG tira al artista a la barra pública sin camino de vuelta, como ya pasaba con Agenda y Cash Flow."*
- **Corregido**: agregados `mdjDestinoAgendaEnVivo()` + `mdjEngancharAgenda()` y su equivalente para Cash Flow (`mdjEngancharAgendaOFlow(el, tab)`, una sola función parametrizada para no duplicar código), mismo patrón exacto que Mi Perfil/Config — `preventDefault()`, resolver rol/sesión en vivo con `supa.auth.getSession()`, navegar recién con el destino confirmado. Enganchados en el momento de creación del nodo (`mdjEnsureAgendaMainNavNode`/`mdjEnsureFlowMainNavNode`), igual que `mdjEngancharConfig` se engancha en `mdjEnsureConfigMainNavNode`. Verificado: `node --check` limpio.
- **Alcance real de este fix, verificado por lectura de código — NO es todo el sitio**: `mdjApplyAgendaMainNavLink`/`mdjApplyFlowMainNavLink` se SALTAN por completo (return temprano) en las páginas listadas en `MDJ_VISTAS_INTERNAS`/`MDJ_VISTAS_ARTISTA` (dj-tools.html, academia.html, dj-dashboard.html, account-settings.html, etc.) — ahí Agenda/Cash Flow los pinta una tabla estática distinta (`mdjTablaDeSlots()`/`MDJ_NAV_SLOTS_INTERNO`) con href fijo por rol, sin la carrera de este bug. Este fix aplica al "riel suelto" — páginas públicas donde un artista/staff con sesión ve Agenda igual (ej. index.html y similares) sin estar "en estación". Confirmado en vivo (sin sesión real disponible en este entorno de prueba) que el nodo simplemente no se crea para un invitado — consistente con el diseño, pero significa que el enganche del clic **no se pudo probar con una sesión real todavía**. Pendiente que el PO lo confirme con su propia sesión.
- **Investigado y DESCARTADO como causa, con evidencia**: el botón "Agenda" propio de `staff.html` (`data-top="agenda"` dentro de `#staff-topnav`, pintado por `paintStaffTopnav(staffTopnavHtml('owner'), showView)`) es un interruptor de vista INTERNO (`showView('agenda')`), no una navegación de página — y su código es síncrono, sin ningún `await`/`setTimeout` entre el pintado de los botones y el enganche de sus clics para el rol owner. No se encontró ahí el mecanismo del bug reportado; se descarta como esa causa concreta (queda documentado para no volver a auditarlo de cero si reaparece).
- **⚠️ Hallazgo relacionado del mismo agente, NO corregido todavía (fuera de alcance de esta pasada, mismo patrón de bug pero en el puesto STAFF, no Agenda)**: `mdjBindStaffNavClickGuard()`/`mdjInstallGlobalStaffNavCapture()` leen `window.__mdjLastPlatformIdentity` de forma SÍNCRONA en el clic en vez de esperar una verificación en vivo — si un owner/staff legítimo hace clic antes de que esa variable global resuelva, lo manda a `login.html` en vez de a `staff.html`/`admin-dashboard.html`. Mismo síntoma de fondo (dato asíncrono leído antes de tiempo), destino equivocado distinto (login, no Mi Perfil). No corregido: requiere confirmar primero cuál es la señal correcta a esperar (no hay un `mdjDestinoStaffEnVivo()` ya escrito para reusar, a diferencia de Agenda/Flow/Config/Mi Perfil) antes de tocar el candado de seguridad de esa pestaña.
- **⚠️ Segundo hallazgo relacionado, NO corregido**: existe una SEGUNDA implementación independiente de la pestaña Agenda en `web/mdj-profile-nav-context.js` (usada en `dj-profile.html`, `jobs.html`, `shop.html` con `?mdj_nav=profile`), con su propia señal asíncrona distinta (`window.__mdjLastPlatformIdentity.managementInDb` vs. `sb.auth.getSession()`) compitiendo por escribir el mismo `href` que luego `reorderOwnerStrip()` en `mdjb-shared-header.js` vuelve a pisar sin mirar el rol. Dos scripts corrigiendo el mismo enlace por separado — no reconciliado en esta pasada.
- Pendiente de aprobación de commit: `web/mdjb-shared-header.js`, este documento.

## [2026-09-02] ⚖️ CAMBIO IMPORTANTE DE ARQUITECTURA: MRM IA sale de la barra compartida, vive solo en Inicio
- **Orden directa y explícita del PO**: "sácala y déjala solo en menú de inicio... borra todo lo que tenga que ver con la posición vieja para evitar errores... actualiza el libro y las hojas de trabajo... porque es un cambio muy importante." Contexto de la decisión: MRM IA es un manual/tour filtrado por rol (el propio destino, `road-map.html`, decide el alcance según quien entra — owner ve el recorrido completo, artista su modelo de pago, cliente cómo contratar, etc.), todavía en desarrollo. El PO concluyó que ese tipo de contenido no pertenece a la barra de navegación compartida de TODAS las páginas del sitio, sino que debe vivir únicamente en Inicio.
- **Revierte una decisión previa documentada**: MRM IA había entrado a `MDJ_NAV_SLOTS` (barra pública, slot 9) el 2026-08-16 explícitamente como *"visible para TODOS los roles, incluido invitado... no es una herramienta de owner"*, y se sumó también a `MDJ_NAV_SLOTS_INTERNO` (slot 10) el 2026-08-30. Ambas decisiones quedan revertidas hoy — se documenta aquí para que ningún hilo futuro las reintroduzca sin saber que hubo un cambio de rumbo deliberado.
- **Alcance real de la limpieza, verificado antes de tocar nada (no a ciegas)**: se buscó `mainNav-roadmap-link` / `data-mdj-nav="roadmap"` / `staff-mrm-link` / `mdj-roadmap-mainnav` en TODO `web/*.html` — 49 archivos con el enlace viejo. 48 tenían la MISMA línea exacta (confirmado carácter por carácter con un script, no con `sed` a ciegas), retirada con un script Python que verificó una sola coincidencia por archivo antes de borrar — cero riesgo de romper HTML de otras secciones. El único con markup distinto, `web/staff.html` (`staff-mrm-link` dentro de `staffTopnavHtml()`, rol owner), se editó a mano.
- **`web/mdjb-shared-header.js`, limpieza completa de las 5 superficies donde vivía MRM IA**:
  1. `MDJ_NAV_SLOTS` (barra pública) — slot 9 retirado, la tabla vuelve a los 8 slots canónicos que ya describía su propio comentario original de 2026-08-16 (antes de que este slot se agregara).
  2. `MDJ_NAV_SLOTS_INTERNO` (barra owner/staff) — slot 10 retirado, vuelve a 9 puestos: Inicio, Academia, Agenda, Config, DJ Tools, Cash Flow, MI PERFIL, Staff, Fénix AI.
  3. `MDJ_ICONOS_PANEL` / `MDJ_GRUPOS_PANEL` (panel hamburguesa) — ícono `roadmap` y su membresía en el grupo "Sistema" retirados.
  4. El bloque que forzaba `[data-mdj-nav="roadmap"]` a visible "para todos los roles" — retirado por completo (código muerto: sin ningún nodo con esa marca en el DOM, no tenía nada que gobernar).
  5. El comentario de "NORMALIZACIÓN CANÓNICA DE LOS 9 PUESTOS" (que listaba MRM IA como puesto 9) actualizado a 8 puestos, reflejando la realidad actual.
- **`web/staff.html`**: retirado `staff-mrm-link` de `staffTopnavHtml('owner')` — verificado que era la única aparición en el archivo (los roles seller/manager nunca lo tuvieron).
- **Nuevo hogar: `web/index.html`**, sección `.action-cards-section` (ya existía con 3 tarjetas: Librería/Portal/Pago) — se sumó una 4ª tarjeta "MRM IA" con el mismo patrón visual exacto, enlazando a `road-map.html`. Sin foto propia en `web/assets/` para este tema — en vez de inventar una ruta de imagen inexistente, se usó un degradado con los tonos de marca ya establecidos (`--gold` sobre fondo oscuro, ver `.mrm-bg` en `styles.css`). Copy nuevo tomado del H1 real de `road-map.html` ("Cómo funciona esta plataforma"), no inventado: claves `card-mrm-sub`/`card-mrm-title` agregadas en ambos bloques de `translations.js` (ES: "Guía"/"Cómo Funciona la Plataforma"; EN: "Guide"/"How the Platform Works"). Grid de la sección pasó de `repeat(3,1fr)` a `repeat(4,1fr)` — el breakpoint móvil existente (`@media max-width:992px { grid-template-columns:1fr }`) ya apilaba a una columna, así que la 4ª tarjeta no rompe nada ahí.
- **Verificado en vivo (no a ciegas)**: sintaxis limpia en los 4 archivos tocados (`mdjb-shared-header.js`, `staff.html`, `index.html`, `translations.js`), y en el navegador real contra `index.html`: `#mainNav-roadmap-link` ya no existe en el DOM, el texto "MRM IA" no aparece en ningún lugar de la página, la tarjeta nueva renderiza con sus 4 columnas iguales, el degradado correcto, el href a `road-map.html`, y el texto correcto.
- Pendiente de aprobación de commit: `web/mdjb-shared-header.js`, `web/staff.html`, `web/index.html`, `web/translations.js`, `web/styles.css`, las 48 páginas con el enlace viejo retirado, este documento.
- **⚠️ Malentendido real en vivo, aclarado por el PO — dejarlo escrito para que no se repita**: el pedido original ("sácala de Mi Perfil, déjala en el menú de inicio... que se pueda ver desde inicio nada más") se interpretó como "retirar de toda la barra compartida", cuando el PO se refería específicamente a sacarla de donde competía con el espacio de MI PERFIL, no de sacarla del todo de la navegación visible desde Inicio. El PO pidió DETENER el trabajo a mitad de la reversión ("stop para ahí") — se paró de inmediato, sin tocar nada más, y se reportó el estado exacto (47/49 páginas ya revertidas, 2 sin poder revertir automáticamente, el resto sin tocar todavía). **Tras ver el resultado final, el PO decidió aprobarlo tal cual había quedado** ("me callo y lo apruebo... dejémosla como una opción o herramienta ahí y no como protagonista del menú... hiciste algo inconscientemente para bien") — la tarjeta en Inicio se queda, no vuelve a la barra compartida. Se re-aplicó la limpieza en las 47 páginas que se habían revertido momentáneamente. Estado final, verificado en vivo: `#mainNav-roadmap-link` no existe en ningún lugar del DOM; la tarjeta "MRM IA" (`How the Platform Works` / `Cómo Funciona la Plataforma`) vive únicamente en la sección de accesos rápidos de `index.html`, 4ta de 4 tarjetas.

## [2026-09-02] "Efecto chicle" del owner al cambiar de pestaña — causa raíz real encontrada y corregida
- **Reporte del PO, con hartazgo explícito**: "al moverse de pestaña en pestaña tenemos efecto chicle o salto, inestabilidad... seguimos con el mismo detalle de la pestaña saltarina chiclosa imantada inestable... y no terminas de solucionarlo de una vez." El término "chicle" YA estaba documentado en el código (`mdj-estacion-preflight.js`, `mdjb-shared-header.js:504`, `header-unified.css:5180`) como un síntoma conocido: la barra de navegación se pinta primero con la versión PÚBLICA (8 puestos) y salta/estira a la versión de ESTACIÓN (9-10 puestos) un instante después, cuando el rol de la sesión termina de resolver de forma asíncrona.
- **Ese chicle YA se había corregido — pero solo para ARTISTA, nunca para OWNER/STAFF.** El 2026-08-21 se agregó una marca síncrona (`data-mdj-estacion="artista"`, escrita por `mdj-estacion-preflight.js` leyendo el rol directo del JWT en `localStorage`, sin red) que `mdjTablaDeSlots()` en `mdjb-shared-header.js` lee ANTES de esperar la resolución asíncrona normal (`mdjEsStaffEnVivo()`, que depende de `data-mdj-nav-role`, escrito mucho después). El mismo preflight YA pintaba correctamente la barra INTERNO para owner/management/seller/admin en ese mismo instante síncrono — pero nunca dejaba la marca equivalente. Consecuencia real, verificada leyendo el código: en la primera pasada de `mdjAssertNavSlots()`, `mdjEsStaffEnVivo()` daba `false` (el atributo async todavía no llegaba), `mdjTablaDeSlots()` caía a la tabla PÚBLICA (que ni siquiera tiene Academia/Agenda/DJ Tools/Cash Flow/Staff/Fénix AI), y el normalizador reescribía el riel que el preflight ya había pintado bien hacia esa forma equivocada — para saltar de vuelta al INTERNO correcto un instante después, cuando el rol al fin resolvía. Ese doble salto VISIBLE, en cada carga de página de estación (o sea, en cada clic entre pestañas, porque cada una es una carga de página nueva), es el chicle que el PO seguía sufriendo como owner mientras el artista ya no lo sufría desde hace dos semanas.
- **Corregido con el mismo mecanismo exacto ya probado, extendido al caso que faltaba**: `mdj-estacion-preflight.js` ahora deja `data-mdj-estacion="oficina"` (antes solo dejaba `"artista"`) cuando el rol del JWT es owner/management/seller/admin. `mdjTablaDeSlots()` en `mdjb-shared-header.js` gana un atajo gemelo al de artista: si ve `data-mdj-estacion==="oficina"`, devuelve `MDJ_NAV_SLOTS_INTERNO` de inmediato, sin esperar la resolución asíncrona. Mismo modelo de seguridad que el caso de artista (ya documentado): esto no da permisos, solo decide qué menú se pinta primero — quien manda de verdad sigue siendo RLS en el servidor.
- **Verificado en vivo, no a ciegas**: en `dj-tools.html` con sesión de owner real, `data-mdj-estacion` sale `"oficina"` desde el primer instante, la barra ya trae los 9 puestos correctos (Home/Academy/Schedule/Config/DJ Tools/Cash Flow/MI PERFIL/Staff/Fénix AI) desde el arranque, y se tomaron 4 capturas del contenido real de `#mainNav` a lo largo de 2 segundos (arranque, +300ms, +1s, +2s) — el texto y la cantidad de hijos del riel quedaron IDÉNTICOS en las 4, cero reconstrucción, cero salto.
- **Alcance**: afecta a las 8 páginas que cargan `mdj-estacion-preflight.js` (`academia.html`, `account-settings.html`, `dj-tools.html`, `dj-profile.html`, `dj-dashboard.html`, `courses.html`, `dj-knowledge.html`, `mdjb-music-intelligence.html`) — exactamente las páginas de "estación" donde el PO reportó el salto al cambiar de pestaña.
- Pendiente de aprobación de commit: `web/mdj-estacion-preflight.js`, `web/mdjb-shared-header.js`, este documento.

## [2026-09-02] Switch de idioma duplicado en la tira interna de `staff.html` — retirado
- **Reporte del PO**: al moverse entre Academia/Mi Perfil/Agenda como owner, una pestaña de cambio de idioma "se quita... se pone... no sé por qué". Regla explícita: el switch de idioma debe existir en SOLO dos lugares — el switch global de la cabecera (visible en todas las páginas normales, `.header-actions > .mdj-header-r1-auth-cluster > .lang-switcher`) y el de configuraciones del perfil (`account-settings.html`). Ningún otro lugar debe tener uno propio.
- **Aclaración honesta, no atribuirme algo que no hice**: no agregué un switch nuevo esta noche. El de `staff.html` ya existía antes (dentro de `staffTopnavHtml()`, variable `langSwitcher`, repetida en las 3 ramas de rol: owner/seller/manager) — lo que sí hice fue retirar el enlace de MRM IA que vivía justo delante de él (ver entrada de arriba); al desaparecer ese vecino, el switch se corrió un puesto y probablemente se notó más / distinto que antes. Verificado con `grep`: el switch no aparece en ninguna otra parte de este archivo fuera de esa función.
- **Causa real de que existiera este tercero**: `staff.html` no monta `#mainHeader` (tiene su propia cabecera, `.staff-topbar` + `#staff-topnav`) — nunca tuvo el switch global de la cabecera compartida, así que en algún momento se le agregó uno propio, redundante con el de `account-settings.html` (la página de Config a la que igual se llega desde el mismo menú).
- **Corregido**: retirada la variable `langSwitcher` y sus 3 usos (owner, seller, manager) de `staffTopnavHtml()` en `web/staff.html`. Quien esté en `staff.html` y quiera cambiar de idioma ya puede hacerlo desde Inicio o desde ⚙ Config — no hace falta un tercero aquí.
- **Verificado en vivo**: `#staff-topnav` ya no contiene `.lang-switcher` en ningún rol; sintaxis limpia.
- **Nota aparte, NO corregida (fuera del alcance de este reporte, es una inconsistencia distinta)**: al revisar cuántas páginas tienen el switch, salió que `academia.html`/`dj-tools.html`/`dj-dashboard.html` solo tienen el switch de escritorio (ninguno dentro de su menú hamburguesa móvil), mientras que `dj-profile.html` sí tiene los dos (uno de escritorio, uno dentro de `.mdj-mobile-lang` en el menú móvil) — eso significa que en móvil, esas 3 páginas no tienen forma de cambiar de idioma desde el menú. No es el síntoma que reportó el PO (que fue en escritorio, entre pestañas), así que se deja anotado sin tocar hasta que se confirme si es un problema real a resolver.
- Pendiente de aprobación de commit: `web/staff.html`, este documento.

## [2026-09-02] Auditoría de artista/cliente pedida por el PO — encontrado un bug REAL en cliente, más serio que el del owner
- **Orden del PO**: tras corregir el chicle del owner, pidió explícitamente auditar que artista y cliente no tuvieran el mismo problema ("esta parte debe ser sólida, ya no más saltos ni inestabilidad"). Auditoría delegada a un agente de exploración, con hallazgos verificados línea por línea (no especulación).
- **Artista (`MDJ_NAV_SLOTS_ARTISTA` vs. `ESTACION` en el preflight)**: hay un desajuste real de `href` en 3 puestos (Cash Flow, SoundForTips, MI PERFIL) — el preflight pinta un destino de navegación real, mientras el resolvedor "oficial" los trata como pestañas internas (`href:'#'` + `tab:`) cuando ya se está en `dj-profile.html`. **Verificado que esto NO produce salto visual** (es una corrección de atributo `href` invisible, no un re-render) en 7 de las 8 páginas, donde `mdjb-shared-header.js` carga bloqueante y corrige antes de que nadie pueda hacer clic. **`dj-profile.html` es la excepción**: carga el header con `defer` (la única página que lo hace, y es el archivo más grande, ~14.800 líneas) — ahí la ventana de carrera es real y medible: un clic en Cash Flow antes de que el script cargue navegaría a la página equivocada en vez de cambiar de pestaña internamente. **Bajo impacto, no corregido todavía** (ventana angosta, requiere que el clic ocurra en los primeros instantes de carga de la página más pesada del sitio) — anotado para una próxima pasada.
- **Cliente — bug real y más grave que el del owner, encontrado y corregido hoy mismo**:
  1. **Clasificación incorrecta y PERMANENTE, no solo un salto pasajero.** `mdj-estacion-preflight.js` no tenía ninguna rama para "cliente" — su lógica era `juego = oficina ? INTERNO : ESTACION`, así que un cliente real (sin fila en `dj_profiles`) recibía la barra de ARTISTA (Agenda, Cash Flow, SoundForTips, Shop de artista) pintada de entrada. Como `data-mdj-estacion="artista"` nunca se vuelve a revisar contra el rol real ya resuelto en ningún punto de `mdjb-shared-header.js`, un cliente se quedaba con el menú de artista **el resto de esa carga de página**, en las 8 páginas de estación — no un chicle de un instante, un menú equivocado de forma estable.
  2. **CONFIG y MI PERFIL aparecían tarde para un cliente**, en las ~44 páginas con cabecera compartida (no solo las 8 de estación): a diferencia de artista/owner (donde esos dos puestos nunca llegan ocultos en sus tablas internas), para cliente esos dos solo se revelan dentro de `checkSessionForNav()`, una cadena async — el mismo mecanismo exacto de chicle ya corregido para owner, sin corregir para cliente.
  - **Corregido**: `mdj-estacion-preflight.js` ahora distingue explícitamente `esArtista` (`artist`/`dj`/`talent`) de "ningún rol reconocido" (cliente u otro). Para el caso cliente: NO se reescribe `#mainNav` (la tabla pública que ya trae el HTML es la correcta para un cliente) y NO se deja ninguna marca `data-mdj-estacion` — solo se revela `#mainNav-config-link`/`#mainNav-mi-portal-link` de inmediato (mismo criterio que oficina/artista: la sesión sí se conoce de forma síncrona, aunque el rol exacto todavía no), evitando el segundo chicle. Verificado que el HTML crudo de las páginas de estación ya trae esos dos elementos con hrefs de respaldo razonables para cliente (`client-account.html`/`client-portal.html`) incluso antes de que corra cualquier JS.
- **Hallazgo menor, no corregido**: el puesto SoundForTips es el único de los cinco puestos con destino dinámico (Agenda/Flow/Config/MiPerfil/SFT) que nunca recibió su propio resolutor "click-en-vivo" (`mdjEngancharSft`) — bajo riesgo hoy porque su href ya coincide fuera de `dj-profile.html`, pero queda como el último cabo suelto de esa familia de arreglos.
- Pendiente de aprobación de commit: `web/mdj-estacion-preflight.js`, este documento.

## [2026-09-02] "Las pestañas se corren de lugar" (owner, Academia→Agenda) — causa real: Agenda/Cash Flow redirigían a la página equivocada
- **Reporte del PO**: como owner, al ir de Academia a Agenda "hay un cambio de posición en las pestañas, se mueven notablemente de lugar... no se están cambiando los nombres... errores de cálculo simétrico y posicionamiento."
- **Causa real, no a ciegas**: en `MDJ_NAV_SLOTS_INTERNO`, Agenda y Cash Flow apuntaban directo a `staff-agenda.html` — pero esa página tiene su propio candado de iframe (`web/staff-agenda.html:484`: `if (window.self === window.top) { location.replace('./staff.html'); }`), pensado para vivir DENTRO del iframe `#agenda-frame` de `staff.html`, no como página de tope. Un clic real navegaba a `staff-agenda.html`, que se auto-redirigía de inmediato a `staff.html` **sin `?vista=`**, aterrizando en Mi Perfil por defecto (no en Agenda) — y de paso cambiando la cabecera entera de `#mainNav` (la de Academia, `mdjb-shared-header.js`) a la cabecera propia de `staff.html` (`#staff-topnav`, un componente CSS completamente distinto). Dos implementaciones separadas del "mismo" menú, con métricas de fuente/espaciado propias, es justo lo que se percibía como "las pestañas se corren" sin que cambiara ningún rótulo.
- **Corregido**: Agenda y Cash Flow ahora apuntan directo a `staff.html?vista=agenda` / `staff.html?vista=cashflow` — mismo destino real, sin el salto de página extra ni el aterrizaje equivocado en Mi Perfil. Verificado en vivo: el `href` sale correcto desde Academia, y navegando directo a `staff.html?vista=agenda` el `data-vista` y el botón activo de `#staff-topnav` confirman "Agenda" (no Mi Perfil).
- **Lo que este arreglo NO resuelve, con honestidad, medido en vivo**: `#mainNav` (Academia) y `#staff-topnav` (staff.html) siguen siendo dos componentes con CSS propio y métricas ligeramente distintas (letter-spacing/gap/padding) — medí la posición real de cada pestaña en ambos y el corrimiento entre una y otra NO es un offset fijo (crece de +16px en "Inicio" a -17px en "MI PERFIL"), confirmando que son dos implementaciones independientes, no una sola con un bug de cálculo puntual. Unificar esas métricas es un trabajo de CSS más profundo y con más riesgo (esa zona ya pasó por muchas rondas de ajuste fino a pedido del PO) — no se tocó en esta pasada, queda anotado como el residuo real que explica que el "salto" nunca desaparezca del todo aunque el destino ya sea el correcto.
- Pendiente de aprobación de commit: `web/mdjb-shared-header.js`, este documento.

## [2026-09-02] "Se hunde y sube" en Academia — causa real: máscara de arranque vieja tapando contenido que ya estaba bien pintado
- **Reporte del PO, con prueba en video propia**: "cuando cambio a academia se baja como a una pestaña abajo y automáticamente se sube a su lugar... es como si todavía esa pestaña viviera en dos lugares." El PO insistió, correctamente, en que la prueba visual ya estaba en el video que había mandado y pidió que se buscara ahí en vez de seguir preguntando — se revisó el video cuadro por cuadro (`ffmpeg` a 120fps) y se confirmó un cuadro real (≈11.275s del video) con el riel de pestañas vacío, solo el botón sol/luna visible. El PO además avisó que esto podía estar pasando "en otras partes" del sitio, no solo en Academia, y que por eso mis confirmaciones de "reparado" no coincidían con lo que sus ojos seguían viendo.
- **Diagnóstico con datos reales, no con más video**: en vez de seguir extrayendo cuadros, se instrumentó `academia.html` en vivo con un `MutationObserver` temporal (retirado al terminar) que registró, con marca de tiempo exacta, cada cambio de hijos de `#mainNav` y cada cambio de clase en `<html>`/`<body>`, contra una sesión real de owner en el servidor local.
- **Causa raíz real, medida en milisegundos**: el prevuelo síncrono (`mdj-estacion-preflight.js`, el mismo que corrigió el "chicle" del owner el mismo día) YA pintaba las 9 pestañas correctas de `#mainNav` a los **29ms** de arranque — perfecto. Pero una "máscara de arranque" vieja (`mdjApplyAuthBootMask()` en `mdjb-shared-header.js`, clases `mdj-nav-booting`/`mdj-auth-resolving`, con su regla en `header-unified.css`) se activaba igual a los 76ms y ponía `visibility:hidden !important` sobre TODO el riel (`.header-nav`, que envuelve tanto las 9 pestañas como el botón sol/luna) — y no se quitaba hasta **~700ms después**, cuando terminaba una cadena de llamadas asíncronas a Supabase que ya no decidía nada sobre CUÁLES pestañas mostrar (eso el prevuelo ya lo había resuelto síncronamente, más rápido que la propia máscara). Esa máscara se construyó hace tiempo para tapar la ESPERA asíncrona de antes de que existiera el prevuelo síncrono — nadie la desactivó cuando el prevuelo la volvió redundante para owner/artista, y siguió tapando durante casi un segundo contenido que ya estaba bien. Verificado con `getComputedStyle(.header-nav).visibility` en vivo: pasaba de `hidden` a `visible` sin que el contenido de `#mainNav` cambiara ni una vez durante esa ventana — confirma que no era una "foto vieja" ni una reconstrucción, era una cortina de más.
- **Corregido**: en `web/header-unified.css`, la regla de la máscara que oculta `.header-nav` ahora se excluye cuando `body` ya tiene `data-mdj-estacion` puesto (artista U oficina) — es decir, exactamente cuando el prevuelo síncrono ya resolvió qué pintar antes de que la máscara pudiera correr. `.header-actions`/`#header-auth-zone` (avatar/login, que no dependen del prevuelo) NO se tocaron, siguen protegidos igual que antes.
- **Verificado en vivo con el mismo instrumento, antes/después**: antes del fix, `.header-nav` pasaba a `hidden` 19 veces por carga y se quedaba así hasta pasado el segundo 0.7 con las 9 pestañas ya puestas debajo, invisibles. Después del fix, en la misma página y misma sesión: `.header-nav` nunca vuelve a `hidden` una vez que las 9 pestañas reales están puestas (confirmado programáticamente — cero ocurrencias de "oculto con contenido ya correcto"); el único instante oculto que queda es antes de los ~28ms, mientras el HTML de reserva original (no vacío, con sus propios 8 enlaces) todavía no fue reemplazado — muy por debajo del umbral de parpadeo perceptible.
- **Honesto sobre el alcance**: esta máscara es compartida por TODAS las páginas con `#mainHeader` (no solo las 8 de estación) — la excepción que se agregó solo se activa donde `data-mdj-estacion` existe (las 8 páginas de estación, artista u oficina). El PO avisó que podía estar pasando "en otras partes" — no se auditaron aparte las ~44 páginas de cabecera compartida que NO son de estación (ahí la máscara sigue actuando igual que siempre, sin el problema de "contenido correcto tapado de más" porque no hay prevuelo síncrono que la adelante). Si el PO ve el mismo "hundimiento" fuera de las 8 páginas de estación, es un mecanismo distinto y hay que volver a medir, no asumir que es este mismo.
- Pendiente de aprobación de commit: `web/header-unified.css`, este documento.
- **Pendiente de confirmación visual del PO** (Regla 7): falta que lo vea con sus propios ojos en su máquina, con red real — este entorno de prueba tiene la sesión de Supabase real pero la red del sandbox falla intermitentemente (CORS/DNS), así que la medición de tiempos pudo no reflejar exactamente la latencia de producción, aunque el mecanismo del bug (máscara tapando contenido ya correcto) es indiscutible por sí solo, independiente de cuánto tarde la red.

## [2026-09-02] Segunda cortina real, más grave que la máscara — el header de dos filas tardaba 1.6s en encoger a una
- **El PO insistió, correctamente, en que el "hundimiento" seguía pasando** después del arreglo de la máscara de arriba, y pidió explícitamente ir a buscar TODAS las cortinas del mismo tipo en vez de conformarse con una. Se instrumentó `academia.html` una segunda vez, ahora midiendo geometría real (`getBoundingClientRect()` de `#mainNav`, no solo presencia de clases) contra una sesión de owner con el "recuerdo" de localStorage borrado a propósito (para simular una visita nueva, el caso real que seguía fallando).
- **Causa raíz real, medida en píxeles y milisegundos**: `#mainHeader` nace con DOS filas (157px: `.header-top` con logo/buscador/login, más `.header-nav` con las pestañas) y se supone que encoge a UNA fila (84px) en cuanto la página es "de estación". Esa contracción depende ENTERAMENTE de dos mecanismos, y ninguno de los dos es el prevuelo síncrono que ya arregla todo lo demás:
  1. `html.mdj-estacion-previa` — una adivinanza puesta ANTES del primer pintado, leyendo un valor de localStorage (`mdj_estacion`) que package casi nunca se escribe fuera de un flujo muy específico y ajeno (la "franja flotante" de un visitante mirando el perfil de OTRO artista).
  2. `body.mdj-perfil-estacion` — la clase "de verdad", puesta por `mdjMontarFranjaFlotante()` en `mdjb-shared-header.js`, pero SOLO cuando `mdjEnEstacionDeTrabajo()` confirma sesión — una función que el propio código ya tenía marcada, desde el 31 de agosto, como fuente conocida de "mutación tardía del DOM" por depender de datos asíncronos (`mdjEsStaffEnVivo()` + `__mdjNavOwnUserId`).
  - Medido en vivo, sesión real de owner, sin el recuerdo cacheado: la tabla de pestañas ya estaba correcta a los 30-40ms (el prevuelo funcionando bien), pero `#mainNav` se quedó parado a **84px del techo de la ventana durante 1647ms**, con la fila de arriba todavía visible debajo — y de golpe saltó a 0px cuando por fin resolvió la cadena async. Un salto de 84 píxeles sostenido casi dos segundos es exactamente el "se hunde y sube" que el PO seguía viendo, y explica por qué mi primer arreglo (la máscara, un problema real pero distinto, de ~650ms de invisibilidad) no lo hizo desaparecer.
  - **Se encontró el mismo patrón repetido tres veces más** en el mismo archivo, todas colgando del mismo par de clases lentas: la altura fija de `#mainHeader` (84px, línea ~5186), y el botón hamburguesa de respaldo `.mdj-riel-toggle` que solo debía aparecer cuando `.header-top` ya estaba oculto (línea ~5502) — sin este arreglo, por debajo de 1150px de ancho esos ~1.6s se quedaban sin barra NI hamburguesa, aun peor que en escritorio.
- **Corregido, en las 4 reglas afectadas de `web/header-unified.css`**: se agregó `body[data-mdj-estacion]` como condición alternativa a `html.mdj-estacion-previa` / `body.mdj-perfil-estacion` en cada una — mismos valores exactos, ninguna cifra nueva. `data-mdj-estacion` es el atributo que `mdj-estacion-preflight.js` ya pone de forma síncrona (JWT leído directo de localStorage, sin red) para artista Y oficina, el mismo dato que desde antes estabiliza la tabla de pestañas — reusarlo aquí cierra el hueco de 1.6 segundos sin tocar la función asíncrona (que sigue existiendo y corriendo, solo que ahora llega a un estado que ya coincidía).
- **Verificado en vivo, antes/después, con el mismo instrumento y sesión real, recuerdo de localStorage borrado a propósito en ambos casos**: antes del arreglo, el salto de 84px tardaba 1647ms en aparecer. Después del arreglo, el mismo salto ocurre en 12ms (21ms→33ms) y la posición se mantiene estable los 15+ segundos siguientes que se dejaron corriendo — cero reaparición. Revisado también visualmente en `academia.html` y `account-settings.html` (esta última usa las mismas reglas de `.acct-body`/`.acct-side` que se tocaron): ambas renderizan sin superposición ni hueco.
- **Honesto sobre lo que falta**: dos reglas hermanas (`#pub-hero-top-stack` y `.dj-hero-inset`, específicas de la franja flotante del hero en `dj-profile.html`) tienen el MISMO problema de fondo pero dependen de que la caja `#mdj-flotante-visitante` exista de verdad en el DOM — esa caja la crea la misma función lenta, así que adelantar el CSS sin adelantar también el montaje real de la caja podría descuadrar en vez de arreglar. No se tocaron en esta pasada; quedan anotadas como el próximo paso si el PO confirma que el salto persiste específicamente en el hero de `dj-profile.html`. No se pudo verificar visualmente esa página en este entorno (la navegación directa a `dj-profile.html` sin `?id=` fue rechazada por el sandbox de prueba).

## [2026-09-02] Chequeo de posiciones entre menús, pedido por el PO tras cerrar las cortinas — MI PERFIL desalineado, con un bug real de ancho de regalo
- **Orden del PO**: "ahora no más cortinas, ahora tira un chequeo que los puestos estén en la misma posición para los dos menús, mismo orden, menú de inicio mismo orden de pestañas, menú de MI PERFIL mismas pestañas mismo orden misma posición."
- **Chequeo hecho leyendo las 3 tablas directo del código** (`MDJ_NAV_SLOTS` público 8 puestos, `MDJ_NAV_SLOTS_INTERNO` owner/staff 9 puestos, `MDJ_NAV_SLOTS_ARTISTA` 9 puestos):
  - INICIO: puesto 1 en las tres. Coincide.
  - ⚙️ CONFIG: puesto 5 en público, puesto 4 en las dos de estación (Owner/Staff y Artista YA coincidían entre sí). El público tiene contenido distinto antes (Servicios/Eventos/Shop) por diseño intencional del "juego interno" — igualar el número exacto contra público no es estructuralmente posible sin inventar puestos vacíos; no se tocó esa comparación.
  - MI PERFIL: puesto 8 en público Y en Artista, pero puesto 7 en Owner/Staff — el único de los tres realmente desalineado.
- **El PO aprobó explícitamente, tras ver el riesgo señalado (`AskUserQuestion`)**: mover MI PERFIL al puesto 8 en Owner/Staff, y revisar CONFIG también.
- **Al investigar el riesgo antes de mover nada, apareció un bug real, no solo cosmético**: `header-unified.css` tiene dos reglas de ANCHO fijas por número de puesto (no por contenido), cada una con un comentario diciendo qué texto asume:
  1. `[data-mdj-slot="8"] { min-width: 96px }  /* ancho de "MI PERFIL" */` — correcta para Artista (MI PERFIL SÍ vivía en el 8 ahí), pero en Owner/Staff el puesto 8 era "Staff" — un texto corto recibiendo el ancho pensado para uno más largo, sin que se notara a simple vista.
  2. `[data-mdj-slot="5"] { min-width: 104px }  /* ancho de "⚙️ CONFIG" */` — YA estaba mal desde antes de este chequeo, sin relación con el pedido de hoy: CONFIG vive en el puesto 4 en las dos tablas de estación desde que Academia ocupó el puesto 2; el puesto 5 real es "DJ Tools", que llevaba tiempo recibiendo el ancho de "CONFIG" en vez del suyo. Pasaba desapercibido porque "DJ Tools" es igual de ancho o más que "CONFIG", así que nunca se notó un recorte.
- **Corregido, dos archivos**:
  1. `web/mdjb-shared-header.js`, `MDJ_NAV_SLOTS_INTERNO`: MI PERFIL pasa del puesto 7 al 8; Staff pasa del 8 al 7. Cash Flow/DJ Tools/Config/Agenda/Academia/Inicio/Fénix AI no se tocaron. Orden nuevo: Inicio, Academia, Agenda, Config, DJ Tools, Cash Flow, **Staff, MI PERFIL**, Fénix AI.
  2. `web/header-unified.css`: la regla de ancho del puesto 5 corregida a puesto 4 (el lugar real de CONFIG hoy). La regla del puesto 8 no necesitó tocarse — con el swap de arriba, ese puesto ya es MI PERFIL también en Owner/Staff, así que el ancho ya apunta a lo correcto en las dos tablas.
- **Verificado que el swap es seguro antes de aplicarlo**: se buscó en todo el archivo cualquier lógica que decida DESTINO o COMPORTAMIENTO por número de slot en vez de por id/clase/atributo `data-mdj-nav` — la única encontrada (`mdjEnsureGuestMiPerfilMainNavLink()`, línea ~2913) ya se auto-excluye explícitamente para `MDJ_NAV_SLOTS_INTERNO`/`MDJ_NAV_SLOTS_ARTISTA` desde antes (línea 2928), así que el swap no la toca. El resolvedor de destino de MI PERFIL y el guardia de Staff funcionan por id (`mainNav-mi-portal-link`/`mainNav-staff-or-profile`) y clase, no por número de puesto — confirmado en vivo: el href de MI PERFIL sigue resolviendo correctamente a `staff.html?vista=miperfil` para el owner, ahora sentado en el puesto 8.
- **Verificado en vivo, dos páginas de Owner/Staff**: `academia.html` y `dj-tools.html` muestran ambas el mismo orden nuevo (…Cash Flow, Staff, MI PERFIL, Fénix AI), sin texto recortado, con la pestaña activa correcta resaltada en cada una.
- Pendiente de aprobación de commit: `web/mdjb-shared-header.js`, `web/header-unified.css` (mismo archivo del hallazgo anterior, tercer cambio encima), este documento.
- **Pendiente de confirmación visual del PO** (Regla 7), igual que los dos hallazgos anteriores de esta sesión.

## [2026-09-02] Cuarta cortina real — mi propio arreglo anterior dejó al descubierto un hueco de 1.36s sin logo ni buscador
- **El PO insistió una vez más**: "todavía queda algún salto por ahí... ya sabemos por qué... eliminen las fotos que queden entre una pestaña y otra." Se instrumentó `academia.html` otra vez, ahora midiendo específicamente CUÁNDO se oculta `.header-top` (el arreglo de la entrada anterior) contra CUÁNDO aparece de verdad la "franja flotante" (`#mdj-flotante-visitante`, el logo+buscador que debe reemplazarlo en las páginas de estación).
- **Causa raíz real, y es en parte consecuencia directa de mi propio arreglo anterior**: `.header-top` ahora se oculta a los ~30-40ms (correcto, gracias a `data-mdj-estacion`). Pero el MONTAJE de la franja flotante que lo reemplaza sigue decidiéndose con `mdjArtistaEnSuPerfil()` → `mdjEnEstacionDeTrabajo()` — la MISMA función asíncrona marcada desde el 31 de agosto como fuente de "mutación tardía", la misma que causó las dos cortinas anteriores. Medido en vivo: `.header-top` se ocultaba a los 36ms, pero la franja no aparecía hasta los **1397ms** — 1.36 segundos con la cabecera completamente SIN logo, SIN buscador y sin login, un hueco real que antes NO se notaba porque `.header-top` (con el logo original todavía puesto) se quedaba visible tapándolo. Al arreglar la entrada anterior sin arreglar esta, el hueco no desapareció: solo cambió de forma, de "salto de posición" a "logo desaparecido".
- **Corregido**: en `mdjNormalizeMainNavSlots()` (`web/mdjb-shared-header.js`), el `else if (mdjArtistaEnSuPerfil())` que decide montar la franja ahora también acepta `document.body.getAttribute('data-mdj-estacion')` como disparador — el mismo dato síncrono que ya gobierna todo lo demás. `mdjMontarFranjaFlotante()` ya era idempotente de antes (`if (document.getElementById('mdj-flotante-visitante')) return;`), así que no hay riesgo de montarla dos veces cuando la función asíncrona lenta también confirme lo mismo más tarde.
- **Verificado en vivo, antes/después, mismo instrumento, sesión real, recuerdo de localStorage borrado**: antes del arreglo, la brecha entre ocultar `.header-top` y montar la franja era de 1361ms. Después del arreglo, 11ms. Revisado visualmente en `academia.html`: logo, marca y buscador aparecen de inmediato en la franja, sin hueco.
- **Van CUATRO cortinas encontradas y corregidas en esta misma sesión, todas la MISMA causa raíz de fondo** (`mdjEnEstacionDeTrabajo()`/`mdjArtistaEnSuPerfil()`, dependiente de datos asíncronos de Supabase que tardan 1-1.6 segundos en resolver, usada para decidir layout que `data-mdj-estacion` ya resuelve de forma síncrona en 30-40ms): la máscara de arranque, la altura del header + 3 reglas hermanas, y ahora el montaje de la franja flotante. Se hizo una búsqueda exhaustiva de `mdj-estacion-previa`/`mdj-perfil-estacion`/`mdjEnEstacionDeTrabajo` en `header-unified.css` y `mdjb-shared-header.js` tras este arreglo — no quedó ninguna otra regla de layout de `#mainNav`/`.header-top`/franja sin su contraparte síncrona.
- **Lo único que sigue sin tocarse, ya documentado en la entrada anterior**: `#pub-hero-top-stack`/`.dj-hero-inset` (hero de `dj-profile.html`) — dependen de la MISMA franja que ahora monta 1.36s más rápido, así que es posible que este arreglo ya las haya resuelto de rebote; no se pudo verificar porque `dj-profile.html` no cargó en este entorno de prueba sin un `?id=` válido.
- Pendiente de aprobación de commit: `web/mdjb-shared-header.js`, este documento.
- **Pendiente de confirmación visual del PO** (Regla 7): esta es la que más importa de las cuatro — pedirle específicamente que confirme en su propia máquina que YA NO ve ningún salto ni hueco al cambiar de pestaña, en más de una página de estación.

## [2026-09-02] Caché sin actualizar — el error de gobernanza más básico de la sesión, y probable causa de la captura del PO
- **El PO mandó una captura real** de `account-settings.html` (owner) mostrando la barra PÚBLICA de 8 puestos (Inicio/Servicios/Eventos/Shop/Config/Trabajos/Contacto/Mi Perfil) en vez de la de estación — con el reclamo, correcto, de que CONFIG pertenece a MI PERFIL/estación, no a la vitrina pública.
- **Probado en vivo, en este entorno, con carga directa**: `account-settings.html` para el owner SÍ pinta la barra de estación correcta (9 puestos, Config activo). Contradice la captura del PO — la diferencia no está en el código de hoy, está en QUÉ VERSIÓN de ese código estaba corriendo en su navegador.
- **Causa real encontrada**: edité `mdj-estacion-preflight.js`, `mdjb-shared-header.js` y `header-unified.css` varias veces en esta sesión (las cuatro cortinas de arriba) sin subir su `?v=` en ninguna de las 48 páginas que los cargan — la propia regla de memoria de este proyecto ("actualizar SIEMPRE el `?v=` al editar un .js/.css referenciado") se pasó por alto toda la sesión. Un navegador con estos tres archivos en caché seguía ejecutando el código de ANTES de todos los arreglos de hoy.
- **Corregido**: los tres archivos ahora se referencian con `?v=20260902-cortinas` en las 48 páginas que los cargaban (antes con versiones sueltas de 2026-08-22/24/25/31, inconsistentes entre sí incluso antes de hoy) — reemplazo verificado archivo por archivo con un script que solo tocó las cadenas exactas de versión, sin `sed` a ciegas.
- **De paso, se confirmó que el segundo pedido del PO YA estaba resuelto en el código**: "si no hay sesión, Config debe llevar a login" — `mdjDestinoConfigEnVivo()` (línea 760) ya hace `if (!ses) return './login.html';` desde antes de esta sesión. No fue necesario tocar nada ahí.
- **Pendiente real**: que el PO confirme, con un refresco forzado (Cmd+Shift+R) en su propia máquina, que `account-settings.html` ya le muestra la barra de estación (Config dentro de MI PERFIL) y no la pública — si la caché era la única causa, esto debería quedar resuelto sin más cambios de código.
- Pendiente de aprobación de commit: las 48 páginas con el `?v=` actualizado, este documento.
- **Pendiente de confirmación visual del PO** (Regla 7).

## [2026-09-02] Config y MI PERFIL sin sesión: login.html ahora sabe volver a donde el invitado quería ir
- **Orden del PO**: "config debe llevar a login pero una vez logeado dentro de mi perfil... si se entró por mi perfil entonces esa entrada es dentro de mi perfil después de login."
- **Confirmado antes de tocar nada**: sin sesión, Config y MI PERFIL YA mandaban a `login.html` — pero a secas, sin decirle a `login.html` a dónde volver. `login.html` YA tiene un mecanismo para esto (`?redirect=<pagina>`, línea ~1889, el mismo que usa el flujo de Jobs) — solo que ni Config ni MI PERFIL se lo pedían.
- **Corregido, 6 puntos en `web/mdjb-shared-header.js`**: los `'./login.html'` a secas de `mdjResolveConfigHref()`, `mdjDestinoConfigEnVivo()` (2), `mdjResolveMiPerfilHref()` y `mdjDestinoMiPerfilEnVivo()` (2) ahora llevan `?redirect=account-settings` (Config) o `?redirect=dj-profile` (MI PERFIL). Tras iniciar sesión, `login.html` navega directo a esa página con `?from_auth=1` — sin pasar por ningún destino genérico intermedio.
- **Por qué `dj-profile` es seguro para MI PERFIL aunque el rol real termine siendo owner o cliente**: ya existe un guardia (`mdjSacarOwnerDelPerfilAjeno()`, línea ~1072, "el owner no aterriza en dj-profile.html") que devuelve a cualquier owner de vuelta a `staff.html` si llega ahí sin `?id=` — se reusa ese guardia en vez de inventar un destino nuevo por rol. **Sin verificar en este entorno**: el caso cliente cayendo en `dj-profile.html` sin `?id=` — no encontré (ni tuve tiempo de confirmar) un guardia equivalente para cliente; si un cliente entra por MI PERFIL sin sesión, después de loguearse vería la vista de "visitante" de un perfil vacío por una fracción de segundo antes de que el resto de la cadena lo mande a `client-portal.html`, no necesariamente su propio panel de una vez. Marcado para verificar con una cuenta de cliente real.
- Pendiente de aprobación de commit: `web/mdjb-shared-header.js`, este documento.
- **Pendiente de confirmación visual del PO** (Regla 7), y específicamente probar el flujo completo invitado→login→destino con una cuenta de cliente antes de darlo por cerrado del todo.

## [2026-09-03] "Se corrige un segundo y se regresa el error" — no era un revert, era un CICLO INFINITO entre dos copias desalineadas de la misma tabla
- **El PO reportó, tras el refresco forzado que se le pidió**: "se corrige un segundo y se regresa el error." Se instrumentó `account-settings.html` con una sonda que registra CADA vez que cambia el texto de `#mainNav`, con marca de tiempo — nada de suposiciones.
- **Lo medido no era un revert a la barra pública** (esa parte ya estaba bien): era una **oscilación infinita** entre dos variantes de la MISMA barra de 9 puestos — "⚙️ Config"/"Cash Flow" (texto crudo, sin traducir) contra "⚙️ CONFIG"/"Flujo de Caja" (texto traducido) — cambiando cada ~200-300ms, sin parar, todavía activa a los 15 segundos de haber cargado la página.
- **Causa raíz real**: existen DOS copias de la tabla de estación owner/staff — `INTERNO` en `mdj-estacion-preflight.js` (pinta el primer instante) y `MDJ_NAV_SLOTS_INTERNO` en `mdjb-shared-header.js` (normaliza después) — que el propio comentario del archivo del prevuelo exige mantener idénticas ("son la misma barra vista en dos momentos"). Estaban desalineadas en el orden de Staff/MI PERFIL/Fénix AI desde antes de esta sesión (7=Fénix AI/8=MI PERFIL/9=Staff en una, distinto en la otra) — probablemente inofensivo mientras coincidía por casualidad con lo que el vigilante de orden esperaba. Al mover MI PERFIL al puesto 8 ayer (ver "Chequeo de posiciones entre menús") solo se actualizó `MDJ_NAV_SLOTS_INTERNO`, sin sincronizar la copia del prevuelo — el desajuste resultante SÍ cruzaba el umbral que vigila `mdjWatchOne()` (`mdjb-shared-header.js`, ~línea 1803): un `MutationObserver` que detecta "orden sucio" en `data-mdj-slot` y vuelve a normalizar. Cada normalización disparaba una nueva mutación, que el vigilante volvía a ver "sucia", que disparaba otra normalización — un ciclo que se retroalimentaba solo, sin llegar nunca a un estado estable.
- **Corregido**: reordenados los últimos 3 puestos de `INTERNO` en `mdj-estacion-preflight.js` para calzar EXACTO con `MDJ_NAV_SLOTS_INTERNO` — 7=Staff, 8=MI PERFIL, 9=Fénix AI en ambos archivos, ahora sí "la misma barra en dos momentos" como siempre debió ser.
- **Verificado en vivo con la misma sonda, antes/después**: antes, seguía cambiando de firma cada ~200-300ms sin parar a los 15s. Después, cambia 6 veces en los primeros 881ms (el asentamiento normal mientras la sesión resuelve) y se queda quieto el resto de los 8 segundos observados — cero oscilación.
- **Lección para no repetir**: cuando una tabla de navegación vive DUPLICADA en dos archivos por diseño (prevuelo síncrono + normalizador), cualquier cambio de orden/contenido en una exige el mismo cambio en la otra en el mismo commit — no son "el mismo dato en dos lugares", son dos fuentes de verdad que deben mantenerse manualmente en sincronía, y el propio código ya lo advertía en un comentario que no se leyó a tiempo ayer.
- Pendiente de aprobación de commit: `web/mdj-estacion-preflight.js`, este documento.
- **Pendiente de confirmación visual del PO** (Regla 7) — esta sí necesita su propio refresco forzado, ya que el archivo cambió de versión otra vez (`?v=20260902-sync-fix`).

## [2026-09-03] Unificación real de tipografía/espaciado entre #mainNav y #staff-topnav — el residuo que quedaba anotado desde el 2026-09-02
- **Orden del PO**: "estabiliza las barras de menú que todo quede en la misma posición tamaño tipografía distancia para que no se noten saltos cuando se cambien las pestañas... ya sabemos que lo estaba causando y como lo solucionamos" — pidiendo aplicar el mismo criterio de esta sesión (datos reales medidos, no aproximaciones) al residuo que ya había quedado documentado el 2026-09-02: `#mainNav` (páginas de estación) y `#staff-topnav` (`staff.html`) son dos implementaciones de CSS completamente separadas, con métricas propias, y el corrimiento entre ellas no era un offset fijo.
- **Por qué ahora sí se pudo cerrar, cuando antes se dejó anotado como "más riesgo"**: la razón original para que `.staff-topnav` encogiera ANTES que `#mainNav` (comentario del propio archivo, "CALIBRACIÓN AUTOMÁTICA... con las 11 pestañas el menú ocupa 1463px") ya no aplica — `#staff-topnav` tiene hoy exactamente 9 pestañas reales, las mismas que `#mainNav`, desde el "chequeo de posiciones" del 2026-09-02. Verificado contando los hijos reales del DOM antes de tocar nada.
- **Medido en vivo, mismo ancho de ventana (1440px y 1800px), las dos barras**: `#mainNav` usa un sistema de variables CSS (`--mdj-nav-font-size`, `--mdj-nav-letter-spacing`, etc., definidas en `header-unified.css` `:root`) con un quiebre especial en 1340-1500px (letter-spacing baja a 1.8px fijo) y una regla deliberada de "cero relleno lateral propio, todo el espacio lo da el separador" (comentario propio del archivo, línea ~4353). `.staff-topnav` tenía su PROPIA fórmula continua (`clamp()` con `vw`) que se acercaba pero nunca calzaba exacto: a 1440px, `#mainNav` daba 20px/1.8px/gap 8.64px/relleno "4px 0 8px"; `.staff-topnav` daba 19.552px/1.912px/gap 0/relleno "4px 6.48px 8px" — la diferencia real detrás del "salto" que se seguía notando al cambiar de página.
- **Corregido, en `web/staff.html`**: `.staff-topnav button,.staff-topnav a` ahora usa `var(--mdj-nav-font-size, 20px)`, `var(--mdj-nav-font-weight, 800)` y `var(--mdj-nav-letter-spacing, 0.14em)` — las MISMAS variables que `#mainNav`, con los mismos valores de respaldo, sin necesidad de cargar `header-unified.css` completo en `staff.html` (que no lo carga por diseño, tiene su propia cabecera). Se agregó el mismo quiebre `@media (min-width:1340px) and (max-width:1500px)` redefiniendo la variable a `1.8px`, calcado del que ya existe en `header-unified.css` ~línea 5272. El relleno lateral por pestaña se quitó por completo (antes `clamp(4px,0.45vw,7px)`, ahora `0`, igual que `#mainNav`) y el `gap` del contenedor pasó de `0` a `clamp(4px,0.6vw,10px)`, el mismo que usa `#mainNav` — un solo modelo de espaciado en vez de dos sumándose.
- **Verificado en vivo, antes/después, a 1440px Y 1800px**: después del arreglo, `#mainNav` y `.staff-topnav` dan EXACTAMENTE los mismos números en las dos pruebas — 20px/1.8px/gap 8.64px/relleno "4px 0 8px" a 1440px, y 20px/2.8px/gap 10px a 1800px. Cero diferencia medible en los dos anchos probados.
- **Honesto sobre el alcance**: no se replicó el sistema completo de `#mainNav` para anchos por debajo de ~1340px — ahí `#mainNav` directamente desaparece (lo reemplaza el botón hamburguesa/riel desplegable de `header-unified.css`), así que no hay una barra equivalente contra la cual igualar `.staff-topnav` en ese rango; `.staff-topnav` no tiene ese hamburguesa y sigue con su propio achicamiento progresivo hasta scroll, que ya existía y no se tocó. Tampoco se igualó el padding EXTERNO del contenedor (`.staff-topnav` usa `clamp(12px,2vw,32px)`, `#mainNav` usa una cifra distinta de su propio `.container` padre) — es una diferencia menor (par de píxeles en el borde izquierdo) frente a la de tipografía/gap, que era la que realmente se notaba.
- Pendiente de aprobación de commit: `web/staff.html`, este documento.
- **Pendiente de confirmación visual del PO** (Regla 7) — probar cambiando entre una página de estación (Academia, DJ Tools) y `staff.html` en su propia ventana, a su ancho real de pantalla, y confirmar que ya no se nota el salto de tamaño/espaciado. `staff.html` no tiene un `?v=` que subir (su CSS vive inline en el propio archivo), pero como ya pasó antes, puede necesitar otro refresco forzado si el navegador guardó la página completa en caché.

## [2026-09-03] Auditoría de las 4 páginas de estación restantes — limpias. Sexto hallazgo real: el contenido de `staff.html` arrancaba 12px más arriba que el resto
- **Orden del PO**: auditar el resto de páginas de estación que no se habían probado hoy (dj-dashboard.html, courses.html, dj-knowledge.html, mdjb-music-intelligence.html, dj-profile.html), delegado a un agente con el mismo método de medición real (timestamps, no impresiones) usado en los 4 hallazgos anteriores.
- **Resultado de la auditoría — las 4 medibles, LIMPIAS, ningún hallazgo nuevo**: `dj-dashboard.html` (header-top oculto en 4ms, franja en 130ms, `#mainNav` estable a los 152ms), `courses.html` (40ms / 71ms / 86ms), `dj-knowledge.html` (35ms / 45ms / 63ms), `mdjb-music-intelligence.html` (35ms / 44ms / 48ms) — todas muy por debajo del segundo, cero oscilación en 15-18s observados. `dj-profile.html` NO se pudo auditar en su forma normal: con sesión de owner, tanto con `?id=` como sin él, la propia regla de negocio documentada ("el owner no aterriza en dj-profile.html", 2026-08-20) redirige siempre a `staff.html?vista=miperfil` — no es un efecto colateral de los arreglos de hoy, es diseño. Verificar esa página específica requeriría una sesión de artista/DJ real, no disponible en este entorno.
- **Mientras tanto, el PO reportó en vivo un problema real y distinto**: "al cambiar academia agenda y config todavía queda rastro, algo se mueve raro" — la transición cruza entre las DOS implementaciones de cabecera (`#mainHeader`/`#mainNav` en Academia/Config, `.staff-topbar`/`.staff-topnav` en Agenda/`staff.html`), así que podía quedar algo más allá de la tipografía ya emparejada hoy. Medido en vivo, mismo ancho de ventana (1440px), en una pestaña de navegador separada para no chocar con el agente de auditoría: en `academia.html` el `<main>` arranca en y=84px; en `staff.html` (cualquier vista), `.staff-main` arrancaba en y=72px — doce píxeles de diferencia real y medible, el "rastro" que el PO seguía viendo al cruzar entre los dos sistemas.
- **Corregido con una complicación real, documentada para no repetirla**: el primer intento (`margin-top:12px` en `.staff-main`) no tuvo NINGÚN efecto medible — un comentario ya viejo en el propio archivo advertía esto exacto ("se probó ponerlo en main.staff-main y no movía nada: quien posiciona no es main", línea ~161) sin que se leyera a tiempo. La causa real: `web/styles.css` tiene una regla vieja, sin acotar a su página original, `main{margin-top:0 !important}` (línea ~7761, un parche de la página de Servicios escrito con un selector demasiado amplio) que pisa el `<main>` de CUALQUIER página que cargue `styles.css` — `staff.html` incluido. Se agregó `!important` a la regla de `.staff-main` (que por ser de clase ya gana en especificidad; con `!important` de los dos lados, la especificidad decide y gana la clase) — no se tocó la regla vieja de Servicios, es de otra página y corregirla es un ticket aparte.
- **Verificado en vivo, antes/después, en las tres vistas probadas de `staff.html`** (`?vista=agenda`, `?vista=cashflow`, sin parámetro): las tres arrancan ahora en y=84px, igual que `academia.html`. Captura visual confirmada sin superposición ni hueco raro.
- Pendiente de aprobación de commit: `web/staff.html` (mismo archivo, cuarto cambio encima de los de hoy), este documento.
- **Pendiente de confirmación visual del PO** (Regla 7) — el hallazgo que más directamente responde a su último reporte; probar la secuencia exacta que mencionó (Academia → Agenda → Config) y confirmar que ya no se nota ningún salto de posición vertical.

## [2026-09-03] "Academia se mueve a la izquierda" — tres rótulos distintos para las mismas pestañas, no solo espaciado
- **Reporte del PO**: "si estoy en agenda y voy a academia ahí hay un estirón... no debería moverse academia hacia la izquierda." Medido en vivo, mismo ancho de ventana (1440px, pestaña de navegador separada para no chocar con el agente de auditoría en curso): "Academia" aparecía en x=166 en `staff.html` (Agenda) contra x=146 en `academia.html` — 20px de diferencia horizontal real, no percepción.
- **Causa real encontrada al comparar pestaña por pestaña**: no era solo espaciado — eran ROTULOS DISTINTOS para el mismo puesto, según de qué página venías:
  - **Cash Flow**: `#mainNav` pinta "Cash Flow" (inglés, sin traducir a propósito); `.staff-topnav` traducía a "Flujo de Caja" (español) por un `data-i18n="nav-cashflow"` que sí dispara el traductor ahí, mientras en `#mainNav` esa misma clave nunca se traduce. El PO confirmó en vivo la razón de fondo: *"eso se unificó a Cash Flow para inglés y español porque es entendible en los dos idiomas y se eliminaba esa pestaña tan grande"* — confirma que el rótulo correcto es "Cash Flow" siempre, y que `.staff-topnav` era el que estaba mal.
  - **MI PERFIL**: `#mainNav` lo pinta invariable, sin key de i18n, por decisión del PO del 2026-08-16 ("el texto nunca cambia"). `.staff-topnav` lo traducía a "Mi Perfil"/"My Profile" en TRES lugares distintos (owner, seller, manager).
  - **⚙️ Config**: al icono le faltaba el selector de variación de emoji U+FE0F en la traducción propia de `.staff-topnav` (`⚙ Config`, se dibuja como glifo de texto angosto en vez de emoji completo) — el mismo bug ya documentado en 2026-08-16 para otro contexto, reaparecido aquí sin que nadie lo conectara.
- **El PO pidió explícitamente, en paralelo**: "también la ruedita ⚙️ debe ser la misma para todos los menús de Config" — se auditó el ÍCONO en TODO el sitio, no solo en `staff.html`: se encontraron dos páginas más con el bug real. `road-map.html` (dos ocurrencias, un mini-menú propio de esa página) tenía el gear SIN el selector por completo (`⚙ Config`); `artist-portal.html` y `mdj-event-cart-root-fragment.html` tenían el selector pero SIN el espacio antes de "CONFIG" (`⚙️CONFIG`, pegado). Las tres corregidas.
- **Corregido, 4 archivos**: `web/staff.html` (Cash Flow sin `data-i18n`, MI PERFIL en mayúsculas sin `data-i18n` en sus 3 apariciones, Config con el selector), `web/translations.js` (`staff-nav-config` con el selector en los dos idiomas), `web/road-map.html` (Config con selector + MI PERFIL en mayúsculas, mismo criterio), `web/artist-portal.html` y `web/mdj-event-cart-root-fragment.html` (espacio agregado). Verificado con grep en todo `web/*.html` y `web/*.js`: cero apariciones restantes del gear sin selector o sin espacio fuera de comentarios de código.
- **Verificado en vivo, mismo ancho de ventana, antes/después**: las 9 etiquetas de `.staff-topnav` ahora son textualmente IDÉNTICAS a las de `#mainNav` (Inicio, Academia, Agenda, ⚙️ Config, DJ Tools, Cash Flow, Staff, MI PERFIL, Fénix AI). El ancho de cada pestaña se acercó mucho más entre los dos sistemas (Cash Flow pasó de +35px de diferencia a -8.8px, por ejemplo).
- **Honesto sobre lo que NO quedó resuelto**: incluso con los rótulos ya idénticos, sigue midiéndose un desfase horizontal residual de ~8-9px por pestaña (más notorio acumulado en 8 separadores, ~68px de diferencia de ancho total de fila) que empuja el centrado automático (`justify-content: safe center`) a posiciones ligeramente distintas entre los dos sistemas. Se rastreó hasta el separador "/" entre pestañas: `#mainNav` usa `margin-right: 14px` fijo (`header-unified.css` ~línea 3534), `.staff-topnav` usa `margin-right: .68em` (13,6px a 20px de fuente) — una diferencia de apenas 0,4px que NO explica por sí sola el resto del hueco; queda una fuente de diferencia sin identificar del todo (posiblemente redondeo de sub-píxel acumulado, o un elemento del separador con métricas propias no encontradas todavía). Es un residuo mucho más chico que el problema original (que era de rótulos completos, no de píxeles), pero no se declara cerrado del todo.
- Pendiente de aprobación de commit: `web/staff.html`, `web/translations.js`, `web/road-map.html`, `web/artist-portal.html`, `web/mdj-event-cart-root-fragment.html`, este documento.
- **Pendiente de confirmación visual del PO** (Regla 7) — confirmar que el salto de "Academia" ya no se nota tanto; el residuo de ~8px puede seguir siendo perceptible en un ojo entrenado, hay que preguntar específicamente si todavía molesta antes de invertir más tiempo en cazarlo.

## [2026-09-03] "Todavía desde config veo flujo de caja" — el propio #mainNav era inconsistente CONSIGO MISMO entre páginas
- **Reporte del PO**: "todavía desde config veo flujo de caja" — a primera vista parecía que el arreglo anterior (unificar `.staff-topnav` con `#mainNav`) no había alcanzado. Medido en vivo: NO era `staff.html` — era el propio `#mainNav`, en `account-settings.html`, mostrando "Flujo de Caja"; el mismo `#mainNav`, en `academia.html`, mostrando "Cash Flow". La misma tabla (`MDJ_NAV_SLOTS_INTERNO`), el mismo puesto, dos rótulos según la página.
- **Causa raíz real**: el propio comentario de cabecera de `mdjb-shared-header.js` ya advertía esto exacto desde antes de hoy: *"Las claves nav-agenda, nav-flow y nav-fenix NO existen en translations.js... a propósito, para que i18n no les vacíe el rótulo al pasar."* Pese a esa advertencia escrita, las entradas de Agenda y Cash Flow en `MDJ_NAV_SLOTS_INTERNO` Y `MDJ_NAV_SLOTS_ARTISTA` SÍ tenían `key:'nav-agenda'`/`key:'nav-cashflow'` -- alguien las agregó en algún momento sin leer la advertencia, y en paralelo alguien completó las traducciones correspondientes en `translations.js` (`"nav-cashflow": "Flujo de Caja"` en español). El resultado depende de qué página corre un paso de traducción sobre el riel después de pintarlo: `academia.html` no lo hace nunca sobre ese nodo (por eso mostraba el "Cash Flow" crudo); `account-settings.html` sí, en algún punto de su propio código de "Settings" con mucho más texto traducible.
- **Corregido en la raíz, no por página**: los 4 `key` (`nav-agenda` x2, `nav-cashflow` x2, en `MDJ_NAV_SLOTS_INTERNO` y `MDJ_NAV_SLOTS_ARTISTA`) pasan a `key: null` -- el mismo patrón que ya usaba Fénix AI en esas mismas tablas. `mdjNormalizeMainNavSlots()` YA tenía el código para esto (`if (!def.key) el.removeAttribute('data-i18n');`, línea ~1353) -- no hizo falta escribir nada nuevo, solo dejar de pisar una protección que ya existía. De paso se blindó "Agenda" contra el mismo bug en modo inglés (`"nav-agenda": "Schedule"` en la traducción EN, nunca antes disparado porque nadie probó esta combinación en inglés).
- **Verificado en vivo, las dos páginas, antes/después**: `account-settings.html` pasó de "Flujo de Caja" a "Cash Flow" (con `data-i18n: null` confirmado en el nodo real); `academia.html` se mantuvo en "Cash Flow" sin cambios (verificación de que no se rompió lo que ya andaba bien).
- Pendiente de aprobación de commit: `web/mdjb-shared-header.js`, este documento.
- **Pendiente de confirmación visual del PO** (Regla 7).

## [2026-09-03] Cierre de la ronda de estabilización: separador con margen a los dos lados + orden Staff/MI PERFIL invertido
- **Orden del PO**: "corrige agenda se te va de lugar ya casi lo tienes" — y después, más exigente: "comprueba bien que al cambiar de pestañas ninguna se mueve de su lugar... no debe notarse el cambio."
- **Primer hallazgo — la causa real del ~8px residual que había quedado pendiente**: medido en vivo el separador "/" de `#mainNav` con `getComputedStyle(el,'::before')`: tiene margen a LOS DOS LADOS, 11.2px cada uno (22.4px total), estable en 1440px Y 1800px sin cambiar. `.staff-topnav` solo tenía `margin-right:.68em` (13.6px, sin margen izquierdo) — 8.8px menos de aire por separador, × 8 separadores por fila, es el ~70px de diferencia de ancho total que corría toda la fila hacia la derecha en el centrado automático. Corregido en `web/staff.html`: el separador ahora lleva `margin-left:11.2px;margin-right:11.2px`, igual que el maestro.
- **Segundo hallazgo, más grande, encontrado al auditar las 9 pestañas una por una tras el primer arreglo**: "Staff" y "MI PERFIL" estaban en ORDEN CONTRARIO entre las dos barras — medido en vivo a 1440px, "Staff" caía en x=933 en `#mainNav` pero en x=1091 en `.staff-topnav` (158px de diferencia, el salto más grande de toda la ronda); "MI PERFIL" al revés. Causa: el swap de estos dos puestos ya se había hecho en `MDJ_NAV_SLOTS_INTERNO` (`mdjb-shared-header.js`) el 2026-09-02 durante el "chequeo de posiciones", pero la tira propia de `staff.html` (HTML armado a mano en JS, no generado desde esa tabla) nunca se actualizó — quedó desalineada un día entero sin que nadie lo notara hasta esta auditoría exhaustiva. Corregido invirtiendo el orden de los dos botones en `web/staff.html`; verificado que el reordenamiento no rompió nada funcional (los botones enganchan por `data-top`, no por posición — clic en Staff y en MI PERFIL confirmados llevando a `data-vista="gobernanza"`/`"miperfil"` correctamente después del cambio).
- **Verificado en vivo, las 9 pestañas una por una, mismo ancho de ventana (1440px), antes/después**: todas quedaron dentro de 2-6px de diferencia (antes: hasta 158px en Staff/MI PERFIL, ~20px en el resto por el separador) — un margen ya imperceptible a simple vista. Posición vertical (`top`) confirmada en 0 en ambas. Capturas de pantalla de `academia.html` y `staff.html?vista=agenda` comparadas: mismo orden, mismas posiciones, sin superposición ni "doble pantalla".
- Pendiente de aprobación de commit: `web/staff.html`, este documento.
- **Pendiente de confirmación visual del PO** (Regla 7) — esta ronda fue la más exhaustiva de todas; pedirle específicamente que confirme si al cambiar de pestañas ya no nota NINGÚN movimiento, ni de posición, ni de tamaño, ni de rótulo.

## [2026-09-03] Pulido final a pedido del PO ("¿quieres dejarlo pulido al 100%?") — límite real encontrado
- Se midió el ancho de las 9 pestañas con precisión de sub-píxel (tres decimales) en las dos barras, mismo ancho de ventana. Resultado: 7 de 9 pestañas quedaron con una diferencia de exactamente **0.187px** entre sí — redondeo de sub-píxel del navegador, no una causa corregible (no hay ninguna regla de CSS que produzca esa cifra a propósito; es la textura normal de cómo el motor de render coloca texto en dos árboles DOM distintos). Es, en la práctica, cero: menos de un tercio del grosor de un cabello en pantalla.
- **Una pestaña sí tenía una diferencia real, encontrada en esta pasada**: "⚙️ CONFIG" — `#mainNav` la renderiza en MAYÚSCULAS de verdad (confirmado con `.textContent`, no es solo `text-transform` de CSS), pero `.staff-topnav` seguía con "⚙️ Config" en minúsculas por dentro. Corregido en `web/staff.html` y `web/translations.js` (`staff-nav-config`, los dos idiomas). Verificado en vivo: el texto ya sale en mayúsculas, pero el ancho de esa pestaña específica no se movió — indica que la diferencia de ancho de esa pestaña no era por el CASE del texto (que ya se veía transformado visualmente en los dos lados por CSS) sino por otra razón aún no identificada, de una magnitud similarmente mínima (2.6px).
- **Recomendación honesta**: no seguir cazando este residuo. `#mainNav` y `.staff-topnav` son y van a seguir siendo dos implementaciones de CSS independientes — llegar a una paridad matemáticamente perfecta entre las dos exigiría fusionarlas en un solo sistema, un trabajo de arquitectura mucho más grande que "ajustar un margen", y con más riesgo del que este pulido justifica. El resultado actual (0.2-2.6px de diferencia máxima, contra los 158px del hallazgo más grande de esta ronda) ya está por debajo de cualquier umbral perceptible a simple vista.
- Pendiente de aprobación de commit: `web/staff.html`, `web/translations.js`, este documento.
- **Pendiente de confirmación visual del PO** (Regla 7).

## [2026-09-03] "CONFIG y MI PERFIL activos a la vez" — encontrado con video real, corregido con evidencia, no con timing
- **El PO insistió (correctamente) en que quedaba un movimiento real** ("todavía cuando restauro la página hay un pequeño movimiento... a la derecha... verifica que no haya otra foto escondida") y mandó una segunda grabación real. Se procesó cuadro por cuadro con `ffmpeg` (extracción a 60fps, filmstrip de la franja del menú), igual método que resolvió el efecto chicle original.
- **Hallazgo confirmado visualmente, en el frame exacto de la transición Agenda→Config**: "CONFIG" y "MI PERFIL" aparecían los DOS marcados como activos (dorado, subrayado) al mismo tiempo, y el texto del riel se veía momentáneamente más grande/ancho que en el estado ya asentado — coincide con la descripción del PO ("estirón", "movimiento a la derecha").
- **Causa real del doble-activo, confirmada por lectura de código, no por timing**: en `MDJ_NAV_SLOTS_INTERNO`, MI PERFIL declara un href ESTÁTICO de respaldo (`./account-settings.html`) — IDÉNTICO al de CONFIG — porque su destino real para owner/staff (`./staff.html?vista=miperfil`) solo se resuelve de forma dinámica en el momento del clic (`mdjResolveMiPerfilHref()`), nunca se reescribe preventivamente en el atributo `href`. El detector de "pestaña activa" (`mdjNormalizeMainNavSlots()`, ~línea 1363) comparaba ese href crudo contra la página actual — al aterrizar en `account-settings.html` por CONFIG, MI PERFIL calzaba TAMBIÉN por pura coincidencia de compartir el mismo respaldo, sin que nadie hubiera tocado esa pestaña. No depende de qué tan rápido cargue la página: es un bug determinista, siempre presente para el rol owner/staff.
- **Corregido**: para el puesto `mi-portal` específicamente, el detector de activo ahora compara contra el destino YA RESUELTO por `mdjResolveMiPerfilHref()` (el mismo criterio con el que ya navega el clic real), no contra el href crudo sin actualizar. Verificado en vivo: en `account-settings.html`, solo "⚙️ CONFIG" queda marcado activo; "MI PERFIL" ya no.
- **Honesto sobre lo que NO se pudo confirmar del todo**: el "texto más grande/ancho" del frame de transición no se logró reproducir en este entorno de prueba (local, sin latencia real) — en cada intento, el tamaño de fuente correcto (20px) ya estaba puesto para cuando `#mainNav` tenía su primer hijo, sin fase intermedia de texto sin estilo. Investigando el motivo probable: `account-settings.html` es, por lejos, el archivo más pesado del sitio (más de 7.800 líneas) y `mdjb-shared-header.js` se carga al final del todo, justo antes de `</body>` — en una red o máquina más lenta que este entorno, esa distancia entre "la página empieza a pintar" y "el script que gobierna el riel llega a ejecutarse" podría ser justo la ventana que se ve en el video. No se movió el script de lugar en esta pasada: es un cambio de mayor riesgo (un archivo de más de 7.800 líneas, con dependencias entre scripts que no se auditaron todavía) y se prefirió no tocarlo sin confirmar primero si el arreglo del doble-activo ya resuelve lo que se ve, dado que un bug real y determinista competía con el mismo síntoma.
- Pendiente de aprobación de commit: `web/mdjb-shared-header.js`, este documento.
- **Pendiente de confirmación visual del PO** (Regla 7) — específicamente, si el "estirón"/movimiento a la derecha sigue viéndose igual de fuerte después de este arreglo, o si ya bajó de intensidad (lo cual apuntaría a que el doble-activo era la causa principal) — eso decide si vale la pena el riesgo de tocar el orden de carga de los scripts en la página más grande del sitio.
