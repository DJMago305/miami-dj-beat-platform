{
  "name": "miami-dj-beat-web",
  "private": true,
  "version": "1.0.0",
  "description": "Static HTML/CSS/JS; build copies site into ./build for Vercel output dir.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node vercel-build.cjs"
  }
}
