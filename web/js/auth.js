/**
 * Miami DJ Beat — Auth (login / signup / device check)
 *
 * Canonical implementation: `web/auth.js` (loaded by `login.html` and others).
 * Do not duplicate signup or `mdjCheckNewDevice` logic here; edit `web/auth.js` only.
 */
